## L1 — 라이브 줄 파편화: 같은 화자 인접 줄이 문장 중간에서 끊김 (2026-09-06, 0.3.13 라이브 → 0.3.14)

사용자 캡처(0.3.13, 00:05:06~05:16): Speaker 1의 한 문장이 "It was during the pandemic, so the story goes," / "and a bunch of early employees would come here, they'd pull chair on the grass," / "they'd have lunch," 세 줄로 갈라지고 각 조각이 따로 번역됨. 저장본(finalize 재그룹)에서는 한 줄이라 **라이브 화면만** 파편화.

**원인 추적**: 이벤트 파일(0.3.13, 단어 5,822·창 281) 재구성 결과 창 내부에 1.5 s 이상 단어 간격 0건 — 간격 규칙이 아니다. 줄 경계는 P15 원장이 첫 인접 시점에 한 번 판정하는데, 그 시점 새 창의 화자 id가 **잠정값**이고 ~24 s 뒤 SPKFIX가 이전 화자로 되돌려도 경계 판정은 남는다. 0.3.9 캡처(창 921) 정량: 초기 라벨 기준 화자 전환 110건 중 **23건(21 %)이 SPKFIX 후 이전 화자와 동일** = 라이브 과분할 경계.

**설계 선택 (측정으로 결정)**: ① "잠정 라벨(마진<0.35)이면 이어붙이기" — 되돌아간 23건 중 16건을 잡지만 **실제 전환 87건 중 44건도 마진<0.35**라 실제 화자 전환의 절반을 늦춘다 → 기각. ② **인접 두 줄이 사후(SPKFIX·AI 교정·고정 승격)에 같은 화자가 되고 앞 줄이 미완(문장부호 없음·간격<1.5 s·상한 미달·사용자 편집 없음)이면 그 경계만 재판정** — 고정 구간은 직접 합치고(생존 줄 id 유지, 번역은 stale로 보존), 꼬리 구간은 원장 항목만 지워 기존 재그룹이 합치게 함. 합친 줄이 16단어+절 소프트 상한을 넘기지 않도록 b 내부의 절 경계도 검사(finalize와 같은 규칙). `LiveFeatureWiring.joinSameSpeakerNeighbors`.

**게이트**(새 헤드리스 `CaptureFragmentationGateTests`: 캡처 stdout을 EngineProtocol.Decoder → TranscriptStore로 그대로 재생, `MADI_CAPTURE_STDOUT`):

| 0.3.9 캡처 47분 | 줄 | 같은 화자 미설명 분리* | ≤5단어 줄 | 단어/줄 p50 | 합침 |
|---|---|---|---|---|---|
| 라이브, join off (종전) | 261 | 55 | 87 | 8 | 0 |
| 라이브, join on | 244 | **42 (−24 %)** | **73** | 10 | 17 |
| finalize 재그룹(참조) | 263 | 22 | 89 | 9 | — |

*앞 줄이 문장부호로 안 끝나고, 16단어+절/28단어 상한·1.5 s 간격에도 해당하지 않는 같은 화자 인접 분리. 남은 20건 차이는 첫 디코드가 "."로 끝나 sentenceBreaks에 기록된 뒤 재디코드로 ","가 된 경계(P10-3 split-once, 의도)와 SPKFIX 시점에 고정/꼬리에 걸친 쌍.

단위 테스트 6건(꼬리 합침·고정 합침+번역 stale 보존·문장 종료 줄 유지·실제 전환 유지·사용자 편집 줄 불변·배선 off) + P15 구조 테스트를 "완결 줄" 계약으로 재정의. 619 통과.

부수: 자동저장 전 폴더 재생성(보강). **정정(같은 날)**: "폴더가 사라져 자동저장이 조용히 실패" 진단은 틀렸다 — 실제로는 사용자 설정 `autoSaveEnabled = 0`이었고 폴더도 `Madi 회의록/2026-06 프로젝트`로 존재했다(defaults 출력의 유니코드 이스케이프를 잘못 읽음). 라이브 구조 검증에는 자동저장이 아니라 캡처 게이트와 접근성 트리 읽기를 쓴다.

## M3 — transcribe 잔여 누수: «partial» 출력마다 디코드 텍스트 버퍼 미해제 (2026-09-06, 0.3.12 라이브 → 0.3.13)

0.3.12 라이브 22분: transcribe RSS 282 → 329 MB(**분당 2.3 MB, 선형**). M2로 10분의 1이 됐지만 남았다. 눈으로 짝을 맞추는 대신 **누수 검출 할당기 빌드**를 만들었다: `LEAK_CHECK = mode == ReleaseSafe or Debug`일 때 `alloc`이 `std.heap.DebugAllocator`(스택 12프레임)로 바뀌고, job 루프가 끝나면 `detectLeaks()`가 살아 있는 할당을 스택과 함께 찍는다. 배포 빌드(ReleaseFast)는 page_allocator 그대로.

첫 실행은 첫 job에서 **"Invalid free"** 패닉 — `transcribe.zig` `alloc.free(text)`의 `text`가 `bpeDecode`의 `buf.items`(용량 버퍼의 일부 뷰)였다. 디버그 할당기는 슬롯 시작이 아닌 포인터라 거부했고, page_allocator는 길이만 보고 조용히 넘겼다. 같은 함수의 다른 호출 2곳은 아예 free가 없었다: «partial» 출력(`ptext`, 디코드 배치마다 1회 → job당 여러 번)과 BATCHDEC 로그(`txt`). 잔여 누수 = **partial마다 page_allocator 한 페이지(16 KB)**.

수정: `bpeDecode`가 `toOwnedSlice()`로 정확한 크기의 소유 슬라이스를 돌려주고, `ptext`/`txt`를 `defer free`.

| 검증 | 결과 |
|---|---|
| 누수 검출 빌드, 프리뷰 0 → 60 job 뒤 살아 있는 할당 | 29 → **30**(1회성 버퍼 1개, job당 0.02) |
| 프리뷰 300회 RSS(프로덕션 빌드) | 272 → **273 MB**(M2 직후 278 → 288, M2 전 +0.14 MB/회) |
| `%%FP` 프리뷰 100회 | 272 → 272 MB |
| 같은 8 job 프리뷰 텍스트 | M2 전 기준과 동일 |
| P6 캡처 재생 게이트 | 독일어 0 %, `[prompt]` 1, rescue 0 |

### 0.3.12 라이브 22분 최종(`@final`)
stt n=283, p50 0.64 s / p95 1.21 s, 프리뷰 956회(분당 42.2), interimTurns 0. 패널 NE **0.24**(0.3.11 0.27, 0.3.10 0.43). 번역 지연 p50 0.85 s / p95 3.93 s(n=588; 0.3.11은 1.23 / 7.26). rescue 6(collapse 1, logprob 5), loop-p2 10/4, 독일어 0. 4B RSS 3,347 MB 22분 평탄(0.3.10 5,395). 화자 표시 번호 11분에 6 — **정답 기준(사용자 확인)**: 이 영상은 2인 인터뷰가 아니라 중간 등장 인물 포함 5~6명, 비중 큰 화자 3명. 따라서 6까지는 정상이고, 0.3.11의 10~11과 종료 직전 한 단어 조각에 새 번호가 붙는 것만 결함이다.

### 0.3.13 라이브 30분 최종(`@final`) — M3 검증
transcribe RSS **280 → 280 MB**(0.3.12 같은 구간 +47, 0.3.11 +340). stt n=365, p50 0.62 s / p95 0.80 s, 프리뷰 1,357회(분당 46.5), 패널 NE 0.26, 번역 p50 0.66 s / p95 2.49 s(n=749), rescue 10, 독일어 0. 4B 3,347 MB 평탄. 앱은 종료 처리(요약) 후 316~331 MB.

교훈: **page_allocator는 잘못된 free를 숨긴다**(부분 슬라이스 free = 조용한 no-op 또는 잘못된 길이 unmap). 라이브 상주 프로세스의 메모리 추세는 항상 수집기로 보고, 선형 증가가 보이면 합성 스트림 + 누수 검출 빌드로 30분 안에 지점을 찍을 수 있다.

## M2 — transcribe 메모리 누수: `%%FP` 프리뷰마다 BPE 어휘 재적재 (2026-09-06, 0.3.11 라이브 → 0.3.12)

0.3.11 라이브(영어 인터뷰, 시스템오디오)에서 transcribe RSS가 **271 → 625 MB / 35분**(초반 ≈23 MB/분)으로 선형 증가. 0.3.10 세션은 40분 내내 262~286 MB 평탄이었으므로 세션 의존 현상. `vmmap` 45초 차분: 증가분은 전부 `VM_ALLOCATE`(Zig page_allocator), 크기별로는 **1,360 KB 영역이 분당 +17개**.

| 합성 스트림 (같은 wav 반복, 100 job) | 수정 전 RSS 증가 | 수정 후 |
|---|---|---|
| 세그먼트 job | 0 (344 → 280, 워밍업 후 평탄) | — |
| PREVIEW (접두 없음) | +14 MB (0.14 MB/회) | +14 MB |
| PREVIEW `%%FP <텍스트>` | **+69 MB (0.69 MB/회)** | **+14 MB** |

**원인** (`transcribe.zig` `bpeEncode`): 호출마다 `loadBpe`로 어휘 파일 전체를 읽고 `StringHashMap`을 새로 만든 뒤 둘 다 해제하지 않았다. 세션당 1회 호출(글로서리 시드)일 때는 무해했지만, 라이브 러너는 꼬리 줄이 있는 **모든 PREVIEW job에 `%%FP` 강제 접두를 실어 보내므로(≈30/분)** 호출마다 0.69 MB가 쌓였다. 덤으로 `" " ++ ftxt` 스크래치 문자열도 page_allocator 한 페이지(16 KB)씩 누수. 0.3.10 세션이 평탄했던 이유는 세션 내용(긴 독백 → 꼬리 줄이 28단어 상한으로 자주 닫혀 `%%FP` 프리뷰가 적음)으로 추정 — 코드는 동일 바이너리 소스.

**수정**: 어휘 맵을 프로세스당 1회 적재(`g_bpe_map`, 경로가 프로세스 수명 동안 고정이라 정확), 스크래치 문자열 `defer free`. 출력 동일성: 같은 8 job의 프리뷰 텍스트 동일(conf ±0.01·시간은 Metal 비결정 노이즈). P6 캡처 재생 게이트: 독일어 0 %, rescue 0, `[prompt]` 1 — P6 결과와 동일.

잔여분 재측정(라이브 종료 후, 접두 없는 프리뷰 300회): 278 → 284 → 288 MB(110 → 221 → 300 job). 회당 0.05 MB이고 **감속** — 워밍업/할당기 안정화이지 선형 누수가 아니다. 프리뷰 레인은 이제 평탄.

### 0.3.11 라이브 47분 최종(안정성 로그 `@final`)
stt n=615, p50 0.68 s / p95 1.24 s, 프리뷰 1,862회(분당 37.9 = 누수 속도와 일치), interimTurns 0. **패널 NE 0.27**(0.3.9 0.42, 0.3.10 0.43) — 꼬리 줄을 프리뷰가 비워진 뒤에만 번역하는 배선(`4b218cd`)의 측정된 효과. 번역 지연 p50 1.23 s / p95 7.26 s(n=1131, 대화체 큐 밀림). 엔진: rescue collapse 5 + logprob 8(2 %), loop-p2 잘라냄 15/유지 7, 독일어 0. 화자 표시 번호는 6분 만에 10, 종료 직전 다시 10(한 단어 잔여 세그먼트가 새 id) — 다음 설계 대상.


## M1 — 메모리 실측 + DNA3 가중치 매핑 해제 (2026-09-05, 0.3.11)

사용자 질문: "900 MB 모델 + 4B 모델 + Madi = 4 GB 정도? 각 사용량, 대화가 길어져도 괜찮은지". 0.3.10 라이브(영어 40분, 48 GB M-series)에서 `ps`(RSS) + `vmmap -summary`(physical footprint) 실측.

| 프로세스 | RSS | physical footprint | 내역 |
|---|---|---|---|
| Madi.app | 139 → 192 MB (40분) | 197 MB | 트랜스크립트·속성문자열 누적 ≈ 1.3 MB/분, 이후 평탄 |
| transcribe (Whisper q8 900 MB) | 277~286 MB | 1.8 GB | IOAccelerator 1.6 GB = 가중치 + `ENC_F16_CACHE` ≈ 1.25 GB(**≥24 GB 기기에서만** 켜짐) |
| translate-engine-4b (DNA3 4B Q4_K_M) | **5,365 MB** | 3.3 GB | IOAccelerator 3.1 GB + **GGUF mmap 상주 2.5 GB(죽은 무게)** |

footprint 합계 ≈ 5.3 GB(이 기기), 8~16 GB 기기는 ENC 캐시가 없어 **≈ 4.05 GB** — 사용자 추정 4 GB와 일치. 40분 동안 엔진 둘은 평탄(4B는 턴마다 문맥을 새로 세우므로 대화 길이와 무관), 앱만 ≈ 50 MB 증가. 시스템 압축기 12~15 GB는 다른 앱(Docker VM·Chrome·Claude·DisplayLink).

**원인**: 4B 엔진은 `QwenModel.init`에서 모든 가중치를 Metal 버퍼로 올린 뒤에도 파일 매핑 전체를 상주로 안고 있었다. 이후 매핑을 읽는 곳은 토큰 임베딩 테이블(`dequantRow`, Q6_K 2560×248320 = 497 MB)뿐. 나머지 2.1 GB는 페이지 캐시가 눈치껏 회수하기 전까지 Activity Monitor에 그대로 보이는 두 번째 모델 복사본이었다(라이브 중 내 벤치가 메모리를 쓰자 5,364 → 3,928 MB로 저절로 줄어드는 것이 관측됨 = 회수 가능한 죽은 무게라는 증거).

**레버(sovereignLLM `b103b44`)**: init 직후 임베딩 테이블 앞뒤의 텐서 영역을 `munmap`. 테이블 범위는 양자화 타입 산술이 아니라 **파일 순서상 다음 텐서의 오프셋**으로 잡아 잘못된 행 크기가 살아있는 행을 풀 수 없게 했다. 헤더/어휘 영역은 유지(어휘 문자열이 슬라이스). `SOV_KEEP_MMAP=1`이면 종전 동작.

| 엔진 (t1_harness, 동일 `%%TRN` 3턴) | RSS 3턴 후 | footprint | 번역 출력 |
|---|---|---|---|
| 4B 기존 → 신규 | 5,368 → **2,516 MB** | 3.3 G → 3.3 G | 6턴 바이트 동일 |
| 2B 기존 → 신규 | 2,534 → **1,727 MB** | 1.6 G → 1.6 G | 6턴 바이트 동일 |

반증 2건: ① `madvise(MADV_DONTNEED)` — macOS의 private 파일 매핑에는 **아무 효과 없음**(RSS 변화 0). ② 테이블을 힙으로 복사하고 전부 unmap — RSS는 같이 줄지만 **footprint +0.4 GB**(회수 가능한 clean 페이지가 dirty 페이지로 바뀜). 8 GB 기기에서 진짜 압력은 footprint라 기각.

남은 레버: `ENC_F16_CACHE` 1.25 GB는 ≥24 GB 기기 한정이라 최소사양과 무관. transcribe/4B의 IOAccelerator는 가중치 자체라 손댈 곳 없음. 앱의 분당 1.3 MB는 8시간 기준 ≈ 600 MB — 문제 아니지만 세션 요약·내보내기 후 트랜스크립트 축약이 다음 후보.

## P6 — "독일어 오염" 근본 원인 = 글로서리 바이어스 프롬프트 (2026-09-05, 0.3.10)

0.3.7~0.3.9 영어 다큐 라이브 4세션에서 세그먼트 29~53 %가 독일어(자신 있는 lp −0.37 포함), 세션 시간에 따라 증가(0.3.9: 43→46→56→67 %).
P5 언어 수정은 의도대로 동작(56/56 en 토큰)했지만 증상은 그대로 → 언어 가설 반증.

### 오프라인 재현 실패 5종 → 라이브 스트림 캡처
같은 세그먼트 파일·같은 오프셋·같은 엔진으로 재생: **0 %**. 환경 플래그(DIAR/OSD/PARTIALS/ENC_F16_CACHE/APP_FILE) 각각, 프리뷰 4회 끼워넣기(+%%FP),
실시간 5 s 페이싱 + 프리뷰 + 화자분리 + 4B GPU 동시 부하, 같은 세그먼트 80회 누적 — **전부 0 %**. 추측을 멈추고 앱→엔진 스트림을 그대로 기록하는
tee shim(`bench/live_capture/tee_transcribe.py`; 앱이 ad-hoc 서명이라 헬퍼 교체 가능)을 설치본에 끼워 stdin(타임스탬프)·feed 시점 wav 스냅샷·stdout 전문·env를 캡처.

| 캡처 93세그먼트 정확 재생 (`replay_capture.py`) | 독일어 | 재디코딩 | 라이브와 동일 텍스트 |
|---|---:|---:|---:|
| 라이브 실제 | 27 % | 39 | — |
| 기준 엔진, 캡처 env 그대로 | 27 % | 38 | **89/93** |
| 기준 엔진, `PROMPT` 제거 | **0 %** | **0** | — |
| P6 엔진, 캡처 env 그대로 | **0 %** | **0** | — |

라이브 env: `PROMPT=opensource istio lambda` — S4 글로서리→`<|startofprev|>` 바이어스(FLEURS-ko 한국어 항목에서만 측정된 승리). 오프라인이 계속 깨끗했던
이유는 재생에 글로서리를 넣지 않아서였다. 변형 실험(19세그): `opensource` 단독 재디코딩 3, `istio` 독일어 5 %·재디코딩 5, `istio lambda` 5 %/4, 문장형
"We talked about …" 0 %/5, `오픈소스 istio lambda` 11 %/6 — **비어 있을 때만 0/0**. 영어 세션에서 이 메커니즘은 어떤 형태든 해롭다.

### 수정
- 엔진: `promptAllowed(seg_lang)` — `PROMPT_LANGS`(기본 `50264`=ko, `*`=전부)에 든 언어에만 접두 적용. 프로브가 `d_tokens[0]`(PROMPT 시 `<|startofprev|>`)을
  임베딩하던 버그 → SOT 시드. `%%FP` 없는 PREVIEW의 잔존 강제 접두 리셋.
- 앱: P5가 `[lang] detected token`→`locked token`으로 바꿔 파서가 못 읽음 → **0.3.8/0.3.9는 `.languageDetected`가 안 떠 프리뷰 레인이 시작되지 않았다**
  (캡처: SEG 48 / PREVIEW 0). 두 문구 모두 파싱 + 테스트.
- 앱: 엔진 진단 줄(`[lang]/[rescue]/[loop-p2]/[prompt]/[warn]`)을 `EngineDiagnostics`로 접어 `@final` 안정성 로그에 기록(`eng lang=… prompt=… rescue=… loopp2=…`).
  이 줄이 있었다면 첫 세션에서 `[prompt] biasing 3 word(s)` + 재디코딩 39회로 30분 만에 끝났을 일.

### 회귀 게이트 (기준 vs P6)
| 코퍼스 | 기준 | P6 | 변경 |
|---|---:|---:|---:|
| FLEURS-ko 382 (언어 강제, 글로서리 없음) | CER 5.63 % | **5.62 %** | 2발화 |
| LibriSpeech test-other 300 | WER 6.09 % | **6.09 %** | 0 |
| jfk / jfk3 | — | 동일 | 0 |
| ko1·ko2·ko4 턴 25 | — | 동일 | 0 |
| ko 턴 25 + 한국어 글로서리 `PROMPT="쿠버네티스 마이그레이션 스케일링"` | — | **동일**(KO에는 접두 계속 적용) | 0 |

주의: 기존 코퍼스는 언어를 강제하고 글로서리가 없어 이 회귀를 **구조적으로 못 잡는다**. 영어+글로서리 회귀 게이트 = 캡처 재생
(`bench/live_capture/replay_capture.py <capture> --engine …`, 기준 27 % → 0 %).

### 같은 세션의 다른 지표(0.3.9, 임시 번역 미배선 후)
`interimTurns=0`(의도대로), 자막 삭제 0. 패널 NE 0.42(삭제 6,581 / 개정 5,198 / 최종 15,604자) — 원문의 1/3이 재디코딩된 쓰레기라 개정이 연쇄. STT p50 0.87 s, p95 2.54 s (n=218).

## P5 — 0.3.7 라이브에서 터진 언어 오탐 + 반복 사각지대 (2026-09-04, 0.3.8)

영어 다큐 40분 라이브. 사용자 보고 "폭파되고 삭제되고". 엔진 이벤트 75세그먼트 분석:

| 지표 | 값 | 정상 |
|---|---:|---:|
| 독일어 오염 세그먼트 | 25/75 (33 %) | 0 |
| 저품질 재디코딩(logprob rescue) | 30/75 (40 %) | <1/300 |
| avg_logprob 중앙값 / 최악 | −0.49 / −2.39 | −0.1 근처 |
| 폐기 세그먼트 | 0 | — |

실제 출력: `es hilft, es hilft, es heilt die` / `wenn du mit Arschlusser gewinnen, wer gewinnt?` /
`wie die Veränderung der GmbH im Regen ist. Wann der Modell werden sehr gefährlich.`
첫 세그먼트(0.0 s)는 깨끗한 영어(lp −0.37)였고 15 s부터 무너진다.

### 원인 — 언어 경로가 틀린 답에 갇히는 두 가지 길
1. **첫 탐지가 세그먼트 하나로 세션 전체를 확정**(`transcribe.zig` 첫-탐지 분기). 신뢰도 요건 없음 →
   오프닝 음악·박수·타이틀카드 한 조각이 세션 전체를 결정.
2. **세그먼트별 재탐지 후보 = `{세션 락} ∪ {번역 타깃 유도 후보}`**. 타깃이 {일,한}이면 **영어가 어느 집합에도
   없다** → 영어 원본이 한 번 이탈하면 정답이 탐색 집합에 없어 복귀 불가.

### 수정
- 락에 `LANG_LOCK_MARGIN`(1.0) 요구, 미달이면 열어두고 다음 세그먼트가 재판단(`LANG_LOCK_TRIES` 4로 상한).
- **영어(50259)를 재탐지 후보에 상시 포함**.
- 전환에 `LANG_SWITCH_MARGIN`(1.0) 요구 — 매 세그먼트 argmax는 음악/무음에 흔들리고, 흔들릴 때마다 실제 발화가
  틀린 토큰으로 디코딩된다.
- **seg 이벤트에 `lang`/`lang_margin` 기록** — 직전 세션은 어느 세그먼트가 무슨 언어였는지 말할 수 없었다.

### 반복 가드 사각지대
`how do we train good models` ×3(주기 ≈7토큰)이 확정 텍스트에 진입. `tokenCollapse`는 p=7에 5회를 요구하고
`tokenPeriod2`는 p≥9부터였다 → **p 4..8에 2~3회가 정확히 빈 구간**. 짧은 주기는 3회부터(run ≥ 2p),
긴 주기는 기존 2회 유지. 절단 여부는 여전히 크로스어텐션 동의(`loopAdvances`) 필요.

### 앱 — 문장 중간 화살표
`stripPromptMarkerEcho`가 끝의 `=>`만 제거했다. 라이브에서 `…우리가 어떻게 ->` 처럼 중간에 남는다 →
`stripInlineArrows`(공백 구분 토큰만; `a->b` 는 보존).

### 게이트 (base vs P5)
| 코퍼스 | base | P5 | 변경 |
|---|---:|---:|---:|
| FLEURS-ko 382 | CER 5.64 % | **5.62 %** | 1발화 |
| LibriSpeech test-other 300 | WER 6.09 % | **6.09 %** | 1발화(쉼표) |
| jfk / jfk3 / ko1·ko2·ko4 턴 | — | 바이트 동일 | 0 |

**주의**: 두 코퍼스 모두 `WHISPER_LANG_ID`를 강제하므로 언어 로직이 거의 발동하지 않는다. 즉 이 표는
**무회귀 게이트이지 수정의 증명이 아니다.** 증명은 다국어 라이브 세션 + 새 `lang` 이벤트 필드로 한다.

### 같은 세션에서 확인된 나머지 (미수정)
- 번역 개정 폭주: 패널 rev 2,214 / erased 2,546, NE 0.39 — **역대 최저**(0.3.5 0.47, 0.3.2 0.57)지만 절대치는 여전히 나쁘다.
  임시 턴이 26 → 238회(9×)로 늘어(P4 기아 밸브) 총량 대비 삭제는 줄었어도 **화면 교체 장면은 더 자주 보인다**.
- 후처리 요약: 멈추지 않고 완료되나 40분 분량에 문장 3개. 원인은 원고의 1/3이 환각인 것 + 다큐에 **회의** 템플릿 사용.
- 통계 패널이 40분 세션을 "0분/10단어"로 표시 — 자동저장 OFF라 워크스페이스 파일이 없어서.
- CPU/GPU: madi는 시스템 CPU의 **18 %**(WindowServer 44.8, madi 앱 34.2, 번역 10.0, 전사 1.4).
  GPU는 device 97 % / **renderer 98 %** — 컴퓨트가 아니라 화면 합성 포화. 메모리 6.06 GB(번역 4B가 5.5 GB), 압축기 8.0 GB, 세션 중 증가 없음.

## P4 — 0.3.6 라이브 검증(한국어 40분) + 새로 드러난 3건 (2026-09-03)

유튜브 한국어 1인 진행 영상, KO→EN·日 2타깃, 40분. 계측 `bench/wer_runs/prof/samples6.csv`·`mt6/`(2분 간격 스택 21개).

### P2/P3 검증 — 전부 확인됨
| 메인 스레드 심볼(누적 47,586 샘플) | 0.3.5 @33분 | 0.3.6 @40분 |
|---|---:|---:|
| 유휴 | 15 % | **57.4 %** |
| `engine(didEmit:)` | 25.9 % | **0.2 %** |
| ㄴ `translateStableLines` | 18.3 % | 0.1 % |
| ㄴ `TranscriptStore.ingest` | 7.3 % | 0.0 % |
| `ContentView.body` | 10.7 % | 1.8 % |
| ㄴ `flaggedWords` | 9.1 % | **0** |

앱 CPU 5분 버킷 30.1 → 32.5 → 40.8 → 38.2 → 46.4 → 36.2 → 44.9 → 31.0 % — **줄 수 비례 성장 사라짐**(0.3.5는 37→45→57 단조 증가). RSS 184→218 MB.
T5 예시 에코·회색 텍스트 중복·STT 2회 반복 루프 = 40분간 **0회 관찰**(0.3.5 37분에 각각 5·6·1회).

### 새로 드러난 3건
| # | 증상(라이브) | 원인 | 조치 |
|---|---|---|---|
| P4-1 | **실시간 요약이 00:01:24 이후 28분간 갱신 0회** | `SessionController.tickLiveSummary`/`tickLiveRail`이 `translateQueueDepth == 0 && translateBacklog == 0`일 때만 실행. 빠른 화자 2타깃에서 큐는 영영 0이 안 됨 → "한가할 때만" = "영영 안 함" | `BackgroundLaneAdmission`: 유휴 fast path + **90 s 기아 밸브**(브로커 `backgroundAgingSeconds`와 동일). 브로커 우선순위는 그대로라 *언제 큐에 넣는지*만 바뀌고 *어떤 순서로 서빙하는지*는 불변. 스킵 틱·기아 승인 수를 stability.log에 기록 |
| P4-2 | **커밋된 줄이 무한히 자람** — 250자 넘는 줄이 1분 전 내용의 번역을 달고 있음 | `groupLoop`의 분리 규칙이 1.5 s 침묵·화자 변경·문장부호뿐. 한국어 Whisper는 문장 중간 구두점을 내지 않고 진행자는 1.5 s를 안 쉼. `translateStableLines`는 마지막 줄을 빼고 디스패치하므로 자라는 꼬리 줄은 **자라는 동안 절대 번역되지 않고**, 새 단어마다 리비전이 바뀌어 이미 온 번역도 stale | 길이/시간 상한. 사용자 저장 전사록 8세션 952줄 실측: p50 5단어/2 s, p95 20/11 s, p99 41/36 s, **최악 1753단어·567 s가 한 줄**. 28단어 또는 14 s 상한 = 실제 줄의 2.6 %/2.1 %만 분리. 16단어 이후엔 쉼표를 경계로 선호. 판정은 P15 원장을 거쳐 **한 번만 결정되고 재생** |
| P4-3 | **1인 영상에서 화자 10명**(28분 시점 4·1·2·6·3번이 27/24/23/14/11 %로 균등 분할), 거의 모든 줄이 "화자분리중…" | 에이전트 조사(보고서 아래) — ① `transcribe.zig:948`이 recluster의 id 발행을 `max_k`가 아닌 리터럴 32로 제한(온라인 출생 경로 `:772`는 `max_k` 준수) ② 앱 표시번호는 append-only이고 엔진에 "id 개명" 이벤트가 없음 ③ margin이 낮게 유지돼 "화자분리중"이 안 풀림 | 아래 실측 참조. **엔진 수정은 보류**, 앱 쪽 파급만 차단(P4-4) |
| P4-4 | 화자 churn이 번역 처리량을 깎음 | `TranslationCoalescer.runs`가 조각 접기를 화자 동일성으로 판단 → churn마다 조각이 독자 턴을 소비. 관측 07:20~07:25 패턴에서 2타깃 기준 **10턴 vs 안정 라벨 6턴** | 미결정 라벨(`SpeakerID.isDeciding`)은 run을 쪼개지 않게. 확정 라벨은 그대로 분리하고 `maxGap`·`maxJoinedChars`가 계속 경계를 지킴 |

### P4-3 화자 실측 (`bench/diar_panel_eval.py`, VoxConverse + ko4, live_stream)
| 케이스 | 참조 화자 | 기준 엔진 | D1(recluster id를 `max_k`로 제한) | DIAR_SIM 0.30 | 0.35 | 0.50 | 0.55 |
|---|---:|---|---|---|---|---|---|
| xypdm (1인, 417 s — **관측과 같은 형태**) | 1 | 5명 / DER 6.49 | 5명 / 6.49 | 4명 / 6.34 | 4명 / 6.34 | 5명 / 6.49 | 6명 / 7.01 |
| ufpel (10인, 401 s — 위험 케이스) | 10 | 9명 / 11.29 | **8명** / 11.29 (relabel DER 10.92→11.56) | 7명 / 12.67 | 7명 / 11.85 | 9명 / 11.75 | 9명 / 11.75 |
| ko4 | 4 | 1명 / 6.47 | 1명 / 6.47 | 변화 없음 | 변화 없음 | 변화 없음 | 변화 없음 |

**두 레버 모두 반증.** D1은 계약(≤`DIAR_MAXK`)을 실제로 지키게 만들지만(ufpel 9→8) **관측된 1인 과분할을 전혀 못 고치고**(xypdm 5명 그대로), 참조 화자가 상한을 넘는 파일에서 relabel DER를 0.64 pt 악화시킨다 → **미머지**, 패치 보관 `bench/runs/p4/d1_recluster_cap.patch`. `DIAR_SIM` 하향은 xypdm을 5→4로 줄이는 대신 ufpel DER를 11.29→11.85/12.67로 악화 → **상수 하나로는 안 됨**.
**후보 A 반증·후보 B 부분 채택 (09-03 착수)**: 설계 `engine/metal/DIAR_OVERSPLIT_DESIGN.md`.
- **A(잠정 centroid를 배정에서도 격리) 반증**: xypdm 5명 그대로, ufpel DER 11.29→12.67. 추적으로 원인 판명 —
  xypdm의 **온라인 출생은 id 0·1 두 개뿐**이고 유령 4개는 전부 `liveRecluster` write-back이 찍은 것이라 출생 경로를
  손대는 방향 자체가 빗나갔다. 패치 보관 `bench/runs/p4/provA_two_tier_scan.patch`.
- **원인 확정**: `DIAR_K_TRACE` 로 본 auto-K 이력은 2,1,2,2,6,6,4,4 → **m=136 이후 13회 연속 K=1**(최고 실루엣 0.148,
  `DIAR_SIL_TAU` 0.35 한참 아래). 재클러스터는 정답을 알고 있었고, livefix 의 broad 되펼침(`:2094`)이 창 7개(2.1 %)·
  3개(0.9 %)짜리 유령을 라벨 후보로 되살렸다. 설계의 "min_sep 붕괴일 때만 차단"은 실측 붕괴 사유가 실루엣이라 발동하지 않는다.
- **B(되펼침 후보에 점유율 하한 3 %) 채택**: 커밋 7d77166. 1차 구현은 하한을 active 목록에도 적용해 ufpel 진짜 소수
  화자를 지워 DER +1.38 → **하한은 fallback 목록만 게이팅**하도록 수정(재클러스터의 결정은 점유율로 재심하지 않는다).

| 케이스 | 참조 | 화자 수(라이브/최종) | DER(라이브/최종) | 오라벨 노출 |
|---|---:|---|---|---|
| xypdm 1인 | 1 | 5→5 / **3→1** | 6.49→6.47 / **6.34→5.51** | **4.61 %→1.19 %** |
| ufpel 10인 | 10 | 9→9 / 7→7 | 11.29→11.29 / 10.92→10.92 | 13.40→13.40 |
| ko2 / ko4 | 2·4 | 불변 | 불변 | — |

**미해결**: 녹음 중 보이는 화자 수(xypdm 5명)는 온라인 per-window 경로에서 나오므로 B가 건드리지 못한다. 설계의 합격선
(화자 ≤2, overcount ≤1)은 미달 — 부분 채택이며 회귀는 0. 점유율만으로 유령과 진짜 소수 화자를 가를 수 없다는 것도 실측됨
(xypdm 유령 2.1 %·0.9 % vs ufpel 진짜 화자 1.7 %~3.3 %, 구간 중첩) — 다음 레버는 점유율 임계가 아니어야 한다.

남은 후보(미착수, 위험 순): ① 신규 centroid를 provisional로 두고 독립 근거(≥3창·≥3 s) 확보 전엔 다른 창을 claim 못 하게(`transcribe.zig:771-786`) ② auto-K가 K를 접었을 때 라벨링의 broad 폴백이 되펼치지 않게(`:2094-2097`, 패널 under-split 회귀 위험 최대) ③ recluster 후 centroid 병합 패스 + `SPKMERGE` 이벤트로 앱 표시번호까지 접기.
게이트: `bench/diar_panel_eval.py --audio … --ref … --mode auto --live` + `bench/live_ux_gate.py --require-win`(`speaker_overcount_peak` 한계 0.00 이미 내장).

게이트: swift test 602/602, 앱 빌드. 잔여 = 0.3.7 라이브에서 요약 갱신 주기·최장 줄 길이·화자 수 재확인.

## P3 — 0.3.5 세션 B(37분 관찰) 후속: STT 2회 반복 루프 가드 + 앱 "문맥 길이 vs CPU" 감사 (2026-09-03)

### P3-STT — 문장 길이 주기 ×2 반복이 확정 텍스트로 커밋 (엔진, 머지 대상)
라이브 13:35 "…hip hop and why are those" 20단어 주기 ×2 + 꼬리가 회색이 아닌 **확정** 줄로 들어왔다. 기존 가드 6종
(단일토큰 ×3, NRNG 창 16, tokenCollapse p≤8 / p 9..48 run≥2p, 4-gram 다양성 0.35, logprob −1.0)은 전부 "3회 이상"
기준이라 2회 복사(다양성 0.54)를 통과시키고, whisper.cpp 엔트로피 게이트(2.74>2.4)도 놓친다. OpenAI gzip CR(2.62>2.4)만 잡는다.
텍스트만 보는 2회 규칙은 jfk3(진짜 3회 반복)를 자른다 → 두 번째 복사가 **오디오에서 전진했는지**를 원시 크로스어텐션 argmax
프레임(정렬 헤드 6개 합)으로 판정: 중앙값 이동 ≥ 첫 복사 스프레드의 ½(≥10프레임) 그리고 두 번째 스프레드 ≥ 첫 스프레드의 40 %면
진짜, 아니면 첫 복사 끝에서 절단(재디코드는 같은 루프를 재생). `tokenPeriod2`/`loopAdvances`/`attnArgmaxFrame`, `LOOP_P2=0` 끔.

| 계측 (`bench/wer_runs/p3_run.py` + `p3/chain.sh`, 기준 vs 새 엔진) | 기준 | 새 엔진 |
|---|---:|---:|
| LibriSpeech test-other 300 WER | 6.09 % | 6.09 % (가설 변경 0/300) |
| FLEURS-ko 382 CER | 5.62 % | 5.63 % (1발화 띄어쓰기 1칸, 가드 이벤트 0) |
| jfk / jfk3 | — | 출력 동일; jfk3 진짜 3회 반복은 주기 26으로 검출됐지만 **유지**(shift 537 fr, span 355/373) |
| 유도 케이스 AUDIO_CTX=640 ko1_t01 | "…되었습니다." ×2 커밋(가드 0건) | 한 문장으로 절단(shift 0, span 104/114) |
| 유도 케이스 AUDIO_CTX=512 ko1 t00/t01 | ×N 루프 → ts 재디코드(rescue 2) | 첫 복사에서 절단(443→21/25 토큰), 최종 텍스트 동일, GPU 패스 1회 절약 |

앱 쪽 `WordMerger.isRunawayRepeat`(p≤6단어, 5회 이상)는 그대로 — 엔진 절단이 1차, 장주기 앱 방어선(C)은 텍스트만 봐서 jfk3류를
자르므로 보류. 회귀 픽스처 후보: LibriSpeech test-other `1688-142285-0087`(s3 hyp 모드에서 2회 복사, rescue 0). quark atom:
`sovereign_metal_whisper file__transcribe.zig/fn__tokenPeriod2`.

### P3-APP — "문맥이 길어질수록 CPU 증가" 감사 (워크플로 32 에이전트, 반증 12건, 보고서 `bench/wer_runs/prof/wf3_context_growth_report.md`)
0.3.5 세션 A 메인 스레드 샘플 12.5→33분(줄 ~135→~355): 성장분은 전부 `engine(didEmit:)` 142→534(=`translateStableLines` 89→376,
`ingest` 51→151, `Line` 복사/`joinedText` 재조립) + `flaggedWords` 116→187(UserDefaults 단어당 읽기) = 33분 메인 스레드 35 %.
**P2(#275)가 이 항목을 전부 덮는다**(윈도우·tail-only·캐시 → 잔여 3~4 % 추정). 나머지 41 %는 줄 수와 무관한 **고정 바닥**
= 루트 `ContentView.body`가 프레임 단위로 재실행(30 Hz displayLines + 12.5 Hz streamingTranslation + speakerNumbers 단어당 대입)
× 창 전체 diff/layout. 세션 B(화자 1명, 줄 느리게 증가)는 24분까지 CPU 31→41 %, 유휴 54~68 %로 같은 결론.

| # | 잔여 원인 | 보정 지분 | 설계 요지 |
|---|---|---:|---|
| P3-1 | 루트 body가 `displayLines`/`livePartial`/`streamingTranslation`/`translateQueueDepth`/`speakerNumbers`를 직접 읽음 + 매 패스 새 클로저 7개로 `TranscriptView` 재실행 | 고정 41 % 중 직접 귀속 16 % | TranscriptPane 추출, `displayIsEmpty`/`displaySpeakerIDs` 플래그, 액션 참조 동일성, 등가 가드(`streamingTranslation`, `speakerNumbers`), dead prop 제거 |
| P3-2 | 내용 모드(기본) `blocks`가 body마다 전 줄 재조립 O(N), 블록마다 `blockTranslations` O(N), RowHost 없음 | 투영 5~11 %(미계측 — 0.3.5 샘플은 전부 상세 모드) | 저장형 `Line.text`, 스토어가 splice 이후 블록만 재계산해 `displayBlocks` 발행, 블록 RowHost |
| P3-3 | `EnergyArcView` 캔버스가 루트 패스마다 재그리기(동적 색 resolve가 지배) | 2.6 % | Snapshot Equatable + `.equatable()`, 색 1회 resolve, 미터 분리 |
| P3-4 | tail 줄당 willSet 6~7회 + `speakerNumbers.assignAll` 단어마다 | <1 %(+루트 패스 유발) | 로컬 배열에 오버레이 적용 후 `replaceSubrange` 1회 |
| P3-5 | 32줄 창 내부 상수(joinedText 재조립·Line 복사·hash) | 1.7 % | 저장형 text + 더티 집합(마지막 줄 지위 상실/텍스트 변경 시만) |
| P3-6 | `Theme.confThreshold` UserDefaults 호출당 읽기(TranscriptView 잠복) | ~0(리뷰 모드에서 재출현) | body 진입 시 1회 읽어 전달 |
| P3-7 | tail `refreshTranslationOverlay` 해시+filter 3회 | <0.5 % | P3-2/P3-4에 흡수 |

반증(요지): 인터림 번역 토큰당 무효화(80 ms 게이트+등가 가드, 샘플 0), 타이프라이터 24 ms 틱 O(N)(RowHost로 마지막 행만),
LoadingDots 루프, SPKOV 전 줄 재투영(희소, 0.11 ms), displayLines COW 복사(0.07 %), 스트리밍 결과 O(N) id 스캔(0.15 %).
0.3.6 합격 기준: 10분 버킷 앱 CPU 평탄(±5 %p), 33분 샘플 `didEmit` ≤90·`flaggedWords` ≤5·유휴 ≥35 %; 상세+내용 모드 각 1샘플.

## P2 — 라이브 0.3.5 재계측에서 드러난 3건: 줄 수 비례 메인 스레드 비용·예시 번역 에코·실시간 텍스트 중복 (2026-09-03)

P1 재계측(위 표) 37분 세션의 메인 스레드 샘플과 화면 관찰에서 나온 항목. PR #275.

| # | 원인(코드) | 수정 | 계측 |
|---|---|---|---|
| C1 | `TranscriptStore.rebuildLive`: 단어마다 `lines = frozen + tail`(전 줄 복사) + `applyOverlays`(전 줄 joinedText 해시·번역 오버레이) + `applyOverlapSpeakers`(전 줄) + `merger.displayWords`(전 단어 복사) | 동결 줄 = `lines[0..<frozenCount]` 자체(별도 base 배열 없음). 변경은 이미 `lines[i]`에 제자리로 착지하므로 재빌드는 꼬리만 `replaceSubrange`, 오버레이는 스플라이스 지점부터; `displayWords(from:)` | 헤드리스 벤치(`TranscriptStorePerfTests`, 501줄에서 마지막 200단어): **3.718 → 0.585 ms/단어(−84 %)**, 전체 재계산과 동일성 검증 |
| C2 | `SessionController.translateStableLines`가 단어마다 전 줄 텍스트 해시 + 코알레서 전 줄 실행(라이브 18 %) | 마지막 32줄 창만(끝내기는 전체) — 커밋 cac6511(#274에 포함) | 라이브 재계측 대기 |
| C3 | `ContentView.flaggedWords`가 30 fps 스냅샷마다 전 단어 순회 + 단어당 UserDefaults 읽기(`Theme.confThreshold`, 18 %) | 스토어에 표시 리비전·임계값 키 캐시(`flaggedWords(threshold:)`) | 라이브 재계측 대기 |
| C4 | T5 예시 쌍이 진짜 번역이면 4B가 **예시 번역을 먼저(또는 대신) 재생** — "Yeah.=>是的。" 뒤 "是的。因为你被放到了29号位…", 이전 문단 전체 한국어, "only."에 이전 줄 중국어 전문 | `TranslationOutputPolicy.stripExampleEcho`(정규화 비교·원문 위치 절단), 스트리밍은 예시 접두인 동안 보류(`mayBeExampleEcho`), 전체 에코는 소스 대비 비현실적 길이일 때만 폐기→repair 재시도(`exampleTarget`를 턴에 보관) | 라이브 사례 5건 단위 테스트(`TranslationExampleEchoTests`) |
| C5 | 회색 실시간 텍스트 중복 제거가 **정확한 접미/접두 일치**만 — 경계 단어 재디코드("…way and" vs "way. A lot"), 여러 커밋 줄에 걸친 창("Where's your ring? … No, I'm gonna")이면 문장 전체가 두 번 | `InterimDedupe`: 최장 공통 연속 블록 정렬(≥3토큰, 꼬리 끝 근처일 때만) + 잔여에 기존 정확 규칙 | 라이브 사례 6건 단위 테스트(`InterimDedupeTests`), 중간 반복 3-gram 오탐 방지 테스트 |

게이트: swift test 589/589, 앱 빌드(0.3.6 로컬 DMG). 라이브 재계측 = 0.3.6 같은 영상에서 10분 구간별 앱 CPU가 평탄한지(37→57 % 성장 해소), 예시 에코·회색 중복 0회.

## P1 — 번역 백로그·프리뷰 기아·줄 정체성 (2026-09-03)

P0(#273) 다음 순위. 라이브 관찰(2타깃·빠른 화자)에서 대기 줄이 5→20(70초)까지 자라고, 회색 실시간 텍스트는 세션 내내 안 보였으며,
세그먼트 경계마다 줄 id가 바뀌어 커밋 후 병합·번역 고아가 생겼다.

| # | 원인(코드) | 수정 | 게이트 |
|---|---|---|---|
| T1 | 번역 단위 = 표시 줄: 조각 줄("And", "tools.")마다 타깃 수만큼 턴 | `TranslationCoalescer`(순수): 같은 화자·3 s 이내의 조각 줄을 다음 줄에 접어 앵커 줄 1턴/타깃, 조각 줄은 `suppressTranslation`(번역 행 없음). 결과 리비전은 `revisionFor(id:source:)`로 앵커 자체 리비전에 매핑 | 테스트 4건 |
| T2 | 백로그가 깊어도 모든 타깃을 계속 큐잉 | `TranslationShedPolicy`: 커밋 백로그 ≥6이면 우선 언어만, 나머지는 `onDrop`→정지 후 백필 | 테스트 1건 |
| T3 | "N줄 대기"가 턴 수(2타깃=2배) | `onLineDepthChange`(중복 없는 줄 수)로 상태 표시 | — |
| P1 | 프리뷰 허용 = `segmentsInFlight==0 && !dnaBusy` → 번역 큐가 비지 않으면 영영 기아 | `dnaBusy`여도 2 s(백로그 >6이면 4 s)마다 1회 허용, 1 s 타이머로 재평가; 정지 시 stability.log에 `stt n/p50/p95/max previews/min` 기록 | 라이브 계측 |
| L1 | 보류 단어 재디코드가 새 UUID → 첫 단어 id로 키잉된 줄 id·P15 원장·번역이 경계마다 유실 | `Word(id:)` 승계(WordMerger) | 테스트 1건 |

게이트: swift test 579/579, 앱 빌드. **머지 #274 (2026-09-03).**

라이브 재계측(0.3.5, 같은 영상 EN→中·한 2타깃, 37분 완주 vs 0.3.2는 13:28 정지, `bench/wer_runs/prof/samples4.csv` vs `samples2.csv`):

| 지표 | 0.3.2 (21분, 정지 포함) | 0.3.5 (37분) |
|---|---:|---:|
| 정지 | 13:28 메인 스레드 포화 | 없음 — 끝까지 팔로우·회색 실시간 텍스트·번역 도착 |
| 번역 대기 | 5→20줄(70 s) | 꼬리 1~3줄 이내 착지, 백로그 ≥6이면 한국어 셰딩(배너 상시) → 정지 후 채움 |
| 앱 CPU 평균/최대 | 43.7 % / 96.5 % | 49.5 % / 97.2 % — **10분 구간별 37→45→57→56 %: 줄 수에 비례 성장(→ P2)** |
| 앱 RSS 평균 | 205 MB | 191 MB (156→208 MB) |
| 번역 엔진 CPU / RSS | 22.0 % / 5428 MB | 19.1 % / 3732 MB |
| GPU device 평균 | 91.7 % | 82.5 % |
| 메인 스레드(3 s `sample`) | 1896/1896 레이아웃 | 8분: 유휴 46 %, sizeThatFits 86 · 33분: 유휴 16 %, AG update 790, CA commit 477, `engine(didEmit:)` 534(=`translateStableLines` 376 + `ingest` 151), `ContentView.body` 218(=`flaggedWords` 187, `confThreshold` 176) |

STT p95·프리뷰/분(`@final stt`)은 정지 시 stability.log에 기록 — 사용자 세션 정지 후 확인. 새로 드러난 회귀 = **P2**:
T5 예시 번역 에코(5회 관찰), 회색 실시간 텍스트 중복(6회), 줄 수 비례 메인 스레드 비용.


## P0 — 라이브 세션 정지(메인 스레드 레이아웃 포화) + 팔로우/새 전사 버그 (2026-09-03)

0.3.2 라이브 프로파일링(유튜브 팟캐스트, EN→中·한 2타깃, 13분 28초 정지): `sample` 1896/1896 샘플이 SwiftUI 레이아웃
(`TranscriptView.body` → `sizeThatFits` 재귀 1396프레임), Whisper는 stdin 대기·번역 엔진 유휴 — 메인 액터가 굳으면 세그먼트 공급·
번역 디스패치·타이머가 함께 멈춘다. 두 번째 세션(가벼운 머신)에서도 앱 CPU 평균 44%(최대 96%), 뷰가 00:05:41에 20분+ 고정,
"새 전사" 1→15→7→3→43 요동 재현. 보고서 = `bench/wer_runs/prof/madi-live-profiling.html`(아티팩트), 데이터 `bench/wer_runs/prof/`.

| # | 원인(코드) | 수정 |
|---|---|---|
| A1 | `ContentView`·`CaptionOverlay`가 본문에서 `transcript.lines`를 직접 읽어(`lockedLineID`, `flaggedWords` 2회, isEmpty 5곳) 30 fps 스냅샷 코얼레싱이 무력화 — 토큰마다 전체 본문 재평가 | `displayLines`/`displayLastLineID`(publish 시 갱신)만 관찰 |
| A2 | 상세 행 `row(line)`이 매 스냅샷마다 전 줄의 `attributed(line)` 재생성 + CoreText 재배치(줄 수에 선형) | `RowHost(key:).equatable()` — 행이 그리는 모든 값(줄·이름·편집/리뷰/찾기/스트리밍/재생/폰트/인터림 꼬리)을 키로 비교, 변하지 않은 행은 본문 생략 |
| A3 | `translateStableLines()`가 단어 이벤트마다 전 줄 정규식 3패스 | 줄별 텍스트 해시 게이트(`stablePassHash`) — 바뀐 줄만 |
| A4 | 내용 모드 화자 블록 = 구간 전체 하나의 Text(+번역 2개), 24 ms 타자 효과가 문단 전체 재배치 | 블록을 8줄/700자로 분할 |
| B1 | 팔로우 해제가 기하(`bottomGap > max(240, h/2)`)로도 발생 — 뷰포트 위 성장(늦은 번역·SPKFIX)에 사용자 입력 없이 풀림 | 순수 `TranscriptFollowModel`(Foundation, 테스트 6건): 해제는 휠 위/프로그램 점프뿐, 기하는 핀만 |
| B2 | "새 전사 N" = `lines.count` 델타, 감소(병합) 시 0 리셋 | 마지막으로 본 줄 id/시각 기준 재계산 |
| B3 | `liveTail`이 20pt 패딩 안쪽 → 착지 갭 항상 20 > 8, 재무장 불가; 앵커·핀이 `activityText`(무음 15 s면 nil)에 묶임 | liveTail에 바닥 인셋 포함·패딩을 top/horizontal로, `isLive` 파라미터로 앵커·핀 |
| B4 | 찾기바가 새 줄마다 재스크롤·리뷰 점프가 팔로우 해제와 구분 안 됨 | 현재 매치 변경 시에만 스크롤, 점프는 명시적 `programmaticJump()` + 0.4 s 핀 억제 |

게이트: swift test 573/573(모델 테스트 6건 포함), 앱 빌드 통과. **라이브 재계측(같은 영상, 샘플러+스택 샘플)은 0.3.4 로컬 빌드로 진행.**

## S3 — Whisper 이전 세그먼트 텍스트 조건화(`<|startofprev|>`): 반증 (2026-09-02)

whisper.cpp `condition_on_previous_text`의 동형. 엔진에 SEG 잡 단위 ` %%PROMPT <text>` 접미(정적 글로서리 뒤에
이전 텍스트의 꼬리, 합계 224토큰 캡, 구조(ts_mode) 패스 제외, `[caps] seg-prompt`)를 구현하고, 연속 발화를
자연 순서로 상주 엔진에 흘리며 **이전 발화 텍스트를 문맥으로 준 조건**을 무문맥과 동일 발화 id로 대조했다.

| 데이터(연속 순서, 동일 id 대조) | n | 무문맥 | 이전 텍스트 문맥 | 판정 |
|---|---:|---:|---:|---|
| LibriSpeech test-other, 이전 **가설** | 1500 | WER 4.88% | 4.79% (400자 꼬리) / 4.88% (120자) | **노이즈** — 쌍 부트스트랩 Δ −0.09pt, 95% CI [−0.63, +0.31], 발화별 개선 71 / 악화 103. −0.09의 전부가 35초 발화 1건(기준이 중간 60단어 누락, 문맥 조건에서 우연히 온전) — 그 챕터 제외 시 4.74 → **4.87(+0.13)** |
| LibriSpeech test-clean, 이전 가설 | 800 | 1.60% | 1.76% | 악화 — 챕터 2830-3980 +26.5pt(14단어 발화가 5단어로 절단) |
| FLEURS-ko, **무관한** 이전 문장(해악 테스트) | 382 | CER 5.56% | 5.62% | 중립(변경 49발화, 개선 19 / 악화 25) |
| ko1/ko2/ko4 TTS 턴(가청 12턴) | 12 | 동일 | 동일 | 변경 0행 — 헤드룸 없음 |
| test-other, **oracle 참조** 텍스트(대문자·무구두점 원문) | 800 | 5.61% | 7.45% | 악화 — 참조의 스타일을 **복사**(ALLCAPS 출력 1 → 573/800, 대문자 모드에서 청취 자체가 붕괴 "HE BEJAS SETTEMEEN") = 교란 |
| 같은 oracle, 자연 대소문자로 정규화 | 800 | 5.61% | 5.69% (+0.08pt, 개선 50 / 악화 69, ALLCAPS 출력 0/800) | 내용 문맥만의 상한 |
| 디코드 ms 중앙값(발화당) | | 43 | 54 | **+25%** — 문맥 ~100토큰의 S1 배치 프리필 비용 |

실패 유형(악화 발화 검사): ① **프롬프트 에코** — 이전 문장을 그대로 다시 출력(길이 2×, tokenCollapse 미감지),
② 이전 가설의 **오철자 전파**("jack up", "biel/dicky"), ③ 잉여 구두점 토큰("well ."), ④ 절단. 개선 유형은 고유명사
일관성("jackal", "moses")뿐인데 이는 S4 글로서리 바이어싱이 이미 더 정밀하게(관련성 게이트 있음) 덮는 영역이다.
라이브 파이프라인(10초 창 + 3초 겹침)에서는 겹침 구간 텍스트가 프롬프트에 들어가 생략/중복 위험이 추가되므로
오프라인 중립조차 라이브 이득으로 이어지지 않는다. **코드 미출하** — 엔진 패치 `bench/wer_runs/s3_seg_prompt.patch`,
러너 `s3_run.py`(MODE=none|hyp|ref, TAIL_CHARS, REF_NATURAL) · 채점 `s3_score.py` · 결과 `bench/wer_runs/s3/*.jsonl`
보관(gitignore). 재개 조건 = 신뢰도/n-best 기반 문맥 필터 + 겹침 제외 배선을 갖춘 라이브 A/B — 현재 이 모델·greedy
디코더에선 우선순위 없음. 데이터: LibriSpeech test-other/clean(openslr 12, `~/antigravity/benchmark_samples/librispeech`).

## T4 — DNA3 프리필 고정비 (엔진측, sovereignLLM PERF_MATRIX §16·§17) (2026-09-02)

캡션 턴의 프리필(B≈14~21)이 두 가지 이유로 고정비에 갇혀 있었다. ① packed Q6_K(V/W2, 바이트 25%)가 B≤16에서
토큰마다 가중치를 재독하는 GEMV 경로(`bid.y`=토큰) → 텐서 경로 + 32열 packed 타일로 1회 독해; GDN conv 커널
2개가 단일 TG 루프 → 채널 축 재그리드. ② Q4 텐서 mul_mm이 rows/64 TG(40~144개)로 점유율-bound → 32행 A타일
기본값(TG 2×, 산술 불변). 실측(순서교대 쌍, 냉각): **B14 프리필 4B 88→66.4ms(−25%), 2B 39.4→28.6(−27%)**,
B21 −18%/−20%, B33 −5%, 디코드 불변, 117턴 텍스트 동일. madi 캡션 TTFT의 프리필 성분이 그만큼 준다(라이브
`ttft=` 실측은 다음 세션). 롤백 env `SOV_Q6_GEMV_SMALLB=1`·`SOV_Q6_NO_N32=1`·`SOV_MM_R64=1`.
**3단계(split-K/K-step 256)는 측정으로 닫힘**(PERF_MATRIX §18): KS=4 B14 −2.5%(그 외 0), K256 0, 고전 simdgroup
mul_mm +11%. 프로브 분해 — 빈 커널 34.8 ≈ dequant만 34.2 < 텐서만 66.3 < 전체 76ms → Metal-4 `matmul2d`의
타일-스텝 수가 바닥(TG·배리어·분할로 안 움직임).
**4단계(PERF_MATRIX §19, sovereignLLM PR#253 머지)**: int8 정식화는 **측정으로 닫힘** — 실제 타일 형상의 타이밍 프로브
5종(냉각 3라운드, 4B B14): int8×int8→int32 K128 **+8%**, K32 MMQ형(Q4_K 32-서브블록 스케일이 강제) **+45%**,
half×int8 −2%, half×half K32 +4.5%, half×uint4b 디바이스 직독 **+34%**. M4 Pro의 협력 matmul2d는 피연산자 타입에
둔감(int8 텐서 유닛 없음)하고 호출당 고정비가 아니라 **타일 MAC 처리량에 묶임** → B=14에서 32열 타일은 매 스텝 56%가
패딩이었다. **16열 타일**(`mul_mm_q4_K_tensor_n16_r32` + packed Q6_K `_p_n16`, 같은 dequant·누산, B≤16 라우팅;
24열은 B=21..24에서 0이라 미출하) 실측: **B14 프리필 4B 66.3→48.6ms(−27%), 2B 28.6→21.5(−25%)**, B21/33 불변,
117턴 두 모델 바이트 동일. T4 누적(캡션 대역): **4B 88→48.6ms, 2B 39.4→21.5 (−45%)**. 롤백 `SOV_MM_N32MIN=1`,
Q6 격리 `SOV_Q6_NO_N16=1`. 프로브 패치·스크립트·로그 = `engine/metal/bench/wer_runs/t4int8/`(gitignore).

## S5 — Whisper 디코드 점유율 레버: 반증 (2026-09-02)

self-attn seqlen 분할(CA-SPLIT 레시피, `SA_SPLIT` 옵트인)을 ko1 STREAM 106토큰 디코드로 순서교대 6쌍 실측:
split 평균 182.2ms vs legacy 183.2ms = **−0.5%, 노이즈 안**(텍스트 동일). self-attn은 층당 ~30µs라 상한이
작고, GEMV 점유율은 D1(int4 바이트 절반 = 0%)·CA-SPLIT 이후 같은 물리가 두 번 반증됨. 코드는 출하하지
않고 패치만 보관. 잔여 디코드 시간은 토큰당 ~56개 의존 디스패치 체인의 고정 지연 — 융합 여지는 FUSE-2
실측률(런치당 ~4µs)로 ≤2.5%. 정본 engine/metal/PERF_LOG.md S5-SELFATTN-SPLIT.

## S4·S6 — 측정/구조로 닫은 항목 (2026-09-02)

- **S4 빔 서치 오프라인 프로브: 기각.** whisper.cpp(Metal) + 참조 `ggml-large-v3-turbo`로 FLEURS-ko 382:
  greedy **5.44%** vs beam 5 **5.43%** (Δ0.01pt, 게이트 0.2pt). 디코딩은 청취력 레버가 아님 —
  정본 `docs/ENGINE_EVAL.md` S4-빔 후속. 엔진 빔 경로는 만들지 않는다.
- **S6 프리뷰→커밋 인코더 재사용: 구조적 폐기.** `Segmenter.previewWindow()` = overlapTail+pending,
  커밋 `cut()` = overlapTail+body 로 구성은 같지만, 커밋은 마지막 프리뷰(≤1s 전) 이후 오디오를 더
  포함하고 조기 flush 컷도 있어 동일 오디오 히트가 사실상 0 — enc_out 재사용 조건이 성립하지 않는다.
- **T2 멀티시퀀스 배치 디코드: 커널 상한 실측 + 스코핑, 미착수** (sovereignLLM PERF_MATRIX §20). 텍스트 동일
  프로브(B==1 Q4_K GEMV 자리에 N열 커널, 0열=실제 벡터)로 4B 디코드 ms/tok: 기준 `q4_k_mv2` 14.1 — mv2 골격+열별
  y 캐시는 2열 **+58%**, 3~4열 스필(+200/+460%); llama.cpp `mul_mv_ext` 이식은 1열 16.2(+12% 구조비), 2열 17.8
  (**추가 열당 +1.5ms**), 4열 24.3(4열이 1스텝의 1.7×). llama.cpp도 K-quant는 ne11≥4에서만 ext 라우팅. 전체 스텝
  추정: Q4 ext만 B=2 1.3×, Q6 쌍둥이 추가 ~1.5×, 소형 커널 grid.z 배치까지 ~1.9× — 단 시퀀스별 상태 ×B·배치 턴
  프로토콜·앱 다중 타깃 묶기가 필요(수일), 소비자는 다중 타깃 세션뿐. 프로브 패치 `bench/wer_runs/t4int8/t2_mvx_probe.patch`.

## T5 — 문맥 이월 예시: 직전 커밋 (원문 ⇒ 번역) 쌍이 프롬프트의 예시 자리에 (2026-09-02)

헤드룸 리뷰 5순위(품질 축). 턴은 완전 무상태 단일 문장이라 한국어 주어 생략을 복원할 수 없었다. 프롬프트의
예시 슬롯("Example — Hello => Hello . Now:")을 **그 타깃 언어의 직전 커밋 (원문 ⇒ 번역) 쌍**으로 대체
(`TranslatePrompt.body`; 캐시 프리픽스는 지시문 머리만, 예시+원문은 턴 본문). 쓸 만한 쌍이 없으면 앵커로
폴백 = 종전 프롬프트와 바이트 동일. 인터림 결과는 문맥이 되지 않는다(커밋 라인만), 200자 초과·자기 문장은 제외.

| 프로브 (주어 생략 2문장 40항목, EN 참조는 **AI 작성** — 게이트가 아니라 프로브) | A 앵커(종전) | B 문맥 이월 | Δ |
|---|---:|---:|---:|
| 4B 대명사 적중 | 15/40 | **22/40** | +7 |
| 4B chrF++ (참조 대비) | 54.7 | **57.8** | +3.1 |
| 4B 턴 엔진 ms | 236.6 | 274.3 | **+38 ms (+16%)** |
| 2B 대명사 적중 | 13/40 | **17/40** | +4 |
| 2B chrF++ | 47.8 | **53.1** | +5.2 |
| 2B 턴 엔진 ms / 생성 토큰 | 303.6 / 37.1 | **147.7 / 13.8** | −51% |
| 실패(에코·이종문자) | 0 / 1 | 0 / 0 | |

2B의 시간 역전은 "Hello => Hello" **항등 예시**가 2B에서 `</think>` 누수·장황을 유발(생성 37토큰)하던 것이
실제 번역 예시로 사라진 효과 — 앱은 누수를 strip 하지만 생성 시간은 이미 쓴 뒤였다. 4B는 +38ms 프리필
(고정비 구간)과 품질을 맞바꾼다 — P10 원칙대로 TTFT +38ms를 같이 기록. 프로브·하네스 = `bench/wer_runs/
t5_probe.json`·`t5_harness.py`(gitignore, 디스크 보존). 라이브 검증 = 다음 세션(회의체 KO→EN 대명사 체감).
게이트: swift test 566/566(`TranslatePromptTests` 3건: 앵커 폴백=종전 레이아웃, 예시 삽입, 부적격 쌍 제외).

**0.3.2 화면 검증 + 근본 수정 (2026-09-02 밤)**: 정제 strip 후 접두 누출은 사라졌으나 中 문장 안에 "现在"가 끼어드는 형태
("我们现在经常卸载工具" ← "We unship tools all the time")가 2/2 연속 관찰 — 혼입된 번역이 다음 예시 쌍이 되어 증폭되는 같은 고리.
오프라인: 앵커 0/8, 실제 쌍+"Now:" 1/8, 실제 쌍+" . " 0/8. **턴 본문에서 "Now:" 제거** ("<src> => <tgt> . <text> =>") — 40항목
프로브 재검증: 4B 대명사 21/40·chrF++ 58.9(표식 있을 때 22/40·57.8 = 동급 이상), 2B chrF++ 51.0(앵커 47.8) ; 4B 누출 EN→KO/ZH/JA·
KO→EN 0/6. strip은 2차 방어로 유지. 라이브 메모리(4B 세션): translate-engine-4b phys 3.4GB(RSS 4.5), transcribe 1.7GB, 앱 0.2GB =
약 5.3GB; 앱 프로세스 CPU 40~65%(WindowServer 100%+인 바쁜 머신에서 측정, 기준선 미비교). 2타깃·빠른 화자에서 대기 줄 5→20(70초)
— 짧은 조각 줄("And", "tools.")마다 턴 2개가 나가는 구조가 원인이라 조각 병합/2B 캡션 레인/T2 배치가 후보.

**0.3.1 라이브 관찰 (2026-09-02 저녁, EN→中/한 2타깃 세션)**: 번역 앞에 "现在:"·"이제:"가 붙는 구간이 있었다 — 턴 본문의
"Now:" 표식이 타깃 언어로 번역돼 새고, 한 번 새면 그 커밋 줄이 다음 턴의 예시 쌍이 되어 세션 내내 자기강화된다(청정한 쌍이
들어오면 사라짐 = 간헐). 오프라인 재현: 앵커 예시 0/6, 실제 쌍 EN→KO 4B 5/6, 2B EN→ZH 3/6. 구분자 교체(" . " / " ; " /
"Text:" / 개행)는 4B에선 누출 0이지만 2B가 예시와 본문을 합쳐 번역하거나 `</think>`를 누출해 프롬프트 재설계는 40항목
프로브 재검증이 필요 → **핫픽스는 정제 단계**: `TranslationOutputPolicy.clean`에 표식·구분자·화살표 에코 strip(스트리밍·최종
공통 경로라 저장되는 예시 쌍도 청정), 테스트 8건. 같은 세션에서 "말이 빨라 번역이 밀렸어요 · 6줄 대기"가 관찰됨 — 2타깃이면
줄당 턴 2개라 커밋 레인이 밀리는 T2(멀티시퀀스 배치)의 실소비자 증거.

## S2 — 프리뷰 forced-prefix: 합의된 원문 접두를 교사강제, 꼬리만 디코드 (2026-09-02)

헤드룸 리뷰 4순위. 라이브 PREVIEW 잡은 매초 열린 창 전체를 재인코드·재디코드했다. P8 `InterimSourceGate`
(LocalAgreement-2)가 이미 굳힌 원문 접두를 `PREVIEW <wav> %%FP <접두>`로 넘기면 엔진이 BPE 인코드 후
S1 배치 프리필로 KV-fill 하고(seed로 취급, 예측 안 함) 그 뒤부터 디코드한다. 플레인 패스만(ts_mode 재디코드·
커밋 SEG는 자유 디코드 그대로), 강제 토큰은 `d_conf`=0으로 avg_logprob에서 제외, 캡 `[caps] preview-fp`로
앱 게이트(`EngineProcess.supportsPreviewForcedPrefix`). 앱은 `interimSourceGate.committedText`(합의분만,
provisional 제외)를 프리뷰마다 실어 보낸다.

부수 수정 2건: ① `metal_backend.m` MPS 형상 캐시가 64엔트리·무교체라 가득 차면 **모든** MPS matmul이
비캐시 경로(호출마다 디스크립터+커널 객체 생성, 수십 ms)로 떨어지던 것을 512 링+재활용으로 교정 —
프리뷰 프리필은 행 수가 매초 달라져 첫 실측에서 jfk 프리뷰 decode가 +140% 튀었던 원인. ② 프리필 행 수를
16 단위로 버킷(`Mp`) → 형상 종류 ≤20. 패딩 행은 유효 토큰 id를 읽고 다음 디코드가 덮어쓰는 K/V 행만 쓴다.

| 프리뷰 레인 하네스 (1s 성장 프리픽스, STREAM·AUDIO_CTX=auto, 앱 게이트 의미론) | free | forced | Δ |
|---|---:|---:|---:|
| 프리뷰 decode 합계 ms (ko1 22회 / ko2 15 / ko4 14 / jfk 10) | 2619 / 1563 / 1220 / 268 | **1630 / 1179 / 454 / 239** | **−38 / −25 / −63 / −11%** |
| 4클립 합계 | 5670 | **3502** | **−38%** |
| 원시 프리뷰 텍스트 NE (소거/최종자) | 4.26 / 4.71 / 2.26 / 0.40 | **0.32 / 0.24 / 0.17 / 0.15** | **−90%** |
| 게이트 표시 NE | 0.00 / 0.00 / 0.05 / 0.08 | 동일 | 0 |
| 창 종료 합의 커버리지 | 0.92 / 0.89 / 0.99 / 0.76 | 0.92 / 0.90 / 0.91 / 0.76 | ≈ |
| 커밋 SEG 텍스트 | — | **4/4 동일** | 불변 |

인코더(ko1 6.9s/22회)가 프리뷰 비용의 지배항이라 레인 총량은 −10% 급이지만, **원시 프리뷰가 구조적으로
append-only**가 된다(회색 인터림 텍스트의 되쓰기 −90%) — P8이 앱단에서 걸러내던 "프리뷰 재디코드가 의미를
바꾸는" 상류 원인 자체를 제거. 게이트: S1 회귀(jfk 골든·verify 편차·72 vs 230ms) 동일, `preview_lane_gate`
PROMPT 유/무 WIN(conf drift 0.0), swift test 전체. **라이브 마이크 실측(캡션 NE·프리뷰 체감)은 다음 세션.**

## T1 — 인터림 forced-prefix: 화면에 굳은 번역 접두를 재생성하지 않는다 (2026-09-02)

헤드룸 리뷰 3순위. P9 표시 합의는 캡션을 append-only로 굳히지만, 다음 인터림 턴은 그 굳은 접두를
토큰당 15ms로 **다시 생성**한 뒤에야 새 꼬리에 도달했다. 엔진(sovereignLLM `%%FP`, caps `fp`)이
접두를 이미 생성된 어시스턴트 토큰으로 **프리필**하고 꼬리만 디코드하도록 하고, 앱은 인터림 턴에
열린 문장의 **합의된(provisional 제외) 표시 접두**를 `forced`로 넘긴다(`TranslateEngine.translate(
…, forced:)` → `%%TRN <slot> <body> %%FP <접두>`; 엔진이 `fp`를 선언한 경우에만). 커밋 턴·재시도는
자유 디코드 그대로 = 최종 번역 불변. 텔레메트리 `forced=<턴>/<자>`가 summary 라인에 추가.

| 엔진 하네스 (FLEURS-ko 20문장, 3어절 성장, 117턴/모드, 앱 프롬프트·게이트 의미론 동일) | free | forced | Δ |
|---|---:|---:|---:|
| 4B 턴당 엔진 ms | 451.7 | **349.3** | **−23%** |
| 4B 생성 토큰 | 2,504 | 1,766 | −29% |
| 4B 창 종료 합의 커버리지 | 0.59 | 0.62 | +0.03 |
| 2B 턴당 엔진 ms | 225.8 | **192.3** | **−15%** |
| 2B 생성 토큰 | 2,569 | 1,909 | −26% |
| 2B 커버리지 | 0.53 | 0.53 | 0 |

에코/이종문자 실패 0/117(양 모델·양 모드). 최종 턴 텍스트 동일 7/20(4B)·5/20(2B), chrF++ forced-vs-free
84.3/79.5 = 꼬리가 다르게 표현될 뿐 붕괴 없음. 이득이 바이트 추정(−50%)보다 작은 이유: 합의 접두는
연속 2턴이 동의한 만큼만 자라(턴당 ~5-6어절) 평균 28자 강제 vs 21토큰 생성. 긴 창·느린 화자에서 더 큼.
**라이브 실측 대기**: 다음 마이크 세션의 `ttft=`·`forced=`·caption NE·커버리지(체감) 비교.
정본 = sovereignLLM `PERF_MATRIX.md` §15. 게이트: swift test(파서 caps 이벤트·합의 접두 제외 테스트 추가).

## S1 — Whisper 디코더 시퀀스 프리필: 글로서리 PROMPT 시드 −155ms/패스 (2026-09-02)

엔진 헤드룸 리뷰(2026-09-02)의 1순위. S4 글로서리 바이어싱이 켜지면 `<|startofprev|>` +
용어 토큰(32용어 = **158토큰**)이 매 패스의 시드가 되는데, `transcribe.zig` 시드 루프는 이를
**토큰당 4층 decodeBlock 순차**로 KV-fill 하고 있었다 — 매 1s 프리뷰와 매 커밋에 반복.
ENGINE_EVAL S4 판정에는 지연 행이 없었다(채택 당시 미측정 비용).

**구현**: 시드 prefix 전체를 **한 번의 배치 인과 패스**로(`decoder.zig prefillBlock`): 투영은
M-row F16 GEMM(JIT dequant, cross-KV 빌드와 같은 패턴), self-attn은 (row, head)당 threadgroup
(`causal_attention_rows`), cross-attn은 공유 KV 행 커널(`flash_cross_attn_f16kv_rows`).
prefix ≥ 8행만 배치 경로(`DEC_PREFILL_MIN`), sot-only 시드는 종전 순차 경로 그대로 = **바이트동일**.
스크래치(~40MB)는 첫 긴 prefix에서 **지연 할당**(글로서리 없는 세션 비용 0 — RSS 실측 +2.7MB).
`DEC_PREFILL=0` 롤백, `DEC_PREFILL_VERIFY=1` = 양 경로 실행 + 층별 K/V 편차 리포트.

| 실측 (M4 Pro, Q8, 32용어=158tok) | 순차(종전) | 프리필 | Δ |
|---|---:|---:|---:|
| jfk 파일 decode(시드 포함), 3쌍 순서교대 | 226/225/226 ms | **71/70/70 ms** | **−155 ms/패스** |
| jfk 시드 단독 (VERIFY 계측) | 190.6 ms | **34.6 ms** | 5.5× |
| ko1 STREAM(AUDIO_CTX=auto) PREVIEW / SEG decode, 2쌍 | 350 / 345 ms | **196 / 191 ms** | −154 ms |
| FLEURS-ko 382 엔진 패스 wall | 409 s | **350 s** | −155 ms/utt |

**게이트**: FLEURS-ko 382 CER **5.91% == 5.91%**, 가설 **382/382 바이트 동일**(같은 32용어 프롬프트,
순차 vs 프리필); jfk PROMPT 텍스트 6/6 동일; PROMPT 없는 jfk 골든 바이트동일; `preview_lane_gate`
PROMPT 유/무 모두 WIN(27 이벤트 state-equivalent, conf drift ≤0.0005). K/V 편차는 F16 GEMM 급
(층3 K max|Δ| 5e-2 / max|K| 5.9, 평균 1e-3) — BATCHDEC 선례와 같은 텍스트등가 클래스.
(참고: 무관 글로서리 자체는 CER 5.63→5.91로 +0.28pt — S4의 "관련성이 배포 조건" 재확인, 본 변경과 무관.)

**해금**: 같은 프리필 경로가 S2(프리뷰 forced-prefix)·S3(직전 세그먼트 조건화)의 선행 조건.
quark: `fn__prefillBlock`·`fn__prefillEmbed`·`metal_kernel__causal_attention_rows`·
`metal_kernel__flash_cross_attn_f16kv_rows`·`metal_kernel__qkv_bias_store_rows`·`metal_kernel__emb_pe_rows_q8`
(+14 atoms), `asset bridge` 갭 0.

## 실시간 요약 — 무열화 봉투 실측 (2026-08-31)

녹음 중 우측 실시간 요약 탭(롤링 carry, 최저 브로커 레인). CLI 프로브
(sovereignLLM 4B/2B, `[perf]` 라인 파싱 — docs/LIVE_SUMMARY.md):

```
4B  per-request total 1.21-3.44s (n=6, KO/EN, carry 왕복 포함) = 자막 최악 블로킹 상한
4B  형식 4/4 PASS (불릿 4-7), carry 누적 검증 (R1 결정 유지 + R3 신규 반영)
2B  KO 폭주 79불릿/8.9s → ≥16GB 게이트 정당성 실증 (게이트 통과 기기 = 항상 4B)
언어 A/B: "전사와 같은 언어로" = EN 회의에 한글 63자 누수 → "반드시 발언과 같은
언어로만(영어면 영어로)" = 누수 0자, KO 무회귀
```

- 이 상한은 브로커 이중 방어(P11 큐잉 게이트 + aging 밸브) **이전**의 비선점
  구간 수치 — 실제 간섭 빈도는 30s 주기 × 자막 유휴 시에만이라 사실상 0.
- 다음 라이브 실측 대비: 캡션 ttft/NE에 요약 켬/끔 A/B 비교가 남은 검증.


## P14 — 표면 교정 리바인드 (2026-08-11)

라이브 실측이 특정한 패널 소거 100%=revision의 낭비 성분 제거: **구두점·공백·대소문자만 바뀐
교정**(mid-reconcile 재디코드의 정규화, 검토의 문장부호 확인 등)은 번역을 무효화하지 않고 기존
레코드를 새 리비전에 **리바인드** — stale 강등 없음, 재번역 턴 0, 패널 교체 0. suppression
verdict도 함께 리바인드(가드 판정은 내용에 귀속, 문장부호에 아님). 자소 변화(소버림→소버린)·
조사 추가는 material로 기존 경로 유지. `editLine`+`editWord` 양 경로, `cosmeticallyEqual`
순수함수, 원장에 `rebinds=` 텔레메트리. 483 테스트. 라이브 검증 = 다음 세션의 rev=/rebinds= 대비.

## P13 라이브 확정 (2026-08-11 오전, EN→日·한 이중 타깃, final 1,262자)

```
caption NE=0.36 erased=450 refined=398 rev=0 | panel NE=1.09 rev=1373(100%)
ttft=463/844ms(n=7) firstPaint=854ms lineTr=561/2077ms(n=38)
```

- **캡션 판정 성공**: 총 0.68→0.36, **core NE(refinement 제외)=0.04** (게이트 0.2의 1/5).
  소거의 88%가 문장 확정 다듬기(refined)로 분류, 무작위 재작성 rev=0 — 무소거 핸드오프 라이브 확정.
- **패널 미스터리 종결**: 소거 100%가 revision — AI 교정(mid-reconcile)·검토가 원문을 고친
  대가로, 표시 정책 결함 아님(P4 stale-keep으로 회색→제자리 교체 표출). 필요 시 P14 후보 =
  "교정 결과가 실질 상이할 때만 재번역". 이중 타깃에서도 지연 건강(ttft 463ms).

## P13 — 문장 핸드오프 무소거화 + 원인분류 계측 (2026-08-11)

라이브 NE 0.68의 재현·수정: 랩에 **지연 갭**(문장 확정 번역이 완성 다음 주기에 도착 — 이전 랩은
캐시 즉납이라 미포착)을 모델링하자 P12 출하 정책이 평균 NE 0.15로 재현됨(문장마다 진행분
blank→확정 2연타). **P13**: 완성 문장의 진행분 표시를 frozen 슬롯에 **provisional로 이관**
(핸드오프 시 composed 바이트 동일 = 소거 0) → 확정 번역이 도착하면 제자리 교체를 **refinement로
태깅**. 패널은 소스 리비전 교체를 **revision으로 태깅** (NE 0.86 원인 조사용).

| (지연 갭 모델, 4언어쌍) | 총 NE | core(=refinement 제외) |
|---|---:|---:|
| P12 출하(blank 핸드오프) | 0.15 | — |
| **P13 무소거 핸드오프** | **0.08** | **0.00** |

summary 포맷 확장: `refined=`/`rev=` — 이제 NE가 "어떤 종류의 소거"인지 스스로 말한다.
라이브 재실측으로 0.68 → (기대) refinement-지배 저NE 확인 예정.

## 정상 전원 라이브 실측 + 0.1.8 릴리즈 (2026-08-11)

정상 클럭·main 1abc087(P12 전체 포함) 마이크 세션(final 1,826자, @final/@settled/@terminate
3중 원장 첫 실전 — 파일+stdout 양쪽 완벽 포착):

```
ttft=283/611ms(n=18) firstPaint=554/2913ms(n=16) lineTr=546/1951ms(n=59)
interimTurns=50 gated=3 | caption NE=0.68 rewrites=49 | panel NE=0.86
```

- **반응 전면 달성**: ttft 2484→**283ms**(8.8×), firstPaint 1107→**554ms**, lineTr p95
  10040→**1951ms**(5×↓). 인터림 50턴이 활발히 돌아도 패널이 안 밀림 — 기아·댐·백로그 종결,
  LPM이 주범이었음을 확정.
- ⚠️ **신규 과제 P13 — 캡션 NE 0.68**(랩 0.08과 괴리): 유력 원인 = **문장 핸드오프 갭** —
  문장 완성 순간 진행분 표시를 비우고(소거 집계) 확정 번역 도착 시 재등장하는 2연타(문장마다).
  랩은 캐시 즉납이라 타이밍 갭이 0이어서 못 잡음. 사용자 체감은 "너무 적당"(블록 확정 다듬기로
  수용) — 지표가 의미동일 refinement를 과벌점하는 측면 병존. 후보: 무소거 핸드오프(진행분을
  확정 도착까지 유지) + NE의 refinement 구분 계측. panel NE 0.86도 원인 조사 대상.
- **0.1.8 stable 발행**: build 629, `madi-0.1.8-arm64.dmg` 37,995,030B,
  sha256 15ceb212…5841cb, latest.json 라이브 확인. 체감 승인("너무 적당") 기반 출하,
  P13은 다음 사이클.

## 측정 환경 경보 — 저전력 모드가 오늘 밤 실측 전체를 1/3 클럭에서 잼 (2026-08-11)

decode 마이크로벤치(번들·sovereignLLM 로컬 동일 바이너리 교차검증)가 **20~25 tok/s**로 문서치
(63~68, PERF_MATRIX §14)의 1/3 — 장문 105tok에서도 20.5로 고정비 가설 기각. 원인 규명:
**macOS 저전력 모드 ON(`pmset -g` powermode=1)** + zig 컴파일 2개 CPU 100% + load ~7.
대역폭-바운드 decode GEMV는 클럭 민감(1/3↓), 배치 프리필은 158~213 tok/s로 유지 — 관측 패턴과
정합. **⇒ 오늘 밤의 모든 지연 실측(ttft 2484ms, lineTr p95 8.6~11.5s, 백로그/shed 관찰)은
1/3 GPU 클럭 조건의 수치**다. 정상 클럭이면 4B가 밀집 콘텐츠도 따라갈 가능성이 높아 **2B 인터림
레인(Idea 2)은 정상 전원에서 재실측 전까지 보류**. 교훈은 §14와 동일 — 측정 전 전원/부하 확인은
하네스의 일부다. (P12 라이브 A/B는 캡처 사고 2건(조기 ⌘Q·Sparkle 자동업데이트의 dev 설치본
되돌림+앱 종료)으로 미결 — @final/@settled/@terminate 3중 flush + fflush + build 9999 스탬프로
전부 봉인, 다음 세션은 무조건 잡힘.)

## P12 — 문장 스코프 인터림 + 적응형 예약 (2026-08-11, (a) 배분 문제의 구조 완화)

**동기**: P10-1 예약이 캡션에 엔진 시간을 배정하자(의도) 연속 발화에서 커밋 백필이 밀림(반대급부).
인터림 턴이 매번 **합의 프리픽스 전체**를 재번역(윈도 후반 60-80tok 프리필/3s)하는 게 비용의 본체.

**하네스 선측정 (4언어쌍, 실엔진 캐시, fastFirst 양측 off로 격리)**:

| | NE | cover | 턴 | 프리필자 | 출력자 |
|---|---:|---:|---:|---:|---:|
| C 현행(전체 프리픽스) | 0.00 | 61% | 26 | 968 | 1,667 |
| **S 문장스코프** | 0.08 | **72%** | 29 | **531 (−45%)** | **914 (−45%)** |

- 완성 문장은 1회 번역 후 동결, **열린 문장만** 재번역 — P9 합의 게이트가 있어 과거 V4 기각 사유
  (경계 재작성 누출)가 소멸했는지 재검증 → NE 0.08(worst 0.18, ≤0.2 게이트 내). 문장 완성 순간
  확정 번역이 1회 다듬는 교체는 Transync류 블록 확정 패턴과 동일. jfk 단일 장문에서 cover 소폭
  역행(61→46%) 기록.
- **보너스**: 문장 확정 번역을 O3 캐시에 **문장 키**로 시딩 — 커밋 라인은 문장-단위라 그 문장이
  커밋될 때 커밋 턴이 캐시 히트로 공짜가 될 수 있음 (라이브 실측 대상).
- **적응형 예약(Idea 3)**: 예약 간격이 커밋 백로그에 비례(≤2→기본, ≤6→2×, 초과→예약 중지) —
  캡션 보장 슬롯이 shed를 유발하지 않게. `InterimTuning.adaptiveGuarantee` 순수함수.
- quark: asset find 최상위 = 자사 `ux.caption.stable-prefix-display`(68.6) — 본 작업은 그 자산의
  확장(머지 후 재verify 예정), 나머지 히트는 무관 오탐 기록.

## P11 — 캡션 압력 게이트: 백로그 댐 현상 진단+수정 (2026-08-10, 실화면 관찰 기반)

**관찰(computer-use로 실화면 직접 확인)**: 연속 발화(팟캐스트 EN→KO) 세션에서 번역 백로그
누적 — 한 라인은 "그래서"에서 멈춤, 이후 3라인 dots, 동시에 "AI가 화자·언어를 검토하는 중".
lineTr p95 8.6s 꼬리와 사용자 percept "몰렸다가 터짐"의 실체.

**진단(quark 토폴로지 + 코드)**: DNA 프로세스는 단일·직렬이고 `fn__pump`는 **인플라이트 턴을
선점 못 한다**. 우선순위(committed 100 > interim 80 > rail 30 > post 10)는 *대기열*만 보호 —
캡션 턴 사이 한 런루프 틈에 rail(18s 주기)/reconcile(45s 주기)의 **긴 턴**(긴 프롬프트 프리필 +
최대 SOV_NSTEPS=512 출력, 예산 최대 180s)이 pump를 타면 엔진을 수 초~십수 초 점유 → 캡션이
댐에 막혔다가 한꺼번에 터짐. reconcile은 추가로 one-shot whisper 프로세스를 병렬 스폰해
Metal GPU 경합까지 유발.

**수정**: ① 브로커 `captionPressure` — TranslateEngine이 자기 큐 깊이를 매 변화마다 보고,
`eligibleIndex`(순수함수, 유닛테스트 4건)가 **압력>0이면 sub-caption 턴을 부적격 처리**
(기아 밸브: 90s 대기 시 통과). 압력 0 복귀 시 pump 재개. ② `tickLiveRail`: 백로그 있으면
큐잉 자체를 skip(다음 18s 재시도). ③ mid-reconcile: 백로그·인플라이트 세그먼트 있으면 skip
(다음 45s 재시도). rail/reconcile은 주기적 배경 분석이라 지연 비용이 0에 가깝고, 캡션
적시성이 항상 우선. 472 테스트.

## P10 — 반응속도×구조 배치 (2026-08-10, TTFT 1급 축 승격)

| # | idea | result | metric |
|---|------|--------|--------|
| P10-0 | **TTFT 계측** — turnTTFT(턴 요청→첫 표시)·captionFirstPaint(첫 가설→첫 페인트)·lineFirstTranslation(라인 큐→첫 번역), median/p95, 주입 가능 클록 | ✅ commit | summary 라인에 `ttft=/firstPaint=/lineTr=` 추가 — **이후 모든 배치는 NE+TTFT 동시 기록** |
| P10-1 | **인터림 기아 해소** — TranslationTurn.reserved: 3s당 1턴이 committed 블록 우회+선실행+eviction 면제 (우선언어만) | ✅ commit | 실측 원인 = 이중 차단(admission 거부 AND committed 선pop). 일반 인터림 배압·리비전 코얼레싱 불변 (큐 유닛테스트 5건) |
| P10-2 | **첫 페인트 fast-path** — 윈도 첫 가설/첫 MT 결과를 **provisional로 즉시 표출**, 첫 합의 프리픽스가 도착하면 영구 인계 | ✅ commit | 첫 페인트 **3.00→1.00 프리뷰 주기**, 캡션 NE 0.00→**0.09 평균/0.20 worst** (승인 게이트 ≤0.2 안). ❌ **반증: 첫 가설을 committed로 직접 커밋하면 커버리지 60%→4% 붕괴**(합의 기준선 오염 — NE 0.00인데 캡션이 얼어붙는 지표 승리·제품 패배) → provisional 재설계로 해소, 커버리지 불변 |
| P10-3 | **split-once 문장분리** — 문장부호 break를 단어에 귀속·영구 존중, 행 구조 단조 증가 | ✅ commit | 재디코드가 문장부호를 옮겨도 un-split/re-split 없음 → 분할 진동이 유발하던 번역 무효화 연쇄 제거 (동결 head의 번역 생존 테스트) |
| P10-4 | **SPKFIX 배치 코얼레싱** — recluster 버스트 N개 라벨 → 시각 재계산 1회(~50ms), 라벨 데이터는 동기 유지 | ✅ commit | "라벨 한꺼번에 뒤집힘" N회→1회. 스코프 컷 기록: 태그 페이드(무애니메이션 불변식, 시각검증 필요)·커밋 배치 내용 분산(TTFT 악화라 이중축 원칙상 기각) |

**실기 라이브 실측 (2026-08-10 마이크 세션, final 1,064자)**:

```
caption NE=0.00 updates=4 rewrites=0 | panel NE=0.05 erased=48 rewrites=4
interimTurns=2 gated=2 | ttft=2484ms(n=2) firstPaint=1093ms(n=1) lineTr=1438/8573ms(n=26)
```

- **firstPaint 1.09s** — 발화 시작→캡션 첫 등장, 목표(≤2s) 달성 (P9 세션은 사실상 무한대 —
  세션당 캡션 갱신 1회였음). 캡션 생동감: 0.2회/1000자 → **3.8회/1000자 (~19×)**, 여전히 무소거.
- **panel NE 0.29 → 0.05 (−83%), rewrites 64 → 4** — split-once(P10-3)+stale-keep 효과.
  양 표면 모두 0.2 밴드 안 + 양 축(NE·TTFT) 동시 기록 체계 가동.
- 잔여 레버: turnTTFT 2.48s(4B 턴시간+인플라이트 대기 — 선점 불가), lineTr p95 8.6s(백로그 꼬리).
  후보 = 인터림 소스 단축(합의 접두 뒷부분만), 2B 프로필, 커밋 백로그 상한.

## P9 실기 라이브 실측 + 다음 지배 축 발견 (2026-08-10, 실기기 마이크 세션)

merge된 main 빌드, 실제 마이크 세션(final 5,176자)의 `[translate-stability]` 로그:

```
caption NE=0.00 erased=0 updates=1 rewrites=0 | panel NE=0.29 erased=1499
updates=1395 rewrites=64 | interimTurns=4 gated=0 holds=5 srcHeld=8211
```

- ✅ **캡션 NE 0.00 실기 확인** — 랩 4언어쌍 결과가 라이브에서 재현.
- ⚠️ **신규 발견 1 — 인터림 기아**: 세션 전체에서 인터림 턴 4회·캡션 갱신 1회(gated=0 =
  게이트 도달 전에 스케줄 자체가 막힘). 커밋 레인이 엔진을 점유하는 동안 interim admission이
  차단되는 기존 우선순위 설계 + 단일 인플라이트 결합 → 캡션이 사실상 문장확정 모드로 동작.
  "2~3s 뒤따름" 설계가 부하 시 윈도 단위 지연으로 확대. (P10-1 후보: 커밋 backlog 중에도
  N초당 인터림 1턴 보장.)
- ⚠️ **신규 발견 2 — 지배 축 이동**: **패널 NE 0.29 (>0.2 밴드)**, rewrites 64회/erased
  1,499자. 사용자 percept와 일치: "행이 한꺼번에 문장분리+화자분리되는 장면이 더 헷갈림",
  "몰렸다 터짐". 텍스트 erasure 밖의 **행 구조 churn**(재그룹 분할, SPKFIX 라이브 relabel,
  분할 시 번역 슬롯 리셋)과 **버스트 cadence**(윈도 커밋 배치)가 다음 대상. (P10-2/3 후보:
  split-once 문장분리·분할 시 번역 승계·SPKFIX 시각 완충·커밋 배치 렌더 분산.)
- 하네스 확장: 라이브 이벤트 스트림 상시 캡처 루프 확보(세션 종료 시 삭제되는
  EVENTS_FILE을 스냅샷) — 다음 세션은 구조 churn까지 전량 리플레이 가능.

## P9 — 조합 Win: 표시측 LocalAgreement로 NE 0.00 도달 (2026-08-10, 후보 스윕)

코퍼스 확장(측정 한계 해소): **4클립 × 4언어쌍** — ko1/ko2/ko4→EN + jfk→KO, 전부 실오디오 →
실 transcribe 프리뷰 레인 → 실 DNA3-4B(101턴 번역 캐시) → 출하 타입 리플레이.

**1차 스윕 (소스측 후보)**: | V2 LA-2(현행) meanNE=0.35 | V3 LA-3 0.29 | V4 LA-2+문장동결 0.43
| V5 LA-3+문장동결 0.29 | — ❌ **문장동결 단독 반증**(V4가 V2보다 악화: tail-문맥 번역과 조기
동결 번역이 어긋나는 경계 재작성). LA-3은 소폭 개선이나 0.2 미달. 잔여 지배항 = 소스가
append-only여도 **MT가 스스로 어휘를 고쳐 쓰는 것**("Today is cloud."→"Today, we are talking…").

**2차 스윕 (표시측 후보)**: MT 출력에 LocalAgreement-2(연속 2회 결과가 합의한 단어만 표시 커밋,
표면형 잠금) → **전 조합 NE 0.00, rewrites 0** (구조적 append-only). 차이는 커버리지뿐:
소스 LA-2가 LA-3보다 우위(61% vs 51%) → **소스 게이트는 기존 P8 그대로 유지**가 정답.

**채택 = W1 (P8 LA-2 소스 + P2 게이트 + P9 표시 합의)**, `InterimDisplayAgreement`
(InterimSourceGate 시맨틱을 언어별 게이트로 재사용):

| pair | states | rewrites | erased | **NE** | cover | turns |
|---|---:|---:|---:|---:|---:|---:|
| ko1→EN | 4 | 0 | 0 | **0.00** | 60% | 6 |
| ko2→EN | 3 | 0 | 0 | **0.00** | 71% | 7 |
| ko4→EN | 5 | 0 | 0 | **0.00** | 54% | 7 |
| jfk→KO | 3 | 0 | 0 | **0.00** | 61% | 6 |

(0.1.6 meanNE=2.50 → 0.1.7 0.90 → +P8 0.35 → **+P9 0.00**; 목표 밴드 0.2 초과 달성.)
비용 = 윈도 내 커버리지 54~71%로 하락(합의 지평 소스 1주기 + MT 1턴 ≈ 2~3s 뒤따름 — Wordly/
Interprefy Camp-B 운영점과 동일). 나머지는 윈도 종료 시 커밋 레인 번역이 기존 동작대로 채운다.
P1 StablePrefixFilter는 캡션에서 P9로 대체(hold-then-accept가 2번째 판정에서 깊은 재작성을
통과시키는 게 잔여 churn이었음 — 참조 구현으로 보존). 한계: 커버리지·지연의 체감 검증은 실기기 몫.

## P8 — 인터림 소스 LocalAgreement 게이팅 (2026-08-10, 실측 후속)

아래 하네스 그대로 재측정. `InterimSourceGate`(LocalAgreement-2, 표면형 커밋 고정, stall 3회 탈출구)를
`livePartial` didSet에 배선해 **연속 2회 디코드가 합의한 단어 접두만** 번역기로 넘긴다. 출하 게이트가
실데이터에서 만든 접두 시퀀스는 측정용 프로토타입과 **바이트 동일**, append-only 불변식 성립.

| 변형 | 표시 상태수 | rewrites | erased | **NE** | vs 0.1.6 |
|---|---:|---:|---:|---:|---:|
| A — 0.1.6 | 10 | 8 | 389 | **2.86** | — |
| B — 0.1.7 (P1+P2) | 7 | 5 | 161 | **1.18** | −58% |
| **C — 0.1.7 + P8 (현재)** | **5** | **3** | **62** | **0.46** | **−84%** |

- **C vs B = −61%**, 표시 상태수 10→5로 **절반**. 목표 밴드 0.2에는 여전히 미달이나, 표시층 단독(B)
  으로는 도달 불가임이 실측된 상태에서 상류 게이팅이 최대 단일 레버임을 재확인.
- **부수 효과**: 커밋 소스가 천천히 자라 P2 인터림 게이트가 실제로 발동(gated 0→3) —
  **DNA 턴 30% 절감**. 안정성과 연산량이 같은 방향으로 움직인다.
- **비용(지연)**: 보류된 꼬리 평균 14.4자(최대 47자 = 환각 중복 구간), 최종 시점 꼬리 지연 7자
  ≈ 프리뷰 1주기(~1s). 캡션 원문 행도 커밋 접두를 쓰므로 원문·번역 세대 일치(P3)는 유지.
- 한계는 아래 배치와 동일(n=1 클립·KO→EN·프리뷰 레인 전용).

## 번역 표시 안정성 — NE 실측 (2026-08-10, ko1.wav × DNA3-4B, KO→EN)

**하네스**: 실제 `ko1.wav`(22.57s) → 0.1.7 번들의 실제 `transcribe`(STREAM/PARTIALS=1/AUDIO_CTX=auto)로
프리뷰 레인 재현(1s 성장 프리픽스 ×10 = 실제 가설 10개) → 실제 `translate-engine-4b`에 **출하 프롬프트
그대로** 투입 → 출하 `TranslationStability.swift`를 그대로 컴파일해 리플레이. 출하 meter 출력과 독립
분석이 **완전 일치**(erased=161, NE=1.18)로 계측기 자체를 교차검증.

| 변형 | 표시 상태수 | rewrites | erased | **NE** | vs 0.1.6 |
|---|---:|---:|---:|---:|---:|
| A — 0.1.6 raw 재번역 | 10 | 8 | 389 | **2.86** | — |
| B — **0.1.7 출하**(StablePrefixFilter+Gate) | 7 | 5 | 161 | **1.18** | −58% |
| E — LocalAgreement-2 소스 게이팅만 | 9 | 6 | 78 | **0.57** | −79% |
| F — LocalAgreement + 0.1.7 | 5 | 3 | 62 | **0.46** | −84% |

- A=2.86은 문헌의 vanilla re-translation(≈2.1)보다도 높다 — STT 자체가 불안정한 실데이터라서.
  0.1.7은 **−58%**를 실증했으나 **목표 밴드 NE<0.2에는 미달**(1.18).
- **지배 잔여항 = 앱이 아니라 상류(STT)**: 프리뷰 재디코드가 의미를 바꾼다
  (`아키테이였습니다`→`아키텍처에 대해 이야기합니다`, `운영의 표정`→`운영에 표준`). 번역은 바뀐 원문을
  충실히 따라간 것뿐. 실제로 소스에 LocalAgreement-2(표면형 커밋 고정)만 걸어도 **E=0.57(−79%)**로
  출하 필터 단독(B)보다 크게 앞선다 → 다음 레버는 표시층이 아니라 **인터림 소스 안정화**.
- ❌ **반증 2건**: ① 대소문자·구두점 무시 *교체*(C) = NE **2.25**로 오히려 악화(교체 자체가 erasure).
  ② 프리픽스 고정+꼬리 추가(D) = NE 0.96이나 `architecture.. Container` 등 구두점 아티팩트 발생.
  둘 다 기각 — 정답은 소스 게이팅(E/F).
- 한계: n=1 클립·KO→EN 단일 타깃·프리뷰 레인만(커밋 레인/«partial» 이미터 미포함)·분모 136자.
  턴 내부 토큰 스트리밍은 append-only라 erasure 0 기여로 미모델링.

## 번역 표시 안정성 배치 — NE 계측 + stable-prefix (2026-08-05, 정본 docs/TRANSLATE_DISPLAY_STABILITY.md)
| # | idea | result | metric |
|---|------|--------|--------|
| P6 | **NE(erasure) 계측** — caption/panel 두 표면, E=\|old\|−LCP(자소), NE=ΣE/최종자수 (Arivazhagan-2020). finalize+10s 요약 로그 | ✅ commit | 목표 밴드 **NE<0.2** (문헌: vanilla re-translation ≈2.1, Google 배포 0.1/BLEU 무손실). 첫 라이브 세션 베이스라인 실측 대기 |
| P0 | **suppression 롤백** — 가드 기각 최종 시 스트리밍된 partial 제거 + 리비전 귀속 verdict(텍스트 변경 시 자동 해제, user-edit 불가침) | ✅ commit | 가드가 기각한 텍스트가 화면에 정식 번역처럼 영구 잔존하던 실버그 제거 |
| P3 | 캡션 (원문,번역) **세대일치 페어링** + 커밋 갭 유지 (livePartialSource) | ✅ commit | 커밋 순간 더 오래된 문장으로의 역행 점프 + 새 원문·옛 번역 미스매치 제거 |
| P1 | **stable-prefix 캡션 표시** — 성장 통과 / 꼬리 ≤12자 즉시 수용 / 깊은 재작성은 2연속 일치 시 수용(LocalAgreement류), 윈도 경계 리셋 | ✅ commit | 전체-프리픽스 재작성이 뷰어에 노출되지 않음 (holds 텔레메트리). 축소(루프붕괴)도 2차 확인 대기 |
| P4 | 패널 **stale-in-place** — 리비전 무효화 시 삭제 대신 회색·이탤릭 유지→재번역 도착 시 제자리 교체. 라우팅 탈락만 진짜 삭제 | ✅ commit | 소실→dots→재스트리밍 사이클 제거. Line.translations는 리비전-유효만 유지 (export/캡션/블록 소비자 불변) |
| P2 | **인터림 게이트** — 이미터 플랩/축소 skip, 순수 꼬리 성장 ≥6자만 턴, 리라이트·첫 요청은 통과 | ✅ commit | 중복 DNA 턴 절감 (interimTurns/gated 텔레메트리로 실측) |

## 배치2: 양방향 언어 + 어텐션 점유율 + prefix-KV 검증 (2026-07-05)
| # | idea | result | metric |
|---|------|--------|--------|
| T12 | **양방향 언어 re-probe** — LANG_CANDIDATES 화이트리스트 argmax, SOT 프로브 매 세그먼트 (seg_lang 시드) | ✅ commit | KO-JA 교대 4세그 TTS: 락 모드 JA 한글혼입 붕괴 → 4/4 클린 + 50266 스위칭 정확. 미설정 비트동일 (jfk 바이트동일) |
| CTX-F | AUDIO_CTX 플로어 192→576 — 4s JA 클립이 ctx448/512서 말미 반복, 576 클린 (스윕) | ✅ commit | FLEURS 4.13% (동급), 짧은 세그 인코드 38% of full 유지 |
| EF-1 | **침묵 조기 flush** (앱 Segmenter, 트레일링 0.7s + 최소바디 2.5s, 적응임계 max(200, 0.15×피크)) | ✅ commit | 10s 윈도 발화 4s 종료 시 커밋 ~10s→~4.7s. far-field(AMI 평균 220) 오절단 방지 테스트 포함 6케이스 |
| **CA-SPLIT** | **flash-decoding 분할 cross-attn** — 20 TG → 20×8 TG, 부분 (m,s,Σe·v) + log-sum-exp 리듀스; 정렬 스코어는 pass1 raw → pass2 정규화 | ✅ commit·기본 ON | ko1 524→**591 tok/s (+13%)**, jfk 풀ctx 418→488 (+17%), 3런 안정; ko1 텍스트 동일, decode 게이트 3종 PASS, FLEURS 4.13% 불변. D1/D2 반증 후 살아남은 유일 레버 실증. CA_SPLIT=0 롤백 |
| T7 | **prefix-KV 캐싱** (sovereignLLM %%PFX/%%TRN + GDN 슬롯 dump/load) — 고정 지시문 39tok 1회 prefill | ⚠️ **구현·검증 완료, 앱 배선 보류** | 프리픽스 경로 출력 레거시와 **바이트동일** (GDN 복원 비트정확 실증). 그러나 웜 A/B(4턴 평균): prefill 162→134ms = **−28ms/턴뿐** — 단문 prefill(≤64tok)은 배치 1-forward '고정비 지배'라 토큰 절감이 시간 절감으로 안 이어짐 (87tok/s 선형 모델 반증). 용어집 주입(200+tok 프리픽스, 멀티청크) 도입 시 재평가 — 프로토콜 보존 |
| T8 | FAQ 사전번역 (앱, 정규화 정확일치, 디스크 영속 + 피부과 스타터 15구) | ✅ commit | 히트 시 0ms/DNA3 턴 0. 퍼지 기각(의료 오히트 > 미스) |

## 화자분리 완벽 배치 — 앵커/마진/미드픽스 + S5 반증 (2026-07-05)
| # | idea | result | metric |
|---|------|--------|--------|
| S1 | **보이스프린트 앵커 모드** (DIAR_ANCHOR=1) — 등록 목소리를 t=0 고정 센트로이드로 시딩, 앵커 대상 출생 문턱 상향(DIAR_ANCHOR_SIM=0.70; 스윕 0.55 흡수/0.65-0.75 정확/0.85 과분할), 재클러스터 후 앵커 방향 재주입 | ✅ commit | KJ 2인 픽스처 **8/8 정확 분리+명명** (기존: 전부 1인 흡수). "누가 말했나"가 추정→대조로. 클리닉(직원 등록) 시나리오 전용 opt-in |
| S2 | **SPK/SPKFIX 5번째 필드 = cos 마진** (best−second) | ✅ commit | 확실 창 0.44-1.0 vs 애매 창 0.22-0.32로 분리 실증 — 앱 융합 게이트(S3) 신호. **부수 사고: live_der.py의 `$`-앵커 정규식이 5필드 거부 → DER 100% (하네스 수정, 러너 awk는 positional이라 무해)** |
| S4 | **mid-session SPKFIX** (DIAR_LIVEFIX=1 기본) — 주기 재클러스터 후 변경 라벨만 재방출, 앱이 라이브 적용 | ✅ commit | ES2004b 라이브: RELABELED **15.03→13.77 (−1.26pt)**, +OSD **14.29→12.95 (−1.34pt)**, 스트리밍 동급(13.72→13.91). 초반 오라벨이 세션 끝까지 안 남음 |
| S5 | 겹침 윈도 제외 (OSD P(ov) 기반 온라인 풀링 게이트 + 재클러스터 skip 마스크) | ❌ **실측 기각 (제거)** | ES2004b full-run 2회 일관 **13.7→37.0% 붕괴**. 격리: 온라인 게이트 복원해도 37.02 → 재클러스터 skip이 원인인데 skip 비율은 **5%뿐(3/61)** — 대량 배제 가설도 기각. 2분 클립 미재현(화자 분포 정상) = 장세션 특이 비선형(K-추정 연쇄 의심, 미규명). 이론상 옳아 보여도 K-민감 파이프라인에 입력 필터는 위험 — 코드 전면 제거, 후속 연구는 제외 대신 다운웨이트 변형으로 |

## P15: 라이브 패널 구조 안정 — decided-once 경계 원장 (2026-08-28)

사용자 리포트: "검증(교정)할 때 밑의 대화 3~5개가 한 번에 사라진다. 어느 구간은 안정,
특정 구간만 심하다."

| # | idea | result | metric |
|---|------|--------|--------|
| P15 | **decided-once 경계 원장** — 줄 경계는 두 단어가 성장 head에서 처음 만날 때 한 번 판정·기록, 이후 재그룹은 결정 재생만. SPKFIX는 각 줄의 **표시 화자만 제자리 갱신**, 진짜 병합은 finalize의 1회 전체 재그룹(원장 우회) | ✅ commit | 재현 테스트: recluster 병합 판정(모든 창→같은 화자) 시 **4줄→1줄 붕괴 + id 3개 소멸 + 번역 고아** → 수정 후 줄 수·id 불변, 화자만 갱신, 번역 VALID 유지. 509 테스트 |

원인 체인 (재현으로 확정): ① recluster가 "잠정 화자2는 사실 화자1" 병합 판정 → SPKFIX가
기존 단어들 밑의 라벨 창을 뒤집음 ② tail 재그룹이 라벨에서 경계를 재유도 → 인접 경계 소멸,
줄 병합 ③ `Line.id`(=첫 단어 id)가 바뀌어 SwiftUI가 삭제+삽입 렌더 = "사라짐", id-키 번역 고아.
"3~5개"인 이유 = freeze가 merger 커밋을 요구해 긴 세그먼트 동안 10s+가 un-frozen(짧은 턴이면
3~5줄). "특정 구간만"인 이유 = margin 낮은 구간(짧은 턴)에만 SPKFIX 버스트가 몰림 —
transcribe.zig:812 주석의 가정("labels **stay put**")이 SPKFIX에서 거짓이었다.

주의(설계 트레이드): 라이브 중 병합/재분할 정보는 화자 라벨로만 반영되고 구조는 finalize까지
유지 — 같은 화자의 연속 말풍선 2개로 보이는 건 정상이며, P10-3("live line count must be
monotonic")과 동일 철학. 라이브 실측(실기기)은 보류 중.
