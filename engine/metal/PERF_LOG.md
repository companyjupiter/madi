# Metal perf optimization loop — log

Autonomous self-paced optimization on branch `feature/metal-perf-loop`.

## Protocol (per iteration)
1. Pick next idea (backlog below, or derive from quark `_perf__measured` bands).
2. Implement (kernel / host edit).
3. **Build** — fail → revert, log FAIL.
4. **Correctness gate** (hard): `test_encoder` + `test_decoder` stay green AND
   `transcribe assets/jfk.wav` text **exactly** == golden:
   `And so, my fellow Americans, ask not what your country can do for you, ask what you can do for your country.`
   Any mismatch → revert, log FAIL.
5. **Measure** encoder/decoder ms (≥3 runs, take min).
6. **Decide**: faster beyond noise (>3%) & correct → keep → regen quark → `git commit`.
   Else → `git revert`(working tree) → log as tried-and-failed (Data).
7. Append a row below. Regenerate quark after any committed `.metal`/`.zig` change.

## Baseline (M4 Pro, jfk 11s) — start of loop
encoder ~565 ms · decoder ~120 tok/s · flash_attention_enc_f16 4.88 ms/layer
front-end conv1d_gelu ~50 ms × 2.

## Idea backlog
- conv1d_gelu: F16 weights (bandwidth), or MPS/im2col GEMM, or better tiling.
- decoder: F16 weights for self/cross/MLP GEMVs (M=1, bandwidth-bound).
- decoder cross-attn (flash_cross_attn): F16 K/V cache.
- logit_gemv_f16: tune threadgroup / 2-row.
- encoder: fuse bias into LN/GEMM epilogue; reduce per-layer sync further.

## Results (newest first)
| # | idea | result | metric | commit |
|---|------|--------|--------|--------|
| S3-PREV-CTX | **이전 세그먼트 텍스트 조건화** — SEG ` %%PROMPT <text>` → `<|startofprev|>` 글로서리 뒤 꼬리(224tok 캡, ts_mode 패스 제외, caps `seg-prompt`), whisper.cpp condition_on_previous_text 동형 | ❌ **반증** — 코드 미출하(패치 `bench/wer_runs/s3_seg_prompt.patch`) | 연속 발화 상주 스트림, 동일 id 대조: test-other 1500 WER 4.88→4.79(부트스트랩 CI [−0.63,+0.31], 개선 71/악화 103, 이득=35s 발화 1건, 120자 꼬리 4.88→4.88) · test-clean 800 1.60→1.76(챕터 +26.5pt 절단) · FLEURS-ko 무관문맥 5.56→5.62 · oracle 참조 5.61→7.45(대문자 스타일 복사 교란; 자연 대소문자 5.69% (+0.08pt, 개선 50 / 악화 69, ALLCAPS 출력 0/800)) · 디코드 중앙값 43→54ms(**+25%**). 실패=프롬프트 에코·오철자 전파·잉여 구두점·절단; 개선=고유명사 일관성(S4 글로서리가 대체). 정본 PERF_LOG.md S3 | — |
| S5-SELFATTN-SPLIT | **self-attn seqlen 분할(20 TG → 20×4) + 점유율 레버 검토** — CA-SPLIT과 같은 레시피(`gpu_attention_split`+log-sum-exp reduce, SA_SPLIT 옵트인) | ❌ **반증 (노이즈)** — 코드 미출하(패치 보관) | ko1 STREAM 106tok decode, 순서교대 6쌍: split 183/181/182/181/188/178 vs legacy 184/185/183/182/184/181 = **−0.5%**(밴드 겹침), 텍스트 동일. self-attn은 4층×~30µs로 상한 자체가 작고, 디코드 2ms/tok의 잔여는 ~56 의존 디스패치 체인 — int4(D1 0%)·CA-SPLIT 후 남은 GEMV 점유율 레버도 같은 물리라 폐기. 프로파일 모드(SOV_METAL_PROFILE)는 jfk/ko1 모두 rescue 경로로 빠져 tiny 커널 분포를 못 잰다(config 주석 재확인). Quark atom `fn__decodeBlock`, `metal_kernel__gpu_attention` | S5 |
| S2-PREVIEW-FP | **프리뷰 forced-prefix** — `PREVIEW <wav> %%FP <합의 원문>`: 앱 LocalAgreement 게이트가 굳힌 접두를 BPE 인코드해 S1 배치 프리필로 seed(예측 없음, `d_conf`=0), 꼬리만 디코드. 플레인 패스만(ts 재디코드·SEG 자유), 캡 `[caps] preview-fp`. 부수: MPS 형상 캐시 64→512 링+재활용(가득 차면 전 MPS matmul이 비캐시 경로로 떨어지던 결함), 프리필 행 16-버킷 | ✅ **Win** | 1s 성장 프리뷰 하네스(ko1/ko2/ko4/jfk): 프리뷰 decode 합계 **5670→3502ms(−38%)**(−38/−25/−63/−11%), 원시 프리뷰 NE **4.26/4.71/2.26/0.40 → 0.32/0.24/0.17/0.15(−90%)**, 게이트 표시 NE 동일, 커버리지 ≈(0.92/0.89/0.99/0.76 → 0.92/0.90/0.91/0.76), 커밋 SEG 4/4 동일. S1 회귀 동일(jfk 골든·verify·72 vs 230ms), preview_lane_gate WIN×2. Quark atoms `fn__main`(PREVIEW 파싱·seed), `fn__prefillBlock`, `fn__mps_cache_get` | S2 |
| S1-PREFILL | **디코더 시퀀스 프리필** — 시드 prefix(`<|startofprev|>` PROMPT 용어 + sot/lang/task)를 토큰당 4층 순차 KV-fill 대신 **한 번의 배치 인과 패스**로: M-row F16 GEMM(JIT dequant) + `causal_attention_rows`(row×head TG) + 공유-KV `flash_cross_attn_f16kv_rows`. prefix ≥8행만 배치(`DEC_PREFILL_MIN`), sot-only 시드는 종전 경로(바이트동일), 스크래치 ~40MB 지연 할당, `DEC_PREFILL=0` 롤백, `DEC_PREFILL_VERIFY=1` 양경로+K/V 편차 리포트 | ✅ **Win** (실측 게이트 전부 통과) | 32용어=158tok 프롬프트: jfk decode **226→70 ms(−155/패스, 3쌍 순서교대)**, 시드 단독 190.6→34.6 ms; ko1 STREAM(auto ctx) PREVIEW/SEG **350→196 / 345→191 ms**; FLEURS-ko 382 엔진 패스 409→350 s. 게이트: FLEURS CER **5.91==5.91**, 가설 **382/382 바이트동일**, jfk PROMPT 텍스트 6/6 동일, PROMPT 없는 jfk 골든 바이트동일, `preview_lane_gate` PROMPT 유/무 WIN(conf drift ≤5e-4), RSS +2.7MB. K/V 편차 F16 GEMM 급(층3 K max 5e-2/5.9, mean 1e-3). Quark atoms `fn__prefillBlock`·`fn__prefillEmbed`·`metal_kernel__causal_attention_rows`·`metal_kernel__flash_cross_attn_f16kv_rows`·`metal_kernel__qkv_bias_store_rows`·`metal_kernel__emb_pe_rows_q8` | S1 |
| KO-DIAR-P7 | **active sparse birth-fallback 보존** — tentative birth 당시의 정상 visible fallback을 `DiarConfirmState`에 보존. P6 quarantine 발동도 post-lock `active=true` cluster 중 support ≤3창으로 축소하고, 그 ID가 다시 direct-match되어도 confirm 전에는 `diarAssign` update가 돌려주는 자기 자신 대신 저장한 fallback을 표시. confirmed 화자·internal raw-id/centroid·최종 relabel·녹음 경로는 불변 | ✅ live 정확도/미해결 Win·실제 Vox24 exact | P6 대비 한국어 probe stream DER **11.42→10.79%(−0.63pt)**, word DER **4.70→4.26%(−0.44pt)**, FA **0.57→0.07s**, wrong-visible **4.906→4.238%(−0.67pt)**, unresolved **12→9**; churn **0.976/min exact**, meeting과 final relabel **12.69/4.97 exact**. 최신 동일 바이너리 실제 4화자 Vox24는 DER **9.38**, wrong-visible **4.55**, unresolved **8.42**, churn **3.82**, TTC p90 **24.59s**, causal DER **8.98**가 평균·case gate 모두 exact/NOISE. Quark atoms `fn__liveVisibleSpeaker`·`fn__main` | phase-7 |
| KO-DIAR-P7-NEG | **inactive stale까지 persistent fallback 적용 역검증** — post-lock에서 `active=false/count=1`인 prototype도 미리 quarantine하고 향후 direct match를 birth fallback으로 접음 | ❌ 실제 복귀 화자 첫 창 지연·Zig 조건 축소 | 한국어 probe는 DER **10.27**, wrong-visible **3.66**, unresolved **7**, overcount **0**까지 추가 개선되고 Vox24 평균 DER도 **9.38→9.35**였지만, `qygfk` ID1은 inactive/count1에서 훗날 실제 화자로 복귀해 첫 1.5s가 ID0으로 접힘: DER/causal DER **17.83/16.97→18.19/17.33(+0.36pt)**. active 재등장 support가 probe 유령 **3창**, qygfk 실제 화자 **5창**임을 trace로 분리해 inactive 선제 quarantine 폐기 | data |
| KO-DIAR-P6 | **post-lock sparse tentative livefix quarantine** — online birth를 삭제하거나 재클러스터 후보에서 빼지 않고 internal centroid·raw-id writeback은 그대로 보존. birth-lock(64창) 이후 direct confirmation이 아직 false인 stable id가 첫 재등장 때 재클러스터 지지 ≤3창이면 presentation만 quarantine하고, 실제 독립 음성 증거로 confirm된 뒤 기존 SPKFIX 복구 허용. trace상 probe 유령 ID4는 m=104 support 3에서 포착된 뒤 support 7/8이어도 direct-confirm false, 실제 `ycxxe` ID2와 `cwryz` ID7은 post-lock support 4로 분리. `DIAR_LIVEFIX_TENTATIVE_QUARANTINE=0` 완전 rollback, max support 독립 튜닝 | ✅ live 체감 Win·녹음/최종/실제 Vox24 exact | 한국어 probe stream/final DER **11.42/12.69 exact**, word DER **4.70/4.97 exact**, unresolved **12 exact**, visible peak/overcount **4/1 exact**인 채 wrong-visible **6.920→4.906%(−2.01pt)**, churn **3.255→0.976/min(−70.0%)**, mid-fix windows **13→6**. meeting 전 축 exact. 최신 동일 바이너리 실제 4화자 Vox24는 DER **9.38**, wrong-visible **4.55**, unresolved **8.42**, churn **3.82**, TTC p90 **24.59s**, causal DER **8.98**가 평균·24/24 case별 exact. `DIAR_K_TRACE`와 harness stderr 보존으로 count/active/confirmed/quarantine provenance 고정. Quark atoms `fn__main`·`fn__liveRecluster` | phase-6 |
| KO-DIAR-P5-NEG | **전역 livefix 최소 지지 3→9창 역검증** — 유령 stable-id의 periodic SPKFIX 참여를 단일 `DIAR_LIVEFIX_MIN_WIN` 상향으로 억제 | ❌ 실제 correction까지 과지연·Zig 변경 없음 | 한국어 probe DER **11.42→16.58**, word DER **4.70→10.72**, churn **3.255→10.42/min**으로 즉시 REGR. 지지창 임계값은 confirmed lifecycle과 분리하지 않으면 정상 화자의 교정도 함께 차단함 | data |
| KO-DIAR-P4-NEG | **near-threshold raw birth soft-match 역검증** — probe의 거짓 B 중복 birth(best 0.3959 vs threshold 0.40, margin 0.151)와 A 재출생(0.3891, margin 0.179)을 전역 `DIAR_SIM` 하향 없이 `[thr−gap,thr)`+최소 margin 조건으로 기존 비앵커에 흡수. gap .008/.020 × margin .15/.17로 두 사건을 분리 | ❌ unresolved tail 또는 NOISE·Zig 원복 | 두 거짓 birth 동시 제거: DER **11.42→11.08**, word DER **4.70→4.27**, churn **3.255→2.278**이나 unresolved **12→13 REGR**. B 중복만 제거도 같은 tail REGR; A 재출생만 제거는 raw births **5→4**지만 DER/wrong-visible/churn/overcount 전부 exact라 사용자 Win 없음. 결론: near-threshold birth가 후속 recluster correction의 유용한 자유도이기도 해 acoustic proximity만으로 억제 불가. 다음 표적은 birth 삭제가 아니라 stable-id 발권/표시 lifecycle | data |
| KO-DIAR-P3 | **라이브 early-K4 sparse support 분리 gate** — 녹음 P2의 장세션 기준 `100창/최소4창`은 보존하고, overlap 재관측이 있는 live recluster에서만 `64창/최소5창`을 요구. 한국어 probe trace상 정답 A→B→C→A인데 online birth가 B를 20.0/21.5s에 중복, A를 71.5s에 재출생시킨 뒤 m=72 spherical K4 support `{4,23,29,16}`이 stable id 5를 추가 발권하던 경로를 늦춤. `DIAR_LIVE_PROMOTE4_LONG_WIN/MIN_WIN`으로 독립 rollback | ✅ live 체감 Win·녹음/최종/실제 Vox24 exact | probe live DER **11.42 exact**, word DER **4.70 exact**, final relabel **12.69/4.97 exact**, unresolved **12 exact**인 채 wrong-visible **8.995→6.920%(−2.08pt)**, churn **5.208→3.255/min(−37.5%)**, visible peak **5→4**, overcount **2→1**, mid-fix windows **20→13**. meeting 전 축 exact; file P2 **9.33/3.38 exact**. 최신 동일 바이너리 Vox 실제 4화자 24파일 baseline/candidate: DER **9.38**, wrong-visible **4.55**, unresolved **8.42**, churn **3.82**, causal DER **8.98** 전부 case별/평균 exact. 오래된 pre-silence-gate 24파일(9.06)을 섞으면 `ypwjd 4.80→12.39` 거짓 회귀가 발생함을 탐지해 폐기하고 same-binary baseline `/tmp/madi-p3-live-default-vox24`로 재고정. Quark atoms `fn__maybePromoteSpherical4`·`fn__liveRecluster`·`fn__diarizeEmb` | phase-3 |
| KO-DIAR-P2 | **긴 세션 spherical K4 singleton 승격 억제** — legacy auto-K가 2/3인 경우의 조건부 K4 승격은 유지하되, 100개 이상 1.5s ledger에서 4번째 cluster 지지가 4창(<6s) 미만이면 outlier split로 판정. 짧은 panel은 brief speaker recall을 위해 기존 승격을 보존하고 `DIAR_PROMOTE4_LONG_WIN/MIN_WIN`으로 exact rollback·민감도 검증 가능. `DIAR_K_TRACE`는 legacy/spherical K별 silhouette·cluster support를 opt-in 노출 | ✅ 녹음 auto-K Win·기존 Vox 승격 Win 보존·라이브 exact | 한국어 probe file auto **K4→K3**, block DER **9.95→9.33%(−0.62pt)**, word-support **3.78→3.38%(−0.40pt)**; meeting과 forced-K exact. 기존 VoxConverse 216 전수에서 DER가 변했던 7개 승격 후보를 재실행해 **7/7 기존 Win exact**: `migzj 49.05→17.77`, `gwtwd 18.06→4.52`, `txcok 14.78→1.55` 등. 2화자 `ntchr`의 K4 singleton만 K2로 억제하고 DER **0.93 exact**. 한국어 16개 file/live profile에서 probe file auto 외 전 지표 exact; live probe 과출생은 별도 P3 표적. Quark atoms `fn__maybePromoteSpherical4`·`fn__diarizeEmb`·`fn__liveRecluster` | phase-2 |
| KO-DIAR-P1-NEG | **fixed-K periodic recluster acceptance 5계열 역검증** — mid-writeback 제거, shadow cadence(16/4/1/every-segment), final-only immutable-online-ledger gate를 한국어 fixed-K와 실제 Vox live core/24파일에 교차 적용 | ❌ 무감독 acceptance 신호 부재·Zig 원복 | writeback 제거는 한국어 probe stream/final **18.93/40.28→10.23/10.23**이지만 실제 `migzj/jnivh/gwtwd` live UX tail 회귀. final-only gate는 한국어 final **40.28→18.93**, 실제 core3 exact였으나 Vox24 reject에서 change ratio `cwryz .254`는 **28.50→15.95 Win**, `dhorc .308`은 **7.08→37.80 REGR**, `ypwjd .439`는 **3.63→32.92 REGR**로 방향 분리 불가. 결론: 정답 없는 fixed-K batch 결과는 online ledger와의 변화량만으로 품질을 판별할 수 없으며 무조건/조건부 writeback 모두 보류 | data |
| KO-DIAR-P0 | **한국어 합성 정답 gate 고정** — 로컬 한국어 diar 킷(TTS 합성, 비배포) 2파일/RTTM/word timestamp를 SHA256으로 잠그고, 원본 utterance-block RTTM DER과 단어 구간 기반 speech-support DER을 동시에 측정. 파일 auto/forced + 라이브 auto/fixed-K × recluster on/off를 한 러너로 전수 재현하며 component seconds와 percent 단위를 분리. Win gate는 한 축 개선으로 다른 축/VAD miss·FA를 악화시키는 후보를 REGR 처리 | ✅ 재현·원인 격리 | 기존 수치 전부 exact 재현. meeting fixed-K: no-recluster 즉시 **6.89/word 3.92**, recluster 즉시 **9.06/6.20**·최종 **4.90/1.93**. probe fixed-K: no-recluster **10.23/4.02** vs recluster 즉시 **18.93/13.64**, 최종 **40.28/36.04**, wrong-visible **5.44→59.23%**. 따라서 전면 recluster 제거는 meeting 최종 Win을 버리고, 무조건 적용은 probe를 붕괴시킴 — 다음 표적은 fixed-K conditional acceptance. 16 Python tests green; baseline `/tmp/madi-ko-diar-p0-20260722-v3`. Quark causal atom `fn__liveRecluster` | gate |
| MEM-CACHE-REPLACE | **encoder F16 cache가 완성된 후 원본 Q8 표현 해제** — 32 layers×4 projections의 cache 완전성을 먼저 검증하고 Q8 `qs/scales` 256 allocations을 free+nullable로 전환. 삭제가 probe chain을 끊던 Metal open-address registry는 tombstone+reuse로 교정하고 Metal 4 residency set에서도 freed allocation을 제거. no-cache fallback은 동일 코드로 유지 | ✅ 메모리 Win·속도/품질 보존 | 정확히 **668,467,200 B (637.5 MiB)** 해제: F16-cache ready **2091→1453 MB (−638, −30.5%)**, JFK active **2247→1609 MB (−638, −28.4%)**. 2-process 구조에서 누적 **3561→1609 MB (−54.8%)**. encoder cached ~**489 ms 불변**; JFK text/timestamp/speaker/barrier exact, score noise worst **0.0014 < 0.005**. no-cache Pareto는 ready/active **890/1071 MB**이지만 encoder **565 ms (+15.5%)**라 24GB+ 기본으로는 기각. buffer collision delete-chain self-test PASS, cached/no-cache preview gate 27 events PASS. Quark atoms `fn__cacheWeights`·`fn__forward`·`fn__releaseQuantizedWeights`·`fn__main`; whisper.cpp 역검증 `fn__whisper_free`·`fn__ggml_backend_metal_buffer_*_free_buffer` | phase-2 |
| LIVE-PREVIEW-1PROC | **라이브 preview 단일 resident lane** — 별도 `transcribe`를 제거하고 main STREAM stdin에 직렬 `PREVIEW <wav>` job을 추가. 전용 begin/end marker로 Swift parser가 preview word/partial만 격리하며 Zig는 structured event, `g_words`, language lock, diar/OSD centroid, session profiler를 불변으로 유지. latest-only/admission 정책은 그대로라 committed SEG·DNA가 우선 | ✅ 메모리 Win·커밋 품질 exact | M4 Pro Q8+ENC_F16_CACHE 실사용 경로: active main+preview **3561→2247 MB (−1314 MB, −36.9%)**; ready 단일 process **2091 MB**. real-model JFK `PREVIEW→SEG→FLUSH`: baseline/candidate normalized structured events **27/27 exact**, marker **2**, SEG_END/FLUSH_END **1/1**; baseline 0.910s, preview+SEG 1.585s(직렬 preview 증분 0.675s). Swift **338 tests**, engine/app build+codesign green. 영구 게이트 `bench/preview_lane_gate.py`; Quark atom `file__transcribe.zig/fn__main` | phase-1 |
| LIVE-SILENCE-GATE | **라이브 무음 유령화자 차단** — Silero가 `crms={0,1}`로 판정한 뒤에도 median×0.3 상대 게이트를 다시 적용해, 무음 과반 청크의 median=0에서 `0 < 0`이 false가 되어 무음 창 전체가 `diarAssign`으로 누출되던 경로를 절대 speech gate로 교정. 동시에 `g_vad_iv.len==0`의 두 의미(VAD asset 없음 vs VAD 실행 결과 speech 0)를 명시적 availability로 분리해 emit fallback이 확정 무음을 화자로 내보내지 않게 함. clustering/confirmation/recluster 수학은 불변 | ✅ 직접 재현 Win·dense-speech 전 축 exact | digital silence 10s: Silero **0 speech / 313 frames**, live `SPKTRACE/SPK` **6/6→0/0**. JFK→20s silence: 기존 11.0s false birth(cos **0.0449**) + 12.5s silence self-confirm(cos **1.0000**) → 수정 후 **t≥11 assignment/event 0**. 최신 실제 3–4화자 `migzj/jnivh/gwtwd` A/B: mean DER **17.27 exact**, wrong-visible **13.04 exact**, unresolved **13.00 exact**, churn **5.87/min exact**, first-label p90 **11.00s exact**, TTC p90 **19.73s exact**; JFK golden exact. STREAM 통합 회귀 2종 + 기존 UX tests 총 11 green. Quark atoms `fn__main`·`fn__diarAssign`·`fn__emitClippedSpk` | phase-1 |
| EMBED-SCREEN-2 | **임베더 스크리닝 2차 (ReDimNet-B2 MIT · ERes2NetV2 Apache-2.0)** — EMBED-UPGRADE 프로토콜 그대로(엔진 VAD dump 재사용·동일 1.5s grid·동일 kmeans/실루엣/md-eval, ResNet34 기준행 바이트동일 재현) python-only 오라클. ReDimNet-B2 ft_lm(torch.hub IDRnD, VoxCeleb2, 5.07M/192d), ERes2NetV2(sherpa-onnx 3dspeaker zh-cn 200k, 17.8M/192d), 대조군 ERes2Net-V1 en | ❌ 두 후보 모두 mean+tail 기각·포트 안 함 | vox24 forced ResNet34 **9.03** vs ReDimNet **10.41**·ERes2NetV2 **11.15**; auto **6.21** vs **7.94**·**16.07**(k=1 붕괴, migzj 69.87). tail: ReDimNet `yfcmz +37.63`·`mesob +19.51`, V2 `yfcmz +35.03` — 전부 bwzyf +5.39 기각 선례 초과. 메커니즘: 1.5s SEP(intra−inter cos) ResNet34 **0.420** > ReDimNet 0.395(1.0s에서 0.299로 절벽 — 짧은창 강건 가설 기각) > V2 0.295(zh-cn 도메인: inter 0.31). 검증 EER 우위↛짧은창 클러스터링, 3·4번째 패밀리에서 재확인. 유일 재방문 신호: 후보 공통 win `txcok −24.05`·`gwtwd −13.86` = ResNet34 forced가 약한 파일 — 앙상블/폴백 질문이지 스왑 아님. 하네스 scratchpad `a3_embedder/a3_oracle.py` | data |
| OVL-GATE-NEG | **S5 후속 완결: 겹침 창 다운웨이트/출생차단 역검증** — S5("제외 대신 다운웨이트로")의 지시대로 OSD `P(ov)` 창 게이트(`osdOverlapFrac`, emitOsdOverlapFrom과 동일 RFIELD/2 프레임 클록)를 `diarAssign`에 연결: 게이트 발동 창은 (a) W=0 완전격리(assign-only), (b) W=0.25 다운웨이트, (c) W=1 출생만 차단. gate 0.5/0.75 × pov 0.5/0.7 스윕, no-op guard(knob off) 3파일 전지표 +0.00 확인 | ❌ 3변형 전부 REGR·Zig 원복 | **진단 골드**: 격리 대상 창의 첫 라벨 오답률 **migzj 46.5% / jnivh 45.0% / gwtwd 83.3%** vs 잔여 업데이트 창 **4.3/1.2/0.0%** — OSD 게이트가 독성 창을 정확히 분리함. 그러나 학습 억제는 전부 순손실: W=0 DER **17.27→17.44**·wrong-visible **13.04→13.91**·churn **5.87→6.73**; W=0.25 DER **+0.33**·churn **+0.94**; W=1(출생차단만) DER **+1.10**·jnivh **+2.24** (신규 화자 출생이 정확히 겹침 순간에 필요). gate 0.75도 REGR. 결론: 이 아키텍처는 겹침 멀티뷰 학습에서 순이득(LIVE-PATH4 일관) — S5 계열(제외·다운웨이트·출생차단) 완결 기각. 미측정 후속: 겹침 신호를 margin 캡(<0.20 ambiguous 버킷)으로 S3 LLM 융합에 노출 — 엔진 DER 불변이라 본 게이트로는 측정 불가. runs `track_a_ovl50/ovl75/ovlbb/ovlw25`; Quark 후보 atoms `fn__diarAssign`·`fn__emitOsdOverlapFrom`; 구현 제거 후 `fn__osdOverlapFrac` atom 없음 | data |
| CENT-CAP-NEG | **센트로이드 무감쇠 누적합 반증** — `DiarCentroid.sum`의 EMA화(`DIAR_CENT_CAP`: cap 도달 시 sum을 cap−1 창 분량으로 재스케일, 앵커 제외) 16/32/64 스윕. "초기 창이 프로토타입을 영원히 지배 → 드리프트 추적 불가" 가설 검증 | ❌ REGR·Zig 원복 | cap16: churn **5.87→7.33**·time-to-correct p90 **19.73→28.42(+8.69s)**·unresolved **+1**; cap32: unresolved **13.00→14.67**(jnivh **9→14**); cap64는 패널 세션(화자당 ~35창)에서 미발동 no-op. 평균 DER은 −0.12 개선이나 라벨 안정성 붕괴 — 무감쇠 누적합이 이 세션 길이(<10분)에선 옳음. 장세션(>30분) 드리프트는 별도 검증 필요. runs `track_a_cap16/32/64` | data |
| WIN-ALIGN-NEG | **1.5s 고정 그리드 → VAD 정렬 윈도 배치 역검증 (3변형)** — straddle(턴 경계 걸친 창의 두 목소리 혼합 임베딩) 제거 가설. v1 발화구간 전면 타일링(온셋 기점+우측정렬 꼬리), v2 하이브리드(그리드 유지+내부 온셋만 0.25s 양자화 스냅, 말미 ¼ 제외), v3 결정론적(전역 1.5s 그리드 중간창+온셋창+꼬리창, 전부 콘텐츠 결정론적·0.25s 양자화·중복 제거). 폭은 1.5s 고정(OSD-TARGET-MASK-NEG의 풀링 캘리브레이션 제약 준수), `DiarJob.off` 오프셋 배열로 구현, no-op guard 전지표 +0.00 | ❌ 3변형 전부 REGR·Zig 원복 | v1: jnivh **16.93→13.76(−3.17)** 실재하나 migzj mid-fix **18→31**·wrong-visible **+7.24**·churn **8.20→12.64** — 잡 경계에서 잘린 발화구간의 타일링 위상이 어긋나 같은 오디오가 다른 시각으로 재관측. v2: 온셋 스냅이 직전 턴 꼬리 커버리지 구멍 생성 → jnivh **+3.84** 역전. v3: 위상 문제를 풀었지만 여분 온셋/꼬리 창이 출생 유발 — migzj overcount **0→3**·gwtwd DER **+2.47**·ttc p90 **+10.27s**. 결론: 고정 그리드+3s 좌컨텍스트 재관측 멀티뷰+시간 키 교정(앱 SPKFIX ±0.11s·first-covering-label)이 **공동 튜닝된 국소최적** — 윈도 배치 단독으론 개선 불가, "첫 배정 품질" 레버는 임베더/아키텍처 교체로. runs `track_a_align/align1/align2`; Quark atoms `fn__diarWorker`·`struct__DiarJob` | data |
| LIVE-CAUSAL-OSD | **FLUSH 전 causal overlap UX** — 닫힌 `OsdWin`만 기존 powerset local→global SOLO vote로 매핑해 두 번째 화자 `SPKOV`를 즉시 방출하고, FLUSH의 `SPKOVRESET` 뒤 기존 whole-session attribution으로 교체. raw 0.25/0.35/0.45와 support gap 1.0/0을 역검증하고, loose 0.45+1.0은 평균 DER 9.06→8.06에도 `rcxzg +0.80` tail이라 기각. 최종 default는 OSD 0.75 + causal primary 동시지지(gap 0); `DIAR_LIVE_OSD=0` exact rollback. 평가기는 primary+overlap 동일화자 interval union으로 앱 Set semantics를 재현하고 `--causal-self` 게이트 추가 | ✅ live overlap Win·primary/final 경로 격리 | 실제 4화자 **24파일 전수**: causal live DER **9.06→8.67%(−0.39pt)**, overlap unresolved **17.13→15.45s(−1.68s)**; **9 개선/14 exact**, 유일 양의 delta `azisu +0.05`는 per-file +0.10 gate 이내. 표적: `migzj 26.32→23.33`, `gwtwd 8.55→8.32`, `rcxzg 7.41 exact`. primary DER·wrong-visible·unresolved windows·churn·latency는 동일 실행에서 exact. Quark atoms `struct__OsdWin`·`fn__emitOsdOverlapFrom`·`fn__mapOsdTracks` | phase-1 |
| OSD-TARGET-MASK-NEG | **pyannote local-SOLO target embedding 역검증** — blind `1−P(overlap)` weighting의 기존 실패와 달리, powerset local track의 dominant SOLO 프레임만 (a) ResNet final stats pooling에 사용하거나 (b) 0.75s+ 연속 SOLO crop으로 재임베딩. local id는 10s window 밖/global id로 승격하지 않고 실제 overlap이 있는 1.5s 창만 선택 | ❌ masked pooling·SOLO crop 모두 구조적 REGR·제품 Zig 원복 | masked stats live DER **17.27→18.49**, wrong-visible **13.04→16.14**, unresolved **13.00→15.33**, churn **5.87→10.54**; offline `migzj 17.77→49.44`, `gwtwd 4.52→13.07`. 연속 crop도 live DER **19.87**, wrong-visible **15.45**, unresolved **13.67**, churn **7.82**; offline `migzj 49.31`, `gwtwd 5.24`. target 신호 문제가 아니라 ResNet FC/mean-vector가 1.5s full-window stats 분포에 캘리브레이션돼 subset pooling/crop이 embedding 공간을 이동. 안전한 다음 OSD 활용은 현행 overlap 2nd-row identity로 제한; target-specific pooling은 전용 학습/증류 전에는 재시도 금지. Quark atoms `fn__embed`·`struct__OsdWin`·`fn__emitOsdOverlap` | data |
| LIVE-LOO-NEG | **segment-coherent acoustic leave-one-out 재판정** — low-margin 창의 자기 embedding을 현재 centroid에서 뺀 뒤, 다른 confirmed centroid가 gain 0/.05/.10/.15로 이길 때 첫 표시만 교체. 전역 centroid/recluster는 불변이라 이전 prototype-update 실험과 격리. margin<.20·gain .05는 핵심 3파일에서 DER **17.27→17.00**, wrong-visible **13.04→13.02**, churn **5.87→5.76**으로 Win처럼 보였음 | ❌ 24-file per-case tail REGR·제품 Zig 원복 | 24개 실제 4화자에서 평균 DER **9.06→8.98**, wrong-visible **4.53→4.51**, churn **3.70→3.62**였지만 `ypwjd` unresolved **1→2**, `txcok` DER **17.57→17.81**. gain .10/.15는 `ypwjd` 회귀만 제거하고 `txcok` 오판을 유지. LOO의 자기편향 제거만으로는 짧은 진짜 turn과 잘못된 prototype을 안전하게 분리할 수 없음. Quark 후보 atoms `fn__diarAssign`·`fn__liveRecluster`; 구현 제거 후 `fn__liveLeaveOneOutSpeaker` atom 없음 | data |
| LIVE-CONFIRM-INDEPENDENT | **overlap-independent tentative-birth confirmation** — 10s live segment의 3s left context가 시간축을 되감아 같은 음성을 다시 embedding한다. 실제 trace에서 `gwtwd` 신규 id가 47.50s에 태어난 직후 다음 segment의 47.00s 재관측으로 즉시 확정됐다. overlap embedding과 centroid/recluster 학습은 그대로 보존하고, 표시용 confirmation streak만 마지막 증거보다 1.5s 이상 전진한 관측에서 증가. 기본 `DIAR_CONFIRM_MIN_ADV_SEC=1.5`, 0이면 legacy exact rollback | ✅ live churn/overcount Win·전 지연/정확도 축 무회귀 | 핵심 3–4화자: DER **17.27 exact**, wrong-visible **13.04 exact**, unresolved **39 exact**, first-label p90 **11.0s exact**, TTC p90 **19.73s exact**, churn **6.25→5.87/min(−6.1%)**, peak overcount `0/1/0` exact. 실제 4화자 **24파일 전수**: DER **9.08→9.06**, wrong-visible **4.53 exact**, unresolved **8.42 exact**, first-label **11.31s exact**, TTC **24.59s exact**, churn **3.85→3.70/min(−3.9%)**, 총 peak overcount **8→6**. Quark atoms `struct__DiarConfirmState`·`fn__liveVisibleSpeaker`·`fn__diarAssign` | phase-1 |
| LIVE-CONFIRM-TTL | **tentative birth 연속증거 TTL** — 기존 2-window 확인은 singleton centroid가 수십 초 뒤 다른 실제 화자에게 재사용돼도 같은 화자의 두 번째 증거로 간주했다. auto/no-anchor live에서 4.5초를 넘긴 미확정 streak을 만료시키고, 이미 보인 fallback은 유지하되 늦은 match가 과거 singleton을 소급 승격하지 못하게 raw centroid와 visible lifecycle을 분리. 기본 4.5s, `DIAR_CONFIRM_GAP_SEC=0` exact rollback. 선행 대안인 저마진 `A–B–A`/`AA–B–AA` temporal bridge는 3파일에서 DER·unresolved를 개선했지만 실제 4화자 24파일 replay에서 flank 2/3/4 × margin .10/.15/.20 전 조합이 짧은 진짜 turn을 지워 unresolved tail을 늘려 전면 폐기 | ✅ live churn/overcount Win·정확도/저장본 무회귀 | 핵심 3–4화자: DER **17.27 exact**, wrong-visible **13.04 exact**, unresolved **39 exact**, first-label p90 **11.0s exact**, TTC p90 **19.73s exact**, churn **7.00→6.25/min(−10.7%)**, peak overcount `0/1/1→0/1/0`; final+OSD **10.87 exact**. 실제 4화자 **24파일 동일 바이너리 OFF/ON 전수**: 23파일 전 지표 exact, `gwtwd`만 churn **6.76→4.51/min(−33.3%)**·peak overcount **+1→0**. 평균 DER **9.08**, wrong-visible **4.53**, unresolved **8.42**, first-label **11.31s**, TTC **24.59s** exact; churn **3.9446→3.8508(−2.38%)**, 총 peak overcount **9→8**. auto/file·final relabel·final+OSD 24/24 exact. Quark atoms `struct__DiarConfirmState`·`fn__liveVisibleSpeaker`·`fn__diarAssign` | f62e596 |
| LIVE-SEGMENT-COHERENT | **segment-coherent 첫 표시** — live 10초 segment의 diar embedding은 출력 전에 이미 전부 계산된다는 경로 특성을 이용. 같은 segment 안에서 tentative birth와 주기 recluster가 확정한 `live_ids`를 모아 `SPK`를 한 번만 방출하고, 아직 표시되지 않은 label에 선행 `SPKFIX`를 적용하는 zero-time churn 제거. 기본 ON, `DIAR_SEGMENT_COHERENT=0` exact rollback | ✅ immediate UX Win·저장본 무회귀 | 3–4화자 panel immediate DER mean **19.38→17.27%(−2.11pt, −10.9% 상대)**: `migzj 27.46→26.32`, `jnivh 17.57→16.93`, `gwtwd 13.11→8.55`. churn **9.44→7.00/min(−25.9%)**. wrong-visible **13.04% exact**, first-label p90 **11.0s exact**, 절대 unresolved **39창 exact**, peak overcount **0/1/1 exact**. final+OSD **17.34/7.30/7.97 exact(평균 10.87%)**. 게이트의 분모 역전도 교정: 초기 오류가 줄어 unresolved 비율만 상승하는 경우는 absolute unresolved count로 판정. Quark atoms `fn__diarAssign`·`fn__liveVisibleSpeaker`·`fn__liveRecluster`·`fn__emitClippedSpk` | data |
| LIVE-PROTOTYPE-NEG | **low-margin prototype 오염 방지 역검증** — (a) hard update quarantine margin 0.05/0.10/0.20, (b) lifecycle count·raw-id vote는 보존하고 centroid sum만 confidence-weighted floor 0.50/0.75, (c) birth threshold `DIAR_SIM` 0.35/0.45. 모두 동일 trace binary·panel gate로 A/B 후 제품 Zig에서 제거 | ❌ 전 후보 REGR·원복 | hard 0.05는 DER **19.38→19.03**·unresolved 비율 개선에도 wrong-visible **13.04→13.24%**, churn **9.44→10.56**; 0.10은 wrong-visible **13.49**, 0.20은 DER **28.19**. soft floor 0.50/0.75도 DER **19.03**이나 wrong-visible **13.32/13.26**, churn **10.24/10.01**. `DIAR_SIM=0.35`는 jnivh/gwtwd 악화, 0.45는 jnivh overbirth(peak +3)·wrong-visible **16.57%**. 결론: centroid만 바꾸면 recluster continuity와 표시 state가 어긋남; 수학 변경 대신 segment 내 이미 확정된 state를 coalesce하는 경로가 Win. Quark atom `fn__diarAssign` | data |
| LIVE-ASSIGN-DIAG | **첫 배정 acoustic-margin 역검증** — 기존 `SPK` 계약의 margin을 live UX one-to-one reference mapping과 결합하고, `margin<0.20` ambiguous / `0.20≤margin<1.0` confident / `margin=1.0` control sentinel(첫 centroid 또는 birth)로 분리. 음수 anchor-fallback margin도 파서가 보존하도록 측정 계약 보강 | ✅ 원인 신호 확립 | 3–4화자 panel 317 visible windows: ambiguous **84개 중 30오류(35.7%)**, real-confident **181개 중 3오류(1.7%)**, sentinel **52개 중 23오류(44.2%)**. 즉 실제 margin 0.20 이상 배정은 거의 정상이고, 오배정 56개 중 **53개(94.6%)**가 low-margin 또는 sentinel에 집중. assignment trace 기준 기존 prototype update 오염률은 `migzj/jnivh/gwtwd` **16.5/8.6/8.8%**, birth는 약 절반 오배정. Quark atom `fn__diarAssign` | data |
| LIVE-UX-EXP1 | **전용 UX 게이트 기반 1차 역검증** — (a) full K/recluster=16은 유지하고 중간 8-window centroid relabel probe, (b) UI `SPKFIX` 최소 margin 0.01/0.05, (c) committed window 5s/7.5s와 overlap 3s/1.5s 조합. 후보는 env 격리·동일 바이너리 A/B 후 Zig 전면 원복 | ❌ 전 후보 REGR·원복 | probe8: TTC p90 mean **18.03→17.09s**지만 churn **9.44→10.50/min**, wrong-visible 무익. margin .01: churn **8.82**지만 wrong-visible **13.04→13.17%**·TTC **18.03→18.76s**; .05: churn **8.10**·TTC **14.09s**이나 wrong-visible **13.42%**, 미교정 **65.42→66.53%**. seg5/ov3: first-label p90 **11→6.5s**이나 DER **19.38→21.12%**, wrong-visible **14.22%**, churn **13.97**. seg7.5/ov3는 DER **26.95%**, seg5/ov1.5도 DER **21.13%**·wrong-visible **15.70%**. 결론: 더 잦은 표시/교정은 overlap 재관측과 불안정 centroid를 과가중; 다음 레버는 **첫 assignment 자체의 품질**. Quark atoms `fn__diarAssign`·`fn__liveRecluster`·`fn__emitClippedSpk` | data |
| LIVE-UX-GATE | **라이브 time-to-correct/immediate UX 전용 게이트** — ordered `SPK`/mid-`SPKFIX`를 각 `SEG_END`의 캡처 frontier에 매핑하고, FLUSH 교정은 제외. one-to-one ref mapping으로 first-label latency, wrong-visible exposure AUC, stable-correct p50/p90, 미교정률, churn/min, peak/final visible speaker를 동시 판정. 평균+파일별 꼬리 비대칭 `WIN/REGR/NOISE` 비교기와 합성 테스트 추가 | ✅ 측정 기반선·결정성 확립 | `migzj/jnivh/gwtwd` 동일 빌드 2회 **전 수치 exact**. immediate DER mean **19.38%**, wrong-visible **13.04%**, initially-wrong 미교정 **65.42%**, TTC p90 mean **18.03s**, churn **9.44/min**, peak overcount **0/1/1**. 기존 final+OSD **10.87%**도 재현. Quark atoms `fn__liveVisibleSpeaker`·`fn__liveRecluster`·`fn__emitClippedSpk` | data |
| LIVE-BIRTH-RECLUSTER | **confirmed birth(2번째 window) 기반 adaptive recluster** — 고정 cadence 16은 유지하고, 새 raw speaker가 visible로 확정되며 직전 recluster 이후 ≥4 windows일 때만 session k-means/SPKFIX를 앞당겨 immediate gap을 줄이는 실험 | ❌ 체감 목표 REGR·Zig 원복 | Stage 9 기준 immediate mean **19.38→19.74%(+0.36pt)**: `migzj +0.34`, `jnivh +0.75`, `gwtwd +0.00`; mid-fix 대상 windows **36→39**. final+OSD는 **10.87→10.48%(−0.39pt)**였으나 no-OSD relabel은 **15.40→15.48**로 악화 — 이득은 더 많은 지연 OSD 교정에서만 발생. 사용자 체감 우선 게이트상 “나중 저장본 개선/즉시 표시 악화”는 기각. Quark atoms `fn__liveVisibleSpeaker`·`fn__liveRecluster` | data |
| LIVE-OSD-BIJECT | **local→global OSD cannot-link 최대가중 일대일 매칭** — powerset 한 window의 local tracks는 서로 다른 화자라는 제약을 3×16 SOLO vote assignment에 적용. 공통 default는 file panel **16.44→16.62%(+0.17pt)**라 기각하고, file=legacy·live-FLUSH=bijective로 경로별 분리. 각 대체 배정은 ≥3 vote·row-best의 ≥25%; `OSD_BIJECTIVE=0/1`로 역검증 가능 | ✅ live 저장본 Win·file 완전 보존 | live final+OSD 3-4화자 panel mean **11.36→10.87%(−0.49pt, −4.3% 상대)**: `migzj 18.47→17.34(−1.13)`, `jnivh 7.65→7.30(−0.35)`, `gwtwd 7.97→7.97`. overlap rows **53/14/10→56/18/10**; live stream/immediate·SPKFIX 수는 동일(FLUSH overlap attribution 전용). no-env file **17.77/27.04/4.52 exact baseline**, `OSD_BIJECTIVE=0` live도 **18.47/7.65/7.97 exact 복귀**. Quark atoms `fn__mapOsdTracks`·`fn__emitOsdOverlap`·`fn__diarizeEmb` | 4a729db |
| EMBED-UPGRADE | **최신 공개 임베더 포팅 전 ONNX oracle 게이트** — Apache-2.0 WeSpeaker/3D-Speaker 계열 CAM++(512d)·ECAPA512(192d)를 제품 포팅 없이 Madi의 동일 Silero speech coverage·1.5초 grid·clustering에 삽입. 새 `embedder_oracle.py`가 기존 ResNet dump로 K/silhouette/DER exact self-check한 뒤 candidate auto/ref-K를 역검증 | ❌ 범용 교체 REGR·포팅 중단 / ✅ oracle 하네스 보존 | self-check 3/3 **DER·K·silhouette 정확 일치**. 3화자 문제 `jnivh`는 ECAPA ref-K에서 **28.18→14.89%**였지만 일반화 실패. 실제 4화자 **24파일 전수** ECAPA forced-K mean **6.20→9.57%(+3.37pt)**, 개선5/악화15, 최악 `yfcmz +29.26pt`; ECAPA auto도 **9.43%**, 개선4/악화16. CAM++ 1.5초 panel forced mean **43.43%** vs paired baseline **21.08%**; 3초 centered context도 악화. verification EER 우위는 short-window diar clustering 품질을 보장하지 않음. Quark/source atoms `fn__embed`·`fn__diarizeEmb`·`fn__kmeansFitMode` | data |
| OSD-WEIGHT | **diart overlap-aware stats pooling의 Madi 적용 가능성 검증** — OSD와 기존 embedding 병렬성은 보존하고, confident overlap이 있는 1.5초 창만 선택 재임베딩. ResNet `fn__embed` 최종 mean/std에 `1−P(overlap)` weight를 적용; 기본(0.45/floor0.10)과 보수(0.65/floor0.50) 두 설정을 panel A/B | ❌ 구조적 REGR·Zig 원복 | baseline mean **16.44%**. 기본 weight **17.28%(+0.84pt)**: `migzj 17.77→17.96`, `jnivh 27.04→27.04`, `gwtwd 4.52→6.83(+2.31)`; 보수 설정도 **17.33%(+0.89pt)**, gwtwd **7.00**. diart는 이미 대상 화자가 알려진 local segmentation activation으로 target frame을 가중하지만 Madi의 blind 1.5초 embedding에는 target identity가 아직 없음. 총 overlap 확률을 쓰면 두 화자의 유효 증거를 함께 약화하므로 threshold 튜닝으로 해결 불가. Quark atoms `fn__embed`·`fn__diarWorker`·`struct__OsdWin` | data |
| OSD-PRIMARY | **diart local diarization의 primary 경계 교차적용 역검증** — pyannote powerset 17ms local-track을 (a) 전체 primary timeline으로 치환, (b) 기존 1.5초 coarse timeline은 보존하고 고신뢰 SOLO identity 교정 구간만 carve하는 두 방식으로 3-4화자 panel 측정. votes/ratio도 30/2→60/3→120/4로 강화 | ❌ 두 방식 모두 REGR·Zig 원복 | baseline final+OSD mean **16.44%**. 전체 fine-grid **16.74%(+0.30pt)**: `migzj +0.77`, `jnivh +0.42`, `gwtwd −0.29`; 더 강한 confidence도 동일하거나 `migzj +1.00`. safe carve도 `migzj +0.32`, `jnivh +0.00`, `gwtwd +0.24`로 순악화. 결론: local track은 같은 OSD window 안 두 번째 화자 identity에는 유효하지만, 10초마다 재시작되는 local label을 global primary로 승격하면 경계/identity noise가 coarse embedding 결정을 오염. 현 OSD는 overlap 행에만 유지. Quark atoms `fn__diarizeEmb`·`struct__OsdWin`·`fn__emitOsdOverlap`; diart의 rolling-buffer local diarization 패턴을 Madi 신호로 역검증 | data |
| OSD-FLOOR | **diart(MIT) constrained online clustering 교차적용 전 검증** — Madi가 이미 보유한 pyannote powerset local-track의 SOLO frame 수를 10초 window별 화자수 하한으로 쓸 수 있는지 threshold 3/10/30/60을 VoxConverse-dev 216 전수 trace. 제품 Zig에는 적용 전 calibration부터 판정 | ❌ 신호 부적격·trace 원복 | f3: auto-K를 올릴 17건 중 안전 후보 **5**, 실제 화자수 초과 **12**. auto K2→floor3만 좁혀도 정답 **4/7**, 오탐 **3/7**. f30은 복구2/오탐10, f60은 **복구1/오탐4**로 강화할수록 recall만 소실. 문제 panel도 `migzj=3, jnivh=2, gwtwd=3`이라 실제 누락 샘플 jnivh를 못 잡음. 결론: local track은 OSD 두 번째 행 identity에는 유효하지만 global K cannot-link/floor로는 calibration 부족. `--vox-all` 전수 하네스만 보존. Quark atoms `struct__OsdWin`·`fn__emitOsdOverlap`·`fn__liveRecluster`; 외부 기준 diart `OnlineSpeakerClustering` | data |
| LIVE-PATH4 | **라이브 segment 경계/전환 안정화 3방향 역검증** — (a) ASR 3초 좌측 context를 diar에서도 중복 소비하는 구조를 끊고 새 PCM만 1.5초 전역 ring으로 연결, (b) 낮은 acoustic margin(<0.25) 전환을 이전 표시 화자로 유지하는 causal hysteresis, (c) recluster 주기 16→8. 모든 실험은 raw/visible 경로를 격리하고 3-4화자 패널로 판정 후 Zig 전면 원복 | ❌ 3안 모두 REGR·원복 | baseline immediate/final+OSD mean **19.38/11.36%**. PCM ring **24.36/14.18%(+4.98/+2.82pt)** — shifted overlap multi-view가 중복비용보다 turn-boundary 정보 이득이 큼. overlap 자체를 0으로 하면 final+OSD **22.64%(+11.28pt)**로 boundary miss 폭증. hysteresis immediate **24.72%(+5.34pt)**·mid-fix 창 **36→75** — 짧은 실제 turn 흡수. recluster8 immediate **19.72%(+0.34pt)**, final+OSD **14.51%(+3.15pt)**; migzj final **18.47→27.91** ID remap 불안정. 결론: 현재 overlap multi-view·16-window cadence·무-hysteresis 유지. Quark atoms `fn__diarWorker`·`fn__diarAssign`·`fn__liveVisibleSpeaker`·`fn__liveRecluster`; app atom `Segmenter.fn__cut` | data |
| EXACT4-UI | **정확히 4명 UI + fixed-K 경로 역검증** — 앱 `SpeakerCount`에 persisted rawValue 충돌 없이 exact-4를 배선하고(309 tests·release app build PASS), VoxConverse 실제 4인 24파일을 전수 측정. fixed 경로가 auto와 같은 K=4라도 `assignFixedKUnknown`의 N+1 clustering 또는 legacy/spherical seed 차이로 다른 파티션을 만든다는 문제 발견. legacy/spherical × Unknown on/off 4조합과 동일-K silhouette 선택까지 반증 | ❌ 전면 원복·미출시 | current auto mean DER **4.76%**. 기존 fixed+Unknown **7.56%(+2.80pt, 개선2/악화8, 최악 +19.02pt)**; plain legacy **7.58%(+2.82pt)**; spherical+Unknown **5.97%(+1.21pt)**; plain spherical **5.25%(+0.50pt)**. 최선인 legacy/spherical silhouette 선택+Unknown off도 mean **4.54%(−0.22pt)**였으나 **bwzyf 1.45→6.84%(+5.39pt)** 꼬리 회귀로 안전 게이트 실패. 평균 개선만으로 출하하지 않고 UI·Zig 모두 clean 원복. Quark atoms `enum__SpeakerCount`·`fn__assignFixedKUnknown`·`fn__kmeansFitMode`·`fn__silhouetteSimplified`; whisper.cpp 교차참조는 embedding diarization 없이 stereo turn 추정만 존재 | data |
| LIVE-CONFIRM | **2-window tentative speaker fold** — auto/no-anchor live에서 새 centroid는 내부 raw id로 즉시 학습하되 2번째 증거 전에는 가장 가까운 기존 **표시** 화자로 접어 새 ID 노출을 보류. 확인 즉시 과거 창을 기존 SPKFIX 계약으로 교정. Unknown(255) 보류안은 immediate DER +8.16pt라 역검증 REGR로 폐기; raw/visible id를 분리해 recluster continuity 오염 방지. 앱은 기존 `TranscriptStore.ingest(SPKFIX)`만 사용해 Swift 변경 0 | ✅ 라이브 체감 Win·최종 무회귀 | 3-4화자 패널 raw live: 표시 화자 **6/6/8→4/4/5**, 평균 **6.67→4.33(−35%)**, 최대 overcount **+4→+1**. immediate DER **19.74→19.38%(−0.36pt)**; final+OSD **11.36→11.36% 완전 동일**. mid-session SPKFIX 대상 창 **37→36**(churn 증가 0). 3-window fold는 화자 4/4/4지만 immediate DER +2.48pt라 기각. Quark atoms `fn__liveVisibleSpeaker`·`fn__diarAssign`·`fn__liveRecluster`; app cross-check `TranscriptStore.ingest`·`SpeakerID` | a50ac6c |
| DIAR-K4 | **조건부 spherical K=4 승격** — legacy K=1/K>=4는 완전 보존하고, legacy K=2/3에 한해 spherical 전수 sweep이 K=4를 독립 선택하며 silhouette≥0.50·절대분리≥0.50일 때만 후보 채택. 전면 spherical은 평균만 7.82→7.72였지만 41개 악화·최대 +30pt 꼬리라 역검증 REGR로 폐기 | ✅ 녹음 Win·라이브 무회귀 | **VoxConverse-dev 216 동일 바이너리 A/B**: mean DER **7.82→7.50%(−0.32pt, −4.1% 상대)**, median 3.51→3.45. 변경은 **7/216 전부 개선·악화 0**, gt=1 **1.84 완전 동일**. 4화자 버킷 **7.28→4.76%(−2.52pt)**; panel `migzj 49.05→17.77`, `gwtwd 18.06→4.52`, `txcok 14.78→1.55`. 라이브 3파일 immediate/final+OSD는 baseline과 동일(최종 K가 이미 4/3/4) → 다음 단계는 tentative speaker lifecycle. Quark atoms `fn__maybePromoteSpherical4`·`fn__kmeansFitMode`·`fn__liveRecluster`·`fn__diarizeEmb` | ea3f394 |
| MADI56-P3 | **adaptive cross-attn split + variable-frame mel** — short `enc_ctx`에서 split 8→4, 계산 mel frame을 encoder receptive field까지만 축소 | ❌ 측정 기각·원복 | ko1 5회 median decode: split4 **597.2 tok/s** vs split8 **599.8 tok/s**(−0.4%). jfk-1.5s mel: full **20ms** vs variable **21–23ms**(thread spawn/normalization 지배). 개선 없음 → 두 경로 모두 원복, 현 8-way/full-mel 유지. Quark atom `fn__decodeBlock`, `fn__melSpectrogram` |
| MADI56-P2 | **24GB+ encoder F16 weight cache** — Q8→F16 `[K][N]`을 process start에 1회 확장, 32-layer forward의 `kDeq` 6회/layer 우회; preview process는 강제 off | ✅ 구현·정확성 통과 | jfk 3회 median encoder **323→257ms(−20.4%)**, cache build **109–120ms**, transcript exact. 메모리 예약 ~1.25GB. main process에서만 `ENC_F16_CACHE=1`; 24GB+ app 자동 선택. Quark atom `fn__forward`·`fn__cacheWeights` |
| ENC-CEILING | **인코더(wall 50%) quark 미세감사 — "깎을 게 남았나" 확정** | ✅ 천장 확인 (구조+측정), headroom 0 | 질문: 인코더가 perf 최대 구간(62분 wall 50%)인데 커널레벨 여지 있나? quark regen 후 인코더 atom 전수 audit: GEMM=**우리 Metal 4 `matmul2d` 협조적 텐서연산**(하드웨어 매트릭스, 64×64 타일·`execution_simdgroups<4>`, `m4_gemm_nn/bias/bias_gelu` — bias·gelu epilogue를 타일 cache-hot 상태서 융합 → MPS 대비 풀 메모리패스 1회 제거), attention=**텐서op flash**(`m4_flash_enc`), norm=**융합 bias+residual+LN**(`bias_res_ln_f16`, LN 메모리패스 제거), 무음프레임 스킵(VAD, P2). **측정 확정(devops_ko)**: M4 텐서op **488ms** vs MPS(`ENC_M4=0`) **574ms** = **1.18× — 우리 커널이 Apple MPS를 이김**. 결론: 인코더는 Metal 4 텐서op 천장 — 하드웨어 매트릭스 API + 전 epilogue 융합 + MPS 추월. **구조적 headroom 0**(타일/​simdgroup 스윕은 canonical 64×64·4sg 대비 투기적, matmul2d가 하드웨어 매트릭스라 FLOP천장이 벽). 향후 인코더 최적화 재시도 금지. **남은 유일 비-마진 레버=로드(8%, dequant)** [[LOAD-mmap]]. quark atom `metal_kernel__m4_gemm_nn`·`m4_gemm_bias_gelu`·`m4_flash_enc`·`bias_res_ln_f16` | audit |
| BATCHDEC-M1 | **멀티청크 배치 디코드 in-engine 재통합(M1) — P5 배치 cross-attn 위에서** (BATCHDEC=1 게이트, 기본off 섀도우) | ✅ 텍스트 등가 + B=8 1.34× / ⚖️ 프로덕션 통합 보류(end-to-end ~타이) | P5(배치 cross-attn)로 J의 잔여원인(per-slot attn) 제거 후 **in-engine 섀도우 재통합**: 가중치 deq→WF16+tok_emb f16 1회, contiguous cross-KV, per-token(embed/pe per-slot→decodeBlockBatched→배치 LN+cvt+matmulF16Batched logit→per-slot suppress/filt/argmax→ragged EOT). **텍스트 등가 재확증**(devops_ko: batchdec B=4 `529 tok` == per-slot 149+144+113+123 = **529 정확 일치**, BPE 텍스트 동일). **속도(같은 실행 head-to-head)**: **B=4(기본 ENC_BATCH=4)=타이**(batched ~530 vs per-slot ~520 tok/s, +2%) / **B=8(ENC_BATCH=8)=1.34×**(균일 2배치 7.7분 — 체리피킹). **정정(62분/124청크 de-risk 실측)**: 스케일 15배치 평균 **~1.15×**(480~686; ragged EOT가 효율 깎음 — 한 배치서 10tok·149tok 슬롯이 가장 긴 것 끝날 때까지 같이 돌아 낭비). **62분 프로덕션 wall 113s=33×RT, 인코더 50%·디코드 23%** → B=8 절감 ~4.8s = **~4.3% end-to-end**(2hr ~10s 같은 ~4%). **순 ~4% 폴리시, 추월 아님.** **결론**: 디코드는 B=8서 whisper.cpp 약간 추월하나 end-to-end는 ~4% 폴리시. **M2(word-ts 배치 alignment 캡처)+M3(retire-to-seq) 프로덕션 재작성은 보류** — 헤드라인 end-to-end는 ~타이인데 전사경로(word-ts/rescue/seek) 회귀리스크 큼(ROI 부정). 스펙 "2×→13.7s 추월"은 순수 probe 2× 기준이었고 실파이프라인은 B=8-한정 1.34×→타이. quark atom `fn__decodeBlockBatched` | feat(decode) M1 |
| LOAD-mmap | **mmap 빠른로드(파일모드) — whisper.cpp 로드격차(~1.3s) 닫기 시도** | ❌ 반증 (측정) | Sf 로더를 파일모드서 pread→mmap zero-copy로(라이브는 RSS 위해 pread 유지). 측정: devops_ko wall **mmap 15.7s == pread 15.7s (차이 0)**. 가설(로드 I/O-bound) 반증: 모델 파일이 **page-cache-warm**이라 mmap·pread 둘 다 디스크 I/O 동일, ~1.3s 로드의 실체는 **Q8 dequant→GPU 복사 CPU 작업**(I/O 아님). mmap은 CPU dequant 안 줄임. whisper.cpp 로드 0.2s는 ggml이 **dequant 없는 load-ready 포맷**이라서 — 우리 격차는 근본적(로드시 dequant). revert(이득0+RSS만 +827MB). 교훈: 로드 격차는 I/O 아닌 텐서 처리. 진짜 닫으려면 load-ready 포맷 or GPU-side dequant(별건). PROF상 로드+IO 23% 중 모델로드는 ~1.3s(8%)뿐이라 ROI도 낮음 | — |
| MEL | **멜 스펙트로그램 twiddle 미리계산 + 멀티스레드 — 숨은 최대 병목** | ✅ commit (비트동일, ~2× end-to-end) | whisper.cpp head-to-head(devops_ko 7.7분) 중 **wall 갭 규명**: ours 33s vs whisper.cpp 14.5s인데 [perf](인코더+디코드) 합은 12.5s뿐 → **~20s가 [perf] 밖**. 추적: [enc] 배치마다 +7.3s 갭, 인코더 forward는 2s뿐 → 나머지 5s/배치 = **멜 계산**. mel.zig `rfft`가 **naive O(N²) DFT + 매 반복 @cos/@sin**(twiddle 미리계산 없음, 단일스레드) → **청크당 ~482M trig = ~1.3s/청크**(whisper.cpp mel 3.6ms!). 해결: ① twiddle factor(cos/sin) f64 테이블 1회 미리계산 → 매프레임 trig 제거(비트동일: f64=옛 @cos) ② 프레임 루프(3000, 완전독립) 멀티스레드(≤8코어). 결과: **devops_ko wall 33s→~16s(~2×)**, jfk 전사 비트동일, 2회 sha 동일(race-free 결정적). **whisper.cpp(14.5s)와 박빙으로 좁힘** — 인코드·디코드는 원래 동률이었고 갭의 전부가 멜이었음. 교훈: [perf]가 GPU만 재서 host CPU 병목(멜)이 가려졌다. quark atom `fn__rfftTw`·`fn__ensureTwiddles` | feat(mel) |
| P5 | **배치 cross-attention 커널 (J 미완 레버 구현)** — multichunk 디코드의 per-slot cross-attn B-loop을 1 dispatch로 | ✅ commit (커널·프리미티브 검증, 1.27× 배치블록) | **probe(test_p5probe)로 GO 판정**: 배치 디코드(B=8) 시간 분해 = self-attn **6.5%** / **cross-attn 43%**(4.18ms/step) / 나머지 50%. cross-attn은 KV 대역폭천장(245MB/step÷400GB/s=0.61ms)의 **6.8× off = occupancy-bound**(NH=20 threadgroup B개 별도 dispatch가 GPU 저활용). **`flash_cross_attn_f16kv_batched`**(grid B*NH, contiguous cross-KV [B][ENC_SEQ][D], tgid→slot b·head h)로 B개를 1 dispatch에. 결과: decodeBlockBatched **9.68→7.66ms/step = 1.27×**(cross-attn 4.18→2.16ms = **1.94×**). 정확성 PASS(test_decblock_batch·test_decloop_batch rel 7.6e-4). self-attn(6.5%)은 per-slot 유지. **J 부활의 토대** — multichunk 재통합 시 nb=8 win이 7%→실질로 커지고 크로스오버↓. (현재는 프리미티브만 — 라이브/파일 디코드는 per-slot decodeBlock 유지, multichunk 재통합이 다음 단계.) quark atom `metal_kernel__flash_cross_attn_f16kv_batched`·`fn__kCAbatched` | feat(decode) |
| AUTO-perf-recon | **자율주행 perf 표적 일괄 판정 — 대역폭 절감류 전부 반증(분석), P5만 생존** | ✅ 분석 판정 (구현 전 EV 컷) | 기존 측정(J-recon: logit GEMV **14%**·디코더 레이어 **86%**, 대역폭천장 180MB/tok의 **5.8× off = occupancy-bound**)으로 후보 일괄 판정: **P1 self-KV F32→F16** = self-attn은 occupancy-bound 86%의 작은 일부, 대역폭 비묶임 커널서 KV 반감 → marginal(PERF_LOG row3 "proj GEMV→f16 3.4× 느림" 동근원) → **반증, 미구현**. **P3 logit vocab prune** = logit 14%뿐 + 이미 batch 9.5× → ≤14% 천장, 약함. **P4 int4 공격적** = occupancy-bound에 대역폭 절감 → marginal. **인코더(P2)** = MPS GEMM 이미 "hand-MSL 불가"(Q8-3a 2.39× 느림)·과거 conv F16/dequant fuse 다 neutral/revert → 헤드룸 낮음, silence-skip은 seq 압축 필요(품질 위험). **결론: occupancy-bound 86% 레이어를 직격하는 P5(배치 attention)만 실 레버** — J가 가리킨 미완 커널. 교훈: 측정-우선이 4개 표적 중 3개를 구현 전 컷(점유-바운드 디코드에 대역폭 절감은 무효) | analysis |
| J | **멀티청크 배치 디코드 — 통합 완성·정확, 속도 반증** (PR#39 프리미티브·#41 decodeBlockBatched·#42 KV성장 검증, 통합 revert) | ✅ 정확성 검증 / ❌ 속도 반증 (측정) | **전체 end-to-end 통합 구현·검증**: BATCHDEC 경로가 nb 슬롯을 배치 디코드(cross-KV·seed·per-token 루프·per-slot argmax/EOT). **전사 품질-등가**(jfk3 2청크 byte-identical, devops_ko 한국어 775단어 동일). **그러나 속도 반증**: nb=2 배치 **704ms vs per-slot 576ms(22% 느림)**, nb=8서야 525 vs 492 tok/s(~7% 빠름), 크로스오버 ~nb=5-6. recon의 projection 6-9× 여유가 전체로 안 옮겨옴 — **attention이 per-slot B-loop(미배치, 디코더 비용 큰 부분)** + **F32↔F16 변환 오버헤드**(projection마다 cvt 2회) + cross-KV 셋업 오버헤드가 projection 이득을 상쇄. "default-ON은 좋을 때만" 제약상 부적격 → **통합 revert**. **진짜 레버=배치 attention 커널**(per-slot B-loop 제거, 상당한 Metal 작업). **유지**: 검증된 decodeBlockBatched(decoder.zig)·cvt_f32_f16·프리미티브/루프 테스트(미래 배치-attention 토대). 교훈: 부분(projection) 배치로는 부족 — 디코드 per-token 비용에서 attention+변환이 projection만큼 큼 | docs(J) |
| J-recon | **추측 디코딩 레버 정찰** — 디코드 병목·배치 verify 효율·draft 실현성 (test_specgemm.zig) | ✅ 정찰·**메커니즘 검증 / draft 반증 / redirect** | **레버 확정**: 강제160토큰 디코드 3.86ms/tok, logit GEMV 14%·디코더레이어 86%, 대역폭천장(~180MB/tok)의 **5.8× off = occupancy-bound**(단일토큰 GEMV가 GPU 저활용). **make-or-break(GEMM batch-scaling)**: FFN(1280×5120) M=1→8 토큰당 **387→64µs(6.0×)**, logit(1280×51866) **1227→129µs(9.5×)**, M=16서 22×/8.8× → 배치 forward가 6-9× 효율(점유 여유 확정). **그러나 draft가 full logit(토큰당 지배비용)을 못 피해 partial-depth self-draft는 ~break-even**(별도 draft 모델/Medusa 헤드=학습 필요, 범위 밖) → **순수 추측 디코딩 보류**. **Redirect: 멀티청크 배치 디코드** — 같은 occupancy 여유를 draft 없이, 긴 파일의 독립 청크들 token-t를 배치 forward로(바이트동일, 파일모드). test_specgemm.zig 보존 | docs(J-recon) |
| L | **diarization-aware 디코딩** — 화자 전환서 디코더 context 리셋(교차화자 누수 차단 의도) | ❌ 반증 (통제실험) | **FLEURS 두 발화(1662+1672, 다른화자) 연결 → 합동 vs 개별 디코드 CER 비교(정답 보유)**. 1차 합동이 B 통째 누락처럼 보였으나 **자가 적발: `concat:` 바이트연결이 이중 WAV 헤더 생성 → 엔진이 A 헤더만 읽음**(측정 버그, early-EOT 아님). `-f concat` 재인코딩(유효 22.4s WAV)으로 재측정 시 **A·B 둘 다 완전·정확 디코드**("…잊지 마라 원단이…" 경계 깨끗), A 부분은 개별 디코드와 동일=**교차화자 bleed 0**. 실제 연속 2화자(devops_ko 16분)도 전체 정상. **결론: diar-aware 디코딩은 실재 문제 미해결 — 엔진이 멀티화자를 이미 완전 디코드, 잔여 오류=confident-substitution(P3 외부지식 레버, G와 일치).** 틀린 결론 직전 측정 아티팩트를 잡은 것이 핵심 | docs(L) |
| H | **OSD-confidence 게이트 (P2b 후속)** — overlap 행 P(overlap) 임계 상향(file-mode diarizeEmb `OSD_THR` 0.25→0.45)으로 노이즈 클립의 저신뢰 false overlap 컷, 고신뢰 실제 overlap 보존 | ✅ commit·**측정된 순개선** | **VoxConverse-dev 216 매칭 A/B(같은 바이너리)**: OSD_THR 0.25 MEAN 8.03%(med 3.54) → **0.45 MEAN 7.82%(med 3.51) = −0.21pt**. **tucrg 356→315(−42pt)**, jiqvr −1.4·sqkup −1.0 개선. **5 개선 / 1만 >0.5 악화**(cwryz +0.8 — 실제 overlap 약간 손실, 수용가능). 라이브 emitOsdOverlap은 **0.25 유지**(L-3 ES2004a 튜닝값, 재측정 없이 불변 — 파일/라이브 default 분리). P2b가 "tucrg만 보고 OSD 끄기"를 막은 뒤, 임계 스윕으로 실제 게이트를 찾은 흐름. jfk 전사 불변 | docs(H) |
| G | **다중-forward / TTA 합의 신뢰도** — N회/섭동 디코드 불일치를 오류신호로(단일-forward conf의 confident-error 약점 직격 의도) | ❌ 반증 (측정) | **2단 측정**: (1) 자연 GPU 비결정 — FLEURS 1660 **5회 바이트동일**(다양성 0). (2) 입력 augmentation(속도±4%·게인·대역통과) — 실제 confident 오류 utts(1660 피이테·1667·1669·1672) **TTA 다양성 0%**(섭동 강건). 전체 12.9%는 대부분 속도섭동 **위치-시프트 정렬 아티팩트**(1705 80%=노이즈), 오류상관 신호 아님. **삼중 확인(CONF-2·P1·G): confident 오류는 비결정·섭동 모두에 강건 → 어떤 self-agreement도 못 잡음**(모델이 안정적으로 확신하며 틀림). 결론: 진짜 레버는 **외부 지식**(LM 재점수/큰모델/도메인 prior=P3 바이어싱)이지 self-agreement 아님. bench/tta_agreement.py 보존 | docs(G) |
| F | **스트리밍 부분가설 + 라이브 브릿지** (quark atoms: `fn evPartial`, seg-loop partial emit, `var g_bpe_cache`) — 디코드 8-batch마다 진행중 텍스트를 `partial` 이벤트로(opt-in PARTIALS=1). Node SSE 브릿지(web/bridge/server.mjs)가 엔진 stream 모드 spawn+EVENTS_FILE tail→SSE, 대시보드 EventSource 라이브 소스+▶라이브 토글+● 인식중 렌더. 부산물: **vocab 64MB 전역 캐시**(bpeDecode 매콜 재로드 제거 — 단어/부분 디코드 가속) | ✅ commit·검증 | jfk3 partial 11개(점진 성장 확인), **seg 텍스트 PARTIALS 유무 바이트동일**(부가적·무회귀), bench 경로(PARTIALS 미설정) jfk 전사 불변. **브릿지 SSE 실측**(60s 클립): meta/ready/38 partial/109 word/2 seg/flush_end 스트림. 대시보드 라이브 스크린샷: 전사·메트릭·perf(496 tok/s) 라이브 채워짐. **알려진 갭**: stream 모드는 diar/spk_seg 미emit(파일모드 diarizeEmb 전용)→라이브 화자패널은 후속(SPK 라벨 이벤트 배선 필요) | docs(F) |
| P2b | **OSD 중첩 행 순효과 A/B** (P2 진단 후속: "OSD가 노이즈 클립서 +124pt → 파일모드 비활성 후보") | ❌ 반증 — OSD 유지(현 기본값 최적) | **매칭 A/B(같은 diar 코드, VoxConverse-dev 216, OSD=1 vs 0)**: OSD-on **8.03%**(median 3.54) vs OSD-off **8.22%**(median 3.92) = **OSD-off가 +0.19pt 악화**. tucrg는 OSD-off로 -123.8 개선되나 **55파일 악화 vs 12 개선**(mevkw +16.0·vbjlx +12.9·nxgad +10.4 — 진짜 중첩 감지 손실). **결론: OSD 중첩은 순효과 +, 전역 비활성화는 55파일 회귀. tucrg OSD 페널티는 광범위 중첩 가치의 수용가능 비용.** 매칭 A/B가 "tucrg만 보고 OSD 끄는" 함정을 차단(55파일 회귀할 뻔). 향후: per-file OSD-confidence 게이트(tucrg 컷 + 55승 보존)는 임계 스윕 필요 — 보류 | docs(P2b) |
| P3 | **용어 바이어싱 (initial-prompt 컨디셔닝)** (quark atoms: `fn bpeEncode`, seed `STARTOFPREV` 프리픽스, `fn evSeg` bias_hits) — env PROMPT=도메인 어휘를 `<|startofprev|>`+토큰으로 시드 앞에 붙여 디코더를 고유명사/전문용어로 편향. 엔진에 없던 **BPE 인코더 신설**(greedy longest-match; vocab이 raw UTF-8 저장이라 한국어 직접 매칭) | ✅ commit·**실증+무회귀** | **FLEURS-ko #1660 ground-truth A/B**: baseline "괴테나 **피이테**, 슐레겔"(오인) → PROMPT="괴테 피히테 슐레겔" "괴테나 **피히테** 슐레겔"(교정), **CER 3.33%→1.67%(반감)**, 피히테 정확 False→True. **안전성 2종**: (1) PROMPT 미설정/빈값 → no-prompt 바이트동일(미사용 무회귀), (2) 무관 한국어 prompt를 영어 jfk에 → **환각 주입 0**(영어 전사 유지, 모델이 무관 context 무시). bias_hits 계약필드(세그먼트에 등장한 prompt 용어) + 대시보드 "적용된 용어 바이어싱" 칩 패널(devops_ko_biased: 데브옵스·제로트러스트 검출). 제품차별: 회의 전사 커스텀 용어집 | docs(P3) |
| P2 | **gt=3 DER 16.76% 버킷 진단** — gt=2(4.43%)의 3.8배 이상치로 보였으나 표적 재정의 | ✅ 진단 / ❌ 단일 수정 불가(2원인 분리) | **핵심: 평균(16.76%)이 오도 — median은 4.58%**(gt=2와 동급, 3화자 diar은 건강). 평균은 **tucrg(356%) 단독 지배**(제외 시 6.76%, 상위3 제외 5.48%). tucrg 분해: 26.2s 중 **ref speech 4.5s(17%)** — near-continuous 크로스토크 TV클립(Trump 토론)을 ref가 희소 annotate → 시스템이 실제 음성 9.6s 올바로 감지해도 232% 과방출, OSD 중첩이 +124pt(356%). **silero 게이트 시도→무효**(silero도 노이즈를 speech 판정, 8/17 동일) → revert(byte-clean). 나머지 꼬리(paibn 99%·jnivh 120%·uatlu 88%·rtvuw 114% speech)는 정반대 **dense overlap-heavy** = 진짜 3화자 중첩 클러스터링 오류(20-28%, 깊은 연구과제). **결론: gt=3는 systematic 문제 아님 — tucrg=희소-ref 아티팩트(수정시 215파일 회귀), dense-overlap 꼬리=별도 deep work 보류. OSD가 노이즈 클립서 +124pt(향후 OSD-confidence 게이팅 후보)** | docs(진단) |
| P1 | **temperature/logprob 폴백 디코딩** (quark atoms: `seg`-loop `pass_avg_lp` 트리거 + `fn evSeg` avg_logprob/fallback 필드) — 그리디 단일패스에 Whisper 표준 강건성 신호 추가. **측정 선행**(bench/logprob_probe.py, test-other worst/best 각 15): avg_logprob가 WER와 상관(best 평균 −0.07 vs worst −0.44)이나 표준 임계 −1.0은 worst 15개 중 **1개만** 포착 — 잔여 실패는 −0.15~−0.72 "확신하며 틀림"(CONF-2 재확인, 재디코드 불가). 유일 −1.0 초과 1건이 진짜 퇴화(REF "WELL"→HYP "Well, I'm going to", WER 400%). 결정: **full temperature-sampling 커널 기각**(WER 무버 아님 + 비용 큼), 대신 **avg_logprob<−1.0 → 기존 ts_mode rescue 트리거**(값싼 안전망, 새 커널 0) + avg_logprob/fallback **계약 필드 실값화** | ✅ commit·**무회귀 +미세개선** / ❌ temperature sampling 기각 | **깨끗한 A/B(같은 바이너리, LOGPROB_RESCUE −1.0 vs −99, test-other 300)**: rescue OFF **6.19%**(I=37) → ON **6.09%**(I=32) = **삽입 5개 제거, S/D 불변, WER −0.10pt**. **정확히 2/300 발화만 변경**(파탄 400%→0% 1건 + ZARATHUSTRA 오인 1건 중립), 오탐 0. 정상 반복음성 안전(jfk3 3× 진짜반복 avg_lp −0.024 ≫ −1.0 → 미발동, byte 영향 0). **핵심 반증: temperature 폴백은 이 벤치들서 WER 무버 아님** — 퇴화/무음환각은 기존 tokenCollapse+hallucination-guard가 이미 커버, 잔여는 confident-substitution. 부수익: avg_logprob/fallback이 출력계약+대시보드 품질신호로 실값화(예약 temp만 잔존). bench/logprob_probe.py 보존 |
| SOLO-1 | **솔로 화자 과분할 게이트** (quark atom: `fn__maxCentroidCosDist`, diarizeEmb+liveRecluster 양 경로) — silhouette는 상대지표((b−a)/max)라 단일화자가 발성변이로 고-silhouette 서브클러스터로 쪼개짐. **절대 분리 게이트**: 선택된 분할의 **max 쌍별 centroid 코사인거리 < DIAR_MIN_SEP(0.50)** → K=1 붕괴. file·live 동일 적용(보이스프린트 하한 전) | ✅ commit·**깨끗한 A/B 무회귀** | 동기: jfk3 단일화자가 K=2 과분할(silhouette 0.530>tau 0.35). 측정: 단일 max=0.326 vs 실멀티 전부 ≥0.727 → 0.50이 갭 한가운데. **동일 바이너리 A/B(게이트 OFF=DIAR_MIN_SEP −1 vs ON, VoxConverse-dev 216파일)**: gt=1 버킷 **4.93→1.84%(−3.09pt)**, **gt=2/3/4/5-6/7+ 전부 Δ+0.00pt 바이트동일**(멀티 0개 변경), MEAN 8.35→**8.03%(−0.31pt)**. 게이트 발동=정확히 5파일 전부 gt=1 단일화자(pqmho 39.72→6.56, hqyok 21.85→3.63), **실멀티 거짓붕괴 0건**. stale baseline(maxK=10)의 gt=3 "악화"는 config차(maxK 10→6)였음을 매칭 A/B로 확정. jfk3 K=2→K=1 검증, devops 2화자 K=2 불변. bench/gate_ab.py 보존 |
| 1 | conv1d_gelu F16 weights | ❌ revert | conv 23–47ms/call, no clear gain (weights cached across time axis → compute-bound, not bandwidth) | — |
| 2 | decoder cross-attn F16 K/V cache | ✅ commit (memory) | decode 120→123 tok/s (speed neutral, within noise); cross-KV cache 61→30 MB; correct | flash_cross_attn_f16kv + extract_ca_head_f16kv |
| 3 | decoder proj GEMVs → custom F16 GEMV (replace MPS M=1) | ❌ revert | decode 212→712ms (3.4× SLOWER); MPS M=1 GEMV is already well-optimized, naive 1-thread/col kernel far worse | — |
| 4 | logit_gemv F16 coalesced (warp/row + simd_sum) | ✅ commit | decode 118→128 tok/s (~8%); logit kernel 882→640us; correct | logit_gemv_f16_cg |
| 5 | flash_cross_attn phase1 warp-per-key (coalesced) | ❌ revert | decode 128→121 tok/s (slower); coalescing gained but parallelism dropped (256 threads→8 warps) for seq=1500 | — |
| 6 | conv1d → im2col + MPS F16 GEMM | ✅ commit | conv front-end ~95→22ms (~4×); total 3.22→3.15s; correct | im2col_f16, gelu_transpose, gelu_pos |
| 7 | MPS object caching (shape-keyed descriptors+op) | ✅ commit | decode 120→130 tok/s (~8%); encoder neutral; correct | backend mps_cache_get |
| 8 | decoder QKV 3→1 batched GEMM | ✅ commit | decode 130→134 tok/s (~3%); 8 fewer GEMM calls/token; correct | (stacked qkvw) |
| 9 | encoder all-layers single command buffer | ❌ revert | encoder 590→589ms (~0%); compute-bound, sync overhead negligible vs GPU work | — |

## Q8 quantization phase (memory + bandwidth)
| # | idea | result | metric | commit |
|---|------|--------|--------|--------|
| Q8-1 | embed_tokens → Q8_0 (int8 + per-32 fp16 scale); logit/emb lookups Q8 | ✅ commit | decode 134→140 tok/s; peak RSS 4.78→3.40GB; text exact | logit_gemv_q8, emb_lookup_indirect_q8, gpu_emb_lookup_q8 |
| Q8-2 | decoder proj GEMVs (q/k/v/o/cq/co/m0/m2) → Q8 warp-per-row GEMV | ✅ commit | decode 140→188 tok/s (~34%!); RSS 3.40→3.13GB; text exact. (iter3's custom GEMV lost only due to bad uncoalesced design; coalesced Q8 beats MPS M=1) | gemv_q8 |
| Q8-3a (de-risk) | direct custom Q8 tile GEMM (mul_mm_q8, 32×32 tile/4 simdgroups) vs MPS F16 @ encoder shape 1500×1280×1280 | ❌ data | correctness OK (max_abs_err 1e-3); **2.10ms vs MPS 0.88ms = 2.39x SLOWER**. compute-bound M=1500 ≠ decoder M=1 GEMV; vendor MPS GEMM unbeatable by hand-MSL here. ⇒ direct-Q8-GEMM rejected for encoder | mul_mm_q8 (test_q8gemm.zig) |
| Q8-3b | **encoder proj weights → Q8 (out-major) + JIT transposing dequant→F16 scratch + MPS F16 GEMM** (qkv/o/fc1/fc2, 32 layers) | ✅ commit (memory) | **peak RSS 3.13→2.54GB (−0.59GB!)**; encoder 587→627ms (+7%, dequant launches, acceptable: encoder is 1×/30s-chunk); decode unchanged 193 tok/s; jfk text **exact**. test_encoder/decoder green. Keeps MPS GEMM speed while dropping F16 weights 1.26GB→0.67GB | dequant_q8_f16 |
| Q8-3c | recover +7%: fuse q/k/v dequant 3→1 launch (dequant_q8_f16_qkv, 192→128 launches/forward) | ❌ revert | encoder 627→630ms (neutral, within noise). The +7% dequant cost is **bandwidth-bound** (F16 scratch round-trip ~1.9GB write + MPS read), NOT launch-bound — cutting 64 launches changed nothing (cf iter9: encoder sync overhead negligible). Irreducible w/ MPS: only fused dequant+GEMM avoids the round-trip, but that's the 2.39x-slower mul_mm_q8. +7% accepted as cost of memory win | — |
| Q8-4 | cross-attn K/V proj weights (ckw/cvw, 4 layers) → Q8 + JIT dequant→MPS (per-chunk cross-KV is M=1500, same as encoder; reuses dequant_q8_f16) | ✅ commit (memory) | clean A/B same thermal window: encoder/decode **identical** (~658ms/~188 tok/s — perf-neutral); peak RSS 2.543→2.531GB (−12MB, F16 cross-KV weights 26→14MB); jfk text exact; test_encoder/decoder green | deqW16 (reuses dequant_q8_f16) |

## Accuracy / quality
Gate (in addition to jfk exact): silence must produce EMPTY output.
Repro: `python3 -c "import wave;w=wave.open('/tmp/sil.wav','w');w.setnchannels(1);w.setsampwidth(2);w.setframerate(16000);w.writeframes(b'\x00\x00'*16000*30)"` then transcribe /tmp/sil.wav.
| # | issue | result | metric | commit |
|---|------|--------|--------|--------|
| ACC-1a (data) | silence hallucination: 30s digital silence → "you. You. You."; tried <|nospeech|>(50363) prob gate at SOT & first-pred positions | ❌ data | P(nospeech)≈0 for silence at BOTH positions (logit_ns even *lower* for silence than jfk) — large-v3-turbo doesn't fire nospeech on OOD digital/near-silence. Token-prob gate unreliable here | — |
| ACC-1b | **energy VAD**: skip a chunk whose loudest 1s-window RMS < 0.01 (speech≈0.14, silence/ambient≲0.001). Skips mel+encode+decode entirely | ✅ commit | silence30 & lownoise30 → **empty** (was hallucinated text); jfk **exact**; jfk3 both chunks full (chunk2's 3s speech kept via max-1s-window metric). Bonus: silent chunks now ~free | hasSpeech VAD |
| ACC-2a (data) | diarization on Whisper **encoder** features (old k=2/≤30s stub, and a new global variable-K AHC) | ❌ data | DER measured on AMI ES2004a (4-spk, md-eval.pl, collar 0.25): all clustering ≥90% ≈ single-spk baseline. Direct proof: intra-speaker cosine 0.006 ≈ inter 0.001 → **encoder output is speaker-invariant** (ASR discards speaker id). Analysis of CUDA reference: it clusters **mel** features, not encoder; final stage is 2-way Fiedler bisection | — |
| ACC-3 | **language auto-detection** (arg-max over language tokens 50259..50358 at the SOT-position logits; env WHISPER_LANG_ID override) + verified Korean | ✅ commit | jfk→en(50259), Korean DevOps 7m42s→ko(50264); fluent Korean across all 16 chunks incl @450s; word timestamps monotonic/aligned; 462s in 36.5s (~12.7× RT); 2-spk timeline alternates with the dialogue. Records in bench/MULTILINGUAL_TEST.md | lang-detect probe |
| ACC-2b | **mel-feature diarization**: pool RAW (unnormalized) log-mel per 1.5s segment → energy VAD → per-dim z-score → k-means (K = CLI arg / DIAR_K, default 2) → global RTTM. mel carries timbre/pitch (intra−inter separation **0.22** vs encoder 0.007) | ✅ commit | **AMI DER 90%→65% (K=2)**; K param exposed (K=4 runs, 76% on hard far-field AMI, better on clean audio); jfk text exact; runs globally on any length (old stub was ≤30s/≤2spk). Oracle ceiling 24% (1.5s seg) — SOTA would need a dedicated speaker-embedding model | melSpectrogramRaw, diarizeMel; bench/ DER harness |

| ACC-4 | **WIN: sovereign ResNet34 speaker-embedding diarization** — hand-ported wespeaker ResNet34 (Apache-2.0) to Zig (kaldi 80-fbank + conv2d via Accelerate sgemm + stats pool + FC), 256-d embeddings per 1.5s window → L2-norm + k-means(K) | ✅ commit | **AMI ES2004a K=4 DER 32.5%** (was 90% encoder / 67% mel; oracle 28.8%). Verified bit-for-bit vs onnxruntime (cosine 1.000000) at every stage. jfk text exact; silence→no spurious speakers. K = CLI/DIAR_K (default 2). No runtime dep (onnxruntime only offline). +~34s/17min for embeds (CPU), RSS +~0.1GB | diar_resnet.zig, bench/ |

## Diarization perf (quark-decomposed, measured)
quark atoms: `file__diar_resnet.zig/{fn__embed,conv2d,fbank,fft512,relu}`.
Per-stage profile (608 AMI embeds) revealed the bottleneck is NOT matmul FLOP:
| stage | share | | conv internal | share |
|---|---|---|---|---|
| stage1 (80×150, 32ch) | 40% | | **im2col** | **77%** |
| stage2 | 26% | | sgemm (Accelerate) | 23% |
| stage3 | 21% | | | |
| stage4 / fbank / pool+gemm | 13% | | | |
| # | idea | result | metric | commit |
|---|------|--------|--------|--------|
| DIAR-OPT1 | im2col: element-by-element strided copy + full @memset → @memcpy of contiguous valid spans (stride-1 path) | ✅ commit | **68→28 ms/embed (2.4×)**; AMI 17min total 100→83s; DER unchanged 32.49% (cosine 1.0 preserved); im2col 15.9s→7.6s | conv2d im2col |
| DIAR-OPT2 | multithread embeds across segments (std.Thread pool over a chunk's ~20 windows; Accelerate pinned to 1 thread/worker via VECLIB_MAXIMUM_THREADS=1 to avoid oversubscription) | ✅ commit | AMI 17min total 83→71s; diar embed ~2.4× (12 cores). NOT Ncore× — im2col is memory-bandwidth-bound, threads share bandwidth → sublinear. DER unchanged 32.49%; jfk exact; silence safe | DiarJob/diarWorker |
| (next) | F16 convs or MPS GPU (compute, not bandwidth) for true scaling; or fuse im2col into a direct conv to cut data movement | backlog | total now transcription-bound (diar ~7s of 71s) | — |

## Memory architecture
| # | idea | result | metric | commit |
|---|------|--------|--------|--------|
| MEM-1a (data) | reclaim mmap'd safetensors via madvise(MADV_DONTNEED, then FREE_REUSABLE) per-tensor after quantize | ❌ data | **no effect** on macOS: peak AND steady RSS flat at 2.53/2.40GB. macOS doesn't drop read-once clean file-backed pages from the resident set (DONTNEED is lazy/deactivate-only; FREE_REUSABLE is for anon malloc pages). ru_maxrss high-water never decreases. ⇒ madvise can't fix it | — |
| MEM-1b | **mmap → pread streaming loader**: never map the 1.6GB data section; pread each tensor into one reusable scratch buffer, consume, overwrite; free scratch+fd after load | ✅ commit (memory, **headline**) | **peak RSS 2.53→1.02GB (−1.5GB, −59%!)**; steady ~0.94GB; jfk text **exact**; encoder/decode unchanged (653ms/188 tok/s); load time unchanged (3.82 vs 3.90s, 2 chunks). Biggest single memory win — exceeds all Q8 work combined | Sf pread + g_rd scratch |

## Auto-K estimator study (2026-06-11, VoxConverse-dev offline)
Harness: `bench/k_study_vox.py` + `bench/k_sweep_vox.py` on `diar_embed_wav`
embedding dumps (215 files, gt speaker counts from ref RTTM). Goal: fix the
demo4 K=2 under-estimate (true 4) without breaking the rest.
| # | idea | result | metric |
|---|------|--------|--------|
| K-EST1 | eigengap (normalized-Laplacian, cosine affinity) | ❌ data | exact 27% vs sil 63% (51-file prelim) — under-estimates far-field badly |
| K-EST2 | BIC elbow (spherical k-means) | ❌ data | demo4 K=3, es K=6 — no better than sil anywhere |
| K-EST3 | raw-pairwise AHC (cosine threshold sweep 0.30-0.60) | ❌ data | far-field explodes (es: 81-469 clusters) — pairwise cosine too noisy without affinity refinement |
| K-EST4 | NME-SC (p-binarized affinity eigengap, simplified Park et al.) | ❌ data | exact 51% vs sil 63%; under-estimates K≥5 |
| K-EST5 | recursive 2-way split (split cluster when its sub-silhouette ≥ sub_tau) — fixes demo4 at sub_tau=0.5 (sub-sils: true-pair 0.55/0.59 vs noise 0.39-0.41) | ❌ data | VoxConverse over-split at EVERY sub_tau (0.45-0.80): best case = no-op, worst exact 50.6% vs 55.1% baseline. demo4's crisp TTS sub-structure is common in real single speakers |
| K-EST6 | silhouette tau bump 0.10→0.25-0.40 (K=1 gate) | 🔬 candidate | K1-recall 0→14% with 0 false positives (89-file prelim) — pending full-set + DER check |
Conclusion: shipped silhouette remains the best estimator measured; demo4
stays a `DIAR_K` hint case. Claimed-voiceprint count now feeds auto-K as a
lower bound in live reclusters (product safety net, no estimator change).

## Hybrid collapse-rescue decode (2026-06-11, quark 품질 후보 발굴)
quark atoms: `metal_kernel__ts_rules_indirect`, `fn__tokenCollapse`,
`fn__main` (seek loop). 교차 대조 트리: `whisper_cpp/quality/large-v3-turbo`
(신규 config, src/whisper.cpp 호스트 품질 로직 taxonomy).
| # | idea | result | metric |
|---|------|--------|--------|
| Q-1 | temperature fallback 사다리 (wcpp entropy/logprob) | ❌ 불필요 | wcpp greedy -nf 도 clova 붕괴 청크를 완벽 전사 — 구원자는 fallback이 아님 |
| Q-2 | **ts-토큰 디코딩이 루프 붕괴의 결정 변수** | ✅ 증명 | wcpp -nt 가 우리와 동일한 "Q. Q. Q." 붕괴 재현 (엔진 독립) |
| Q-3 | EOT 게이트 (유성 잔여 시 EOT 금지) | ❌ 역검증 | 모델이 junk 텍스트로 채움 (ko-2spk ". . . ~~") |
| Q-4 | 재인코드 없는 seek (forced initial ts ± startofprev 프롬프트) | ❌ 역검증 | 분포 밖 — 윈도 시작부 재전사 (jfk "and so," 중복) |
| Q-5 | 순수 ts-모드 상시 적용 | ❌ data | 코드스위치 음차("아키텍츄럴", wcpp도 동일) + 다른 11/99 청크 꼬리 붕괴 ("네."×22) |
| Q-6 | **하이브리드: plain 기본 + 토큰 주기성 감지 시 ts+재인코드 seek 재디코드** | ✅ commit | clova 5/99→0/99 (정확히 그 5청크만 구조), 픽스처 PASS, 클린 자산 텍스트 bit-identical, tok/s 495-511(=기준), 오발동 0 |

## Speech-validation study (2026-06-11, 후속: VAD 거짓 경보)
재현: tucrg(26s 중 발화 4.5s) DER 919%, pqmho(138s 중 14.9s) 149% — 거짓
경보가 채점 발화의 수 배. diar VAD가 상대 임계(>0.4×median)라 음악/잡음만
있어도 절반가량 통과.
| # | idea | result | metric |
|---|------|--------|--------|
| SV-1 | 절대 RMS 하한 | ❌ data | pqmho 음악 RMS p25=0.154 > ko-2spk 실발화 med 0.052 > ES2004a far-field med 0.0067 — 에너지로 음악/발화 분리 불가 |
| SV-2 | no_speech_prob (모델측, OpenAI 공식) | ❌ data | **large-v3-turbo에서 <|nospeech|>는 죽어있음**: 순수 음악(pqmho)과 실발화(jfk) 모두 P≈1e-11~1e-10. 디지털 침묵 한정이 아니라 전면적 (증류 캘리브레이션 소실 추정). SOT 프로브는 lang-detect와 공용으로 유지 |
| SV-3 | 단어 스팬 게이팅 (Whisper 자체 = VAD) | 🔬 측정 중 | tucrg 전사가 정확히 희소 발화만 검출 — RTTM을 단어 스팬 합집합으로 게이트 |
| SV-3 | 단어 스팬 게이팅 (Whisper=VAD) | ❌ 역검증 | 양방향 실패: 음악 위 환각 단어가 DTW로 퍼져 마스크 미축소(tucrg 25s/26s), 회의 중첩 발화 잘림(ES2004a 31.9→47.8% 역회귀) |
| SV-4 | 임베딩 발화-방향 분리 (ResNet 코사인) | ❌ data | tucrg 진짜 발화가 음악보다 낮음 (-0.04 vs +0.03) — far-field 전이 실패 |
| SV-5 | 2-8Hz 음절 변조 비율 | ❌ data | 보컬 음악이 음절 대역 변조 보유 (pqmho 음악 0.582 ≈ 발화 0.578) |
| SV-6 | **Silero-VAD v6 소버린 포팅** (vad_silero.zig, wcpp ggml 가중치 + CLI 교차검증 ±1프레임) | ✅ commit | 윈도 게이트(crms 0화) + 청크 스킵 + RTTM 서브윈도 클리핑. tucrg 919→230%, pqmho 149→78%, **ES2004a 31.85→26.44%(-5.4pt)**, demo4 24.18→24.67, 픽스처 PASS, clova 99/99 + rescue 5 정상. diar 스레드풀과 병행 실행으로 벽시계 비용 ~0 |

## K=3 / 7+ 버킷 돌파 (2026-06-11, 진단 주도)
md-eval 성분 분해(/tmp/dg 하네스)로 두 버킷의 실패 모드 분리:
| # | 발견/아이디어 | 결과 | 근거 |
|---|------|--------|--------|
| B-1 | K=3 버킷: FA≈0인데 **miss 12-27%** — Silero 커버리지는 95-99% (mevkw: floor 0.9% vs 실측 miss 23.4%) → 손실은 우리 게이팅 | ✅ 진단 | 상대 RMS 게이트가 조용한 발화 윈도를 버림 + 클리핑 pad 30ms 과소 |
| B-2 | **silero 권위화**: 윈도 keep을 {0,1} 이진화 (RMS 게이트 자연 퇴화) | ✅ commit | 12-파일 서브셋 22.7→19.5% |
| B-3 | **클리핑 pad 30→200ms, min_speech 250→60ms** (env: VAD_PAD_MS/VAD_MIN_SPEECH_MS) | ✅ commit | 서브셋 19.5→17.5% (pad 250 동률, 300 반전); ES2004a 26.44→**18.83%** + auto-K가 진실 K=4 적중(이전 5), demo4 24.18 완전 복원 |
| B-4 | 7+ 버킷: **confusion 20-41% 지배** — DIAR_MAXK=6 캡 (최악 10중 5파일이 K=6 포화) | ✅ 진단 | maxK 스윕: 7+ 서브셋 31.6→22.0(K8)→**18.0(K10)**→18.1(K12, 포화) |
| B-5 | maxK=10 부작용: **K=1 파일 악화** (hqyok 57→77%, sil이 캡까지 상승) + K=3 서브셋 +1.5pt | ⚠️ 트레이드 | 전수 벤치로 판정 (진행 중) |
| B-6 | K=2↔3 추정기 잔여: 정화된 임베딩에서 일부 자가수정(bravd 2→3), 잔여는 마진 ±0.03 양방향 한계 케이스 (paibn sil2=0.65 vs sil3=0.62; mevkw 역방향 과분할) | ❌ 보류 | 안전한 레버 없음 — 재귀분할 재기각 |
| B-7 | 벤치 인프라: 동시 full_bench 레이스로 결과 오염 사고 → **flock 단일 인스턴스 가드 + 런별 전용 rttm 디렉토리** | ✅ commit | 오염 jsonl 폐기 후 클린 재실행 |
| B-8 | **윈도-퍼-화자 K 하한** `maxK_eff=clamp(m/8,2,maxK)` + maxK 6→10 (파일 모드) | ✅ commit | 손상/수혜 파일이 m으로 완벽 분리 (K1 손상 m=14-26 vs 7+ 수혜 m≥79). 혼합 서브셋 K6 40.7/K10 35.8 → **16.65**. 전수 216: **12.37→8.67%** (med 4.13), 전 버킷 개선: K1 14.8→5.2, K2 7.2→5.0, K3 17.7→14.8, K4 9.4→8.5, K5-6 9.6→7.9, K7+ 16.9→10.2. **pyannote 3.1(≈11.2%) 추월**. ES2004a 18.83%(auto-K=4 정답), demo4 24.18, 픽스처 PASS |

## 라이브 페널티 + 중첩 발화 (2026-06-11)
새 하네스 `bench/live_der.py` (러너 동일 잡 구조 10s+3s 좌측컨텍스트, SPK/SPKFIX 채점):
| # | idea | result | metric |
|---|------|--------|--------|
| L-1 | 라이브 재측정 (현 바이너리): 스트리밍 44.65 / 저장본 37.54 vs 파일 18.83 — 갭 주범 = 파일만 받던 Silero 구간 클리핑 | ✅ 진단 | far-field 침묵이 1.5s 그리드 통짜 SPK로 FA화 |
| L-2 | **SPK/SPKFIX 소스 클리핑** (4번째 dur 필드, 러너 awk 하위호환) + 스트림에서도 g_vad_iv 누적 | ✅ commit | ES2004a 라이브: 스트리밍 44.65→**25.66%**, 저장본 37.54→**18.43%** (파일 18.83 동급). 픽스처 PASS, 파일 모드 18.83 불변 |
| O-1 | 중첩 정량화: ES2004a ref 중첩 추가화자시간 = 채점시간 14.7% = 단일라벨 miss 바닥. 우리 1화자 구간 miss는 7.2%뿐 — **miss의 본체가 중첩** | ✅ 진단 | 천장: ES2004a -13pt급 |
| O-2 | 중첩 탐지 #1: 센트로이드 모호도 cos2/cos1 | ❌ data | 재현 20%/오탐 6% (1.5s 풀링이 중첩 구조를 뭉갬) |
| O-3 | 중첩 탐지 #2: 화자 전환 인접 윈도 (±모호도 결합) | ❌ data | 최선 재현 35%/정밀 46% — 2차 화자 방출 손익분기 미달 |
| O-4 | 결론: 학습 OSD 필요 (pyannote segmentation급 포팅 = Silero급 이상 별도 프로젝트) | 📋 백로그 | 단일 라벨 구조 한계로 기록 |

## 학습 OSD 포팅: pyannote segmentation-3.0 (2026-06-11)
소버린 포팅 공식 재적용: sherpa-onnx ONNX → bench/convert_pyannote_seg.py →
osd_pyannote.zig (SincNet+BiLSTM×4+파워셋7), onnxruntime 심판 검증
max|Δlogp|=3.5e-5, argmax 불일치 0/589. Accelerate sgemm/sgemv로 413→90ms/10s.
| # | idea | result | metric |
|---|------|--------|--------|
| P-1 | 파워셋 argmax 중첩 검출 + 턴테이킹 prior 2차 화자 | ✅ 1차 | ES2004a 18.83→17.16% (miss 16.0→12.7) |
| P-2 | 확률 임계(P(ov)≥θ) 검출 | ✅ 미세 | θ 스윕 포화 ~17.1 |
| P-3 | 5s 슬라이딩+프레임 평균 집계 (pyannote식) | ❌ 무익 | 17.05 vs 17.12 — 디스조인트로 회귀 |
| P-4 | **로컬 트랙 정체성**: 파워셋 페어 + 로컬 솔로 프레임의 전역 투표 | ✅ commit | ES2004a **16.47%** (θ=0.25 포화; miss 12.5/fa 1.8/conf 2.2) |
| P-5 | OSD 행의 silero 클리핑 우회 → tucrg 232→462% 사고 | ✅ 수정 | emitOverlapRow가 g_vad_iv 교차로만 방출 + prim≥0 가드 (1차 화자 위에만 2차) |
| P-6 | 군중 가드 (중첩 런 ≥3s 차단) | ❌ 역검증 | tucrg 불변(런이 원래 짧음), ES만 16.49→16.92 손상 — 롤백 |
| P-7 | tucrg 잔차 (229.7→356%) | 📋 수용 | 군중 함성 = 진짜 다성인데 ref 미라벨 — 알려진 병리 파일 |

## Metal-4 tensor-ops 인코더 — Phase 1 (2026-06-11)
quark atoms: `metal_kernel__m4_gemm_nn`, `metal_kernel__m4_gemm_bias`,
`metal_kernel__m4_gemm_bias_gelu`, `fn__forward`(encoder dual-path).
toolchain: Xcode 26.5, -std=metal4.0 (m4_*.metal만), MPP tensor_ops.
| # | idea | result | metric |
|---|------|--------|--------|
| M4-1 | 셰이더 내 raw 포인터 tensor 뷰 (런타임 MTLTensor 불필요) | ✅ 검증 | **비-const 필수** (mpp 헤더에 const 오버로드 없음 — "Unsupported type" 정적 단언의 정체) |
| M4-2 | 순수 tensor-ops GEMM vs MPS (fc1 형상 1500×5120×1280, 64×64타일/4SG/동적K) | ✅ data | **3.64ms vs 3.83ms (1.05×)**, max|Δ|=0 — 미튜닝으로 MPS 동급+ |
| M4-3 | mode::multiply가 기본(덮어쓰기) — zero-init 패스 불필요 | ✅ 확인 | descriptor 7번째 인자 |
| M4-4 | **융합 에필로그**: GEMM+bias, GEMM+bias+erf-GELU (타일 cache-hot 상태서 적용; bias_add_f16/gelu_f16 전체 패스 제거) | ✅ commit | run()은 lvalue 슬라이스 요구 |
| M4-5 | 인코더 6 GEMM 전부 교체 (ENC_M4=0 = MPS 폴백 이중 경로) | ✅ commit | **배치4 569→511ms(-10.1%), 배치1 647→588ms**; jfk 단어·스팬 바이트 동일; wcpp 571ms 최초 추월 |
| M4-roadmap | Phase 2: Q8 직행 GEMM (half×int8 네이티브 — dequant 77ms+가중치 트래픽 ½ 제거, k-loop+coop tensor로 블록 스케일), Phase 3: flash_attention_enc tensor-ops 재작성 (160ms), int4 경로 (모델 Q4 시) | 📋 | 지원표: half×int8→half/float, int4b_format까지 1급 |
| M4-6 | **Q8 직행 GEMM (Phase 2)** — A: 32-K coop 스케일 패스, B: TG 타일 dequant(tilek 128/256) | ❌ 기각 (M4급) | A 0.57× / B(128) 0.84× / B(256) 0.56× vs dequant+f16GEMM 4.3ms. 원인: 열당 24 TG가 동일 가중치 타일 중복 dequant — legacy의 레이어 단위 1회 dequant+SLC 상주 왕복이 사실상 최적. 정확성은 검증(max|Δ|=0.0005). **M5 GPU neural accelerator(네이티브 int8 matmul)에서 뒤집힐 후보** — 커널·하네스 보존 |
| W-1 | **WER 표준 벤치 — 절대 ASR 품질 (최종)** (resident STREAM 잡피드 + 공식 Whisper 정규화기 + jiwer, 영구 bench/wer_runs/) | ✅ **확정** | **test-clean WER 2.17%** (2620발화/53k단어), **test-other WER 4.19%** (2939발화/52.9k단어), **FLEURS-ko CER 3.99% / WER(공백토큰) 13.32%** (382발화, 빈hyp 0). 공표 large-v3(clean 2.0 / other 3.9) 사실상 동급 — **Q8 양자화 + 자체 Metal 파이프라인이 레퍼런스급 절대 품질**임을 입증. 한국어 CER 3.99%는 강력(read-speech). 게이트-프리(VAD_THRESH=0)+피크정규화(-1dBFS) 측정 |
| W-2 | **발화 게이트의 절대 진폭 민감성** — FLEURS-ko(피크 −33 dBFS)에서 222/382 청크 무음 스킵 (에너지 게이트 + silero 둘 다 저게인서 침묵) | ✅ 진단 | 제품 함의: 저게인 마이크면 제품 전체 침묵 가능. +20dB 부스트 → 완벽 전사로 진폭 원인 확정 |
| W-2b | **AGC 구현 + 자기수정** (transcribe.zig) — 1차: 게이트 전용(인코더 불변) → **CER 5.54%/5빈값으로 역검증 실패**. 진단: 빈값 5파일 peak 0.11~0.23(중간게인)인데도 빈전사 = **인코더도 저게인서 실패** (앞선 3.99%는 외부 부스트값이었지 raw 아님 — 전제 정정). 2차: peak<0.30일 때만 타깃 0.9(−1dBFS)로 **in-place 정규화(게이트+인코더 동시)**, 정상(peak≥0.30) 불변. AGC=0 롤백 | ✅ commit | **제품경로 FLEURS-ko 0빈값 CER 4.05%**(외부정규화 3.99%와 동일, 핵 없이). 회귀: jfk **바이트동일**, ES2004a DER **16.49→15.57 개선**(조용한 발화 4개 복구, 무음청크 40× 부스트해도 게이트가 헛전사 차단). devops 타임스탬프 흔들림은 AGC 무관(AGC=0 2회도 다름=기존 비결정성, 백로그). 벤치는 제품경로로 단순화(게이트우회·외부정규화 제거) |
| CONF-2 | **신뢰도 메트릭 A/B + 임계 정밀화** — prob vs margin(top1−top2) vs entropy, FLEURS 정답 단어별 align(jiwer) precision/recall | ✅ data·결정 | 동기: Speaker0(명료)에서도 과다플래그. **세 메트릭 측정상 등가**(최고 F1≈0.41, precision 0.33-0.44, 하위15% 플래그) — 단일-forward 신뢰도는 **확신하며 틀린 오류를 못 잡는** 본질 한계. margin/entropy 가설 반증. 결정: **prob 유지(margin/entropy 디코드비용만↑ → 커널 prob-only 복원, 토큰 바이트동일), 임계 0.65→0.55**(플래그 ~20%→~7%, precision 0.44→0.50, 과다플래그 완화). 포지셔닝: 약한 검토 힌트. conf_eval.py 보존. 별건: 솔로 화자 과분할(Speaker 0/1/2) 백로그 |
| CONF-1 | **단어별 신뢰도 (기능+품질 Super Win)** (quark atom: `metal_kernel__argmax_conf`) — argmax에 logsumexp pass 추가 → 토큰 softmax 확률, 단어=토큰 min | ✅ commit·바이트동일·**Super Win 검증** | 토큰 경로 바이트동일(같은 argmax). 거의 공짜(logits 207KB 재읽기 vs gemv 66MB). **검증: 저신뢰가 실오류와 상관** — FLEURS "섹스는"0.36(정답 색스/Sacks), "빛공예가"0.54(정답 빛공해), 외래지명 비슈케크0.38·가지안테프0.45 정당불확실; 정답단어 0.94-1.00로 명확분리. 장식 아닌 품질신호. CONF=1 덤프(기본 출력 불변). 다음: 러너/앱 색상·밑줄 렌더 |
| FUSE-2 | **디코드 융합 #2: qkv gemv+bias×2+store×2 → 1** (quark atom: `metal_kernel__gemv_q8_qkv`) + **가설 반증** | ✅ commit·바이트동일 | self-attn qkv를 단일커널로(q=acc+qb, k=acc→캐시, v=acc+vb→캐시). FUSE-1+2 누적 **24런치/토큰 제거(80→56)**. **jfk 원본 바이트동일**. 깨끗한 A/B(같은 머신): gpu-sync 281→268ms = **~4.6% 디코드 가속**. **핵심 발견: 24런치 제거가 4.6%뿐 → 런치 레이턴시 ~95%는 GPU 파이프라이닝으로 이미 숨겨져 있었다.** "launch-bound→1.85×" 가설 반증 — 디코드는 **GEMV/어텐션 대역폭-bound**. tiny-커널 융합은 체감↓. 진짜 디코드 레버는 GEMV 대역폭(in-memory sub-Q8=int4, 제로리스크로 보류) |
| FUSE-1 | **디코드 융합 #1: MLP gelu/residual을 gemv 에필로그로** (quark atom: `metal_kernel__gemv_q8_bias_gelu`/`_res`) — decode dispatch-bound 근본 표적 | ✅ commit·바이트동일 | 측정 선행: DBATCH 8vs1 동일(276/268ms)=**CPU sync 아닌 GPU 런치레이턴시** 병목, 토큰당 ~80 tiny 커널. 단일-threadgroup persistent는 기각(한 코어가 273GB/s 못채움 ~3.6× 느림 + Metal grid-barrier 없음) → 정답은 **풀-그리드 유지 융합**. 1라운드: kGelu+kRes 폴딩 → 레이어당 2커널↓ × 4 = **8런치/토큰↓**. **jfk baseline 바이트동일**(결정적 경로, gelu 공식 정확복제+residual 동일산술). 속도는 단일융합(8/80=6.5%)이 노이즈 이내 → 집계는 풀셋(80→~25) 후 조용한 머신서. 다음: bias×2+store×2 폴딩 |
| BC-1 | **flash_cross_attn_f16kv 뱅크충돌 (quark `_axon/bank_conflict_suspect/...s_part`)** — 21% 디코더 커널 표적 | ❌ false-positive (분석 역검증) | s_part 읽기 `[od]+[64+od]+[128+od]+[192+od]`에서 64/128/192=32배수 → simdgroup이 뱅크 0-31 정확매핑, **충돌 구조적 불가**. 게다가 s_part 1KB = 커널 30MB/token(cross-KV 재읽기) 트래픽의 0.003%. 실측 A/B는 디코드 ±40% 머신노이즈로 불가 → 분석 판정. **커널 21%는 뱅크충돌 아닌 KV 대역폭(어텐션 본질)**. quark suspect는 "32정렬 파티션 리덕션" 흔한 오탐 패턴. 코드변경 없음(non-conflict 수정은 churn). 진짜 디코드 레버는 dispatch 융합(P0 dispatch-bound) |
| INT4-2 | **Q8 파일 출하 (제로리스크 다운로드 1.86×)** — quantize_q8.py가 F16 safetensors→Q8(233 큰행렬을 K.qs/K.scales, 나머지 F16 복사), 엔진 로더가 K.qs 존재 시 직접 로드(readQ8, 4 로더 분기) | ✅ commit·**비트동일 증명** | 1618MB→867MB. **무위험 증명(가중치 레벨)**: WHASH 해시(tok_emb+dec qkv+dec ow+enc o_w+enc qkv, 4 로더 전부) F16==Q8파일 일치 . 전사 바이트동일은 불가(엔진 GPU FP가 모델무관 run-to-run 비결정 — F16 2회도 단어 흔들림 실측) → 증명은 가중치로. jfk 파일모드 전사도 동일. Python 양자화가 Zig quantInto와 바이트일치(round-away+f16스케일) |
| INT4-1 | **양자화 비트폭 품질 곡선** — q4r 프로브(Q8 양자화 직전 N-bit 라운드, 디폴트 off=무위험), WER 하네스로 측정 | ✅ data | **test-other 300-subset, 같은 발화, 검증 바이너리(md5 distinct)**: Q8 6.19% / Q6 6.21%(+0.02) / Q5 6.45%(+0.26) / Q4 6.51%(+0.32). test-clean·FLEURS-ko는 전 비트폭 ~0. 무릎=Q6. 다운로드(근사): Q8 1.9× / Q6 2.4× / Q4 3.5×. **교훈: 빌드 u5/u6 시프트 에러를 `rg error\|head` 필터가 먹어 구 바이너리로 Q5/Q6를 측정(전부 Q4) → md5동일 적신호로 적발. 이후 "✅ Built+mtime 갱신" 확인 규칙.** 출하 결정: **Q8(제로리스크 — 엔진이 이미 Q8 in-memory, 품질변화 0 + 다운로드 1.9×)** |
| T-1 | **라이브 번역 (Whisper translate 토큰)** — SEED 태스크 토큰 transcribe(50360)↔translate(50359) 런타임 분기(TRANSLATE=1), 파일/스트림/seek 전 경로 | ✅ 배선 / ❌ turbo 불가 | 토큰 배선 정확·무해(기본 transcribe 회귀 클린). **그러나 large-v3-turbo는 번역 못 함** — 지피지기: wcpp turbo `--translate`도 KO 입력에 KO 출력(동일). OpenAI가 turbo 파인튜닝서 번역 데이터 제외(공표). 배선은 번역가능 모델(full v3 / 텍스트 MT) 물리면 즉시 작동 — **미래 대비 보존**. 번역 기능은 보류 |
| W-3 | **엔진 wav 파서 강건화** (mel.zig wavFmt/loadWavChunk + transcribe.zig 가드) | ✅ commit | **float32(tag 3/0xFFFE, 32bit) 네이티브 지원** — FLEURS 원본 무변환 전사 확인. 빈/미지원 파일은 **segfault→명시 스킵**("empty or truncated"/"unsupported WAV format", stream은 빈 SEG_END 후 continue). 회귀: jfk PCM16 경로 산술 불변, devops 멀티청크 정상. 4케이스(PCM16/빈/float32/8bit) 검증 |
| M4-7 | **flash attention tensor-ops 재작성 (Phase 3)** — 64-q 타일(기존 32), strided device tensor 뷰로 Q/K/V 제자리 소비(TG 스테이징 0), QK^T/P·V matmul2d + coop 누산, S는 coop→TG f32 | ✅ commit | 격리 5.19→**3.84ms (1.25×)**, max|Δ|=6e-5 bad=0; 인코더 배치4 494→**462ms (flash 단독 -32ms)**, 배치1 570→545ms; MPS 대비 전체 M4 스택 548→462 = **-15.7%**; devops_ko/jfk 전사·단어 바이트 동일, KO+EN 픽스처 PASS. ENC_M4F=0 = flash 단독 롤백 |
| M4-8 | 진단: 1-thread/row 소프트맥스가 병목 (스테이지 프로브: QK만 1.65ms / +scatter 1.80 / +softmax 4.61 / full 6.35) | ✅ data | 2-thread/row + float4 벡터화로 6.31→3.84ms. 교훈: tensor-ops 도입 시 matmul 바닥(1.65×2)이 빨라져 비-matmul 직렬 구간이 즉시 지배 |
| M4-9 | strided tensor 생성자 발견: tensor(ptr, dextents, array<int,2>{1, row_stride}) — mpp matmul2d와 호환 | ✅ 검증 | TG 복사 불필요 → TG 25KB로 한도(32KB) 통과; 단 64-타일 꼬리가 seq 너머를 읽으므로 버퍼 +64행 패딩 필수 (qkv 스크래치) |
| L-3 | **라이브 경로 OSD** (스트림 기본 ON, FLUSH에서 SPKOV 행 — relabel 윈도 기반 로컬트랙 정체성 + silero 클리핑) + 러너 끼어들기 마커 "⟨+Speaker N 겹침⟩" | ✅ commit | ES2004a 라이브 저장본 18.43→**17.41%** (116 겹침 행); 지연 +26ms/세그먼트(+2.4%, 2분 리플레이 A/B); ES 2분 양성대조 .md 마커 2건 확인; ko-2spk(무중첩) 마커 0 ✓; 픽스처 PASS |

## 라이브 지연 + 디코드 대역폭 실험 (2026-07-02)
E2E 목표: 발화→화면 지연 단축. quark 재독 + whisper.cpp 교차참조 주도.
| # | idea | result | metric |
|---|------|--------|--------|
| E1 | **AUDIO_CTX 가변 인코더 컨텍스트** (whisper.cpp exp_n_audio_ctx / stream `-ac` 패턴) — encoder.forward(+seq), decodeBlock(+enc_ctx), ca_accumulate(+ca_stride 분리), env AUDIO_CTX off/auto/N. auto = 오디오 프레임 +224(4.48s) EOT 마진 ×64 라운드. 마진 스윕(ko2 768프레임): +64→반복×9, +128→×3, +160→클린 | ✅ commit | jfk 인코더 566→**321ms(−43%)** 텍스트 동일; FLEURS-ko 382발화 STREAM 제품경로 **CER 4.05→4.14%(+0.09pt)**, 엔진 패스 462→**205s(2.25×)**; off는 jfk 바이트동일. 라이브 10s 세그 인코더 ~545→~250-300ms |
| E1b | **문장단위 repeat-loop 가드** — tokenCollapse p 9..48 확장(run≥2p=3회 반복 시 ts-rescue). STATUS 백로그 'repeat-loop hallucination' 그물 | ✅ commit | FLEURS 1728 스트림 반복(문장×12, p≤8 우회)이 CER +2.6pt 전체를 지배 → 가드 후 4.14%. jfk/ko 픽스처 불변, off 경로 바이트동일 |
| D1 | **인메모리 int4 디코드 가중치** (repackQ4: Q8→니블 ±7 per-32 f16 스케일; gemv_q4 패밀리 + logit/emb q4 쌍둥이; DEC_INT4=1 블록만/2 +헤드) — FUSE-2가 지목한 "GEMV 대역폭" 레버 | ❌ **반증 (기본 OFF 프로브 보존)** | ko1 106tok, AUDIO_CTX=auto: Q8 532 / int4블록 535 / int4+헤드 523 tok/s = **0% (헤드는 −2%)**. GEMV 바이트 ½이 무효 → M=1 GEMV는 대역폭 아닌 **메모리 레이턴시/점유율-bound** (N=1280 → 160 TG 소그리드). FUSE-2의 "대역폭-bound" 결론을 커널 레벨에서 재반증. 산술 검증: jfk 텍스트 정확(26tok, 1패스), repack 60-105ms/로드. M5 네이티브 int4 matmul에서 재평가 후보(M4-6 동형) |
| D2 | cross-KV F16→Q8 (계획) | ❌ 분석 기각 | flash_cross_attn_f16kv 실효 대역폭: actx1408 기준 ~7MB/134µs ≈ **52GB/s ≪ 피크 273** — 커널이 이미 대역폭 미포화(NH=20 TG 점유율-bound). KV 바이트 ½은 D1과 같은 물리로 무효 예측. 진짜 레버 = seqlen 분할로 TG 수 증대(어텐션 점유율 재작성) — 백로그 |
| APP | 앱 상수: interim 디바운스 600→300ms, 스크롤 애니 250→50ms, firstSegment 3→1.5s, preview 1.5→1.0s, 라이브 spawn AUDIO_CTX=auto | ✅ commit | 첫 커밋 텍스트 ~3.5→~1.8s, 임시 자막 ~3.0-4.0→~2.0-2.5s (분석치; swift test 240 PASS) |
| FIX | wer_bench 하네스: validateStreamWavPath 가드(6월말 도입)가 scratch(bench/wer_runs/) 경로를 거부 → 전 발화 빈 hyp | ✅ 진단 | STREAM_WAV_ROOTS=<scratch> 지정으로 복구. 가드 도입 후 하네스 미실행이었음 — 재발 방지: 러너에 env 내장 필요(백로그) |
| REV | **적대 역검증 워크플로** (85 에이전트: 4렌즈 리뷰 → 발견별 3-스켑틱 반박 투표) | ✅ **19건 확정·전수 수정** | HIGH 7: 부재 앵커 kmin 과분할, 앵커 출생봉쇄 회귀(recl_done), W2 재시작→installTap 크래시+didTerminate 세션사망, mid RELABEL이 rebuildLive로 증발(→화자 교정을 오버레이로 재설계), W1 재시도가 워터마크에 폐기+갭 은폐(→백로그 시 즉시 갭). MED: reconcile mid/final 스냅샷 경합(pendingFinal), 워치독 큐대기→하트비트 기준, 재시작 후 FLUSH 라벨이 앞구간 파괴(namespace 플래그), .flushing 미커버(강제 마감), 유령 앵커(second-best 폴백+유령 흡수), 이웃 창 containment 오적용(근접 매칭). 수정 후 재게이트: DER 13.91/13.77/12.95 유지, 앵커 8/8, jfk 정상 |
| NRNG | **windowed no_repeat_ngram** (quark atoms: `kernels_graph_helpers.metal` logit_filter_indirect + ts_rules_indirect, `transcribe.zig` kFilt) — 디코드 중 반복 억제. 기존 anti-loop은 단일토큰 ×3(period-1)만 밴 → 멀티토큰 구 반복("A B A B", "단어X Y X Y")과 짧은 n-gram 반복 통과, GREEDY argmax가 반복 문맥에 구조적 취약. no_repeat_ngram(n=3): 직전 (n-1)-gram이 **최근 NRNG_WIN=16 토큰 내** 등장하면 그걸 완성하는 토큰 로짓 −inf (HF no_repeat_ngram_size / fairseq NGramRepeatBlock). env NO_REPEAT_NGRAM(기본 3, 0=off 바이트동일). 5각도 진단(29 에이전트)+실측 게이트 | ✅ commit·**측정 게이트 통과** | **핵심: 실측이 진단 교정.** naive 전역 no_repeat_ngram(n=3)은 **jfk3(정확한 3회 반복 합성) 2/3번째 문장 파괴**(회귀). → **window=16 도입**: hallucination은 인접 반복(period 1-4, window 내)이고 진짜 문장 반복은 >16토큰 간격이라, **jfk3 3회 온전 보존 + jfk 1회 무손실(OFF/n=3 텍스트 완전 동일)** 동시 달성. 긴 문장 반복은 tokenCollapse 담당(역할 분리). temperature fallback 재제안 안 함(P1 이미 실측 기각). test_decoder GPU=CPU |
