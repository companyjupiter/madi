# 고려 기능 백로그 (Madi)

즉시 구현하지 않고 **고려만 해 둔** 기능들. 우선순위가 오거나 선행 조건이 충족되면 착수한다.
(구현/검증 파이프라인: `.claude/workflows/madi-feature-pipeline.mjs` — `Workflow({scriptPath: …})`)

---

## 온디바이스 TTS (텍스트 → 음성)  ⟦고려중⟧

전사/번역/요약은 만들었지만 **글자를 소리로** 읽어주는 기능은 없다. 현재의 오디오 재생
(click-to-play, listen-review)은 전부 *원본 녹음* 재생이지 음성 합성이 아니다.

- **방법**: macOS `AVSpeechSynthesizer` — 온디바이스·무료·다국어(KO/EN/JA/ZH). 클라우드 0,
  sovereign 원칙과 정합.
- **붙일 후보**:
  - 번역 자막 읽어주기 — 외국어 회의의 번역문을 귀로 듣기 (자막 오버레이/줄 옆 재생).
  - 요약 읽어주기 — A.I 요약을 오디오로 (이동 중 듣기).
  - 전사문 읽어주기 — 회의록 전체 오디오.
- **노트**: 줄→오디오 재생(click-to-play)과 UI 패턴을 공유 가능. 언어 선택은 translateTargets/
  source 언어에서 유추.
- 추가: 2026-06-24 (사용자 요청으로 백로그 보류).

---

## 라이브 녹음 재생 (click-to-play v2)  ⟦선행 필요⟧

지금 click-to-play는 **파일 전사 세션**만 지원(원본 파일이 디스크에 있고 타임라인 일치).
라이브 녹음도 줄→오디오 재생하려면 연속 오디오 보존이 필요.

- **선행**: 세션 오디오 보존(AudioCapture의 세그먼트는 temp+overlap → 연속 WAV로 적재) +
  저장공간 관리 정책(어디에·언제까지 보존, 큰 WAV 정리).
- 시작 시 그 보존 정책부터 결정.

---

## 번역 품질 레버 (DNA3 4B → 9B)  ⟦트레이드오프 평가 필요⟧

`Engine/TranslateEngine.swift` 주석의 deferred A/B. 현 4B 모델은 in-target anchor로 KO→KO
재구성을 막고 있으나, 더 큰 9B 모델이 번역 품질을 더 올릴 수 있음.

- **트레이드오프**: 메모리(↑)·속도(↓) vs 품질(↑). ≥16GB 게이트 / 모델 다운로드 정책과 함께 평가.

## ~~P10~~ ✅ 전항목 구현 완료 2026-08-10 (PERF_LOG P10 배치 참조; 잔여 = 실기 TTFT 실측, 태그 페이드는 시각검증 필요로 보류) — 반응속도(TTFT) × 구조 churn 배치

실기 판정 3건이 근거: ① 캡션 NE 0.00 재현 ② **인터림 기아**(세션당 턴 4회·캡션 갱신 1회 —
커밋 레인 점유+단일 인플라이트로 스케줄 차단) ③ **체감 반응속도 악화** — P8(소스 합의 +1 프리뷰
주기) + P9(표시 합의 +1 MT턴)의 지연 비용이 기아와 결합해 "더 느려졌다"로 체감됨. 패널 NE 0.29(>0.2)
+ "행 문장분리·화자분리 한꺼번에/몰렸다 터짐" percept.

**원칙: 이후 모든 번역 배치는 NE와 TTFT를 동시 기록 — 한 축의 승리가 다른 축을 퇴행시키는지
매번 PERF_LOG에 남긴다 (매 턴 첫 TTFT 의무).**

- **P10-0 TTFT 계측 (선행, 다른 항목의 게이트)**: `TranslationStabilityMetrics`에 지연 축 추가 —
  (a) **매 번역 턴: 스케줄→첫 표시 프레임 TTFT** (b) 윈도 오픈(livePartial 첫 글자)→캡션 첫 페인트
  (c) 커밋 라인 생성→그 라인 첫 번역 표시. summary 라인에 median/p95, PERF_LOG 기록 의무.
- **P10-1 인터림 기아 해소**: 커밋 backlog 중에도 N초(기본 3s)당 인터림 1턴 보장(우선순위 예외 슬롯).
  실측 기아가 TTFT 지배항 — 최대 레버.
- **P10-2 첫 페인트 fast-path**: 윈도 첫 턴은 합의 대기 없이 표출(소스=첫 가설, 표시=첫 MT 결과)
  후 합의 모드 전환 — "빈 캡션 3~4초"의 0→1 문제 제거, 목표 첫 페인트 ≤2s. 첫 1턴만 안정성 예외
  (이후 append-only 유지). NE 소폭 상승 허용 범위: 캡션 NE ≤ 0.2 밴드 내.
- **P10-3 split-once 문장분리 + 번역·ID 승계**: 행 분할을 재그룹마다 반복하지 않고 한 번 확정되면
  유지, 분할 시 기존 번역/라인 ID 승계 → 분할이 번역 리셋을 유발하지 않게 (패널 NE 0.29의 본체).
- **P10-4 SPKFIX 시각 완충 + 버스트 렌더 분산**: 라벨 뒤집힘 배치+페이드, 윈도 커밋 배치를
  시간축으로 분산 반영 ("몰렸다 터짐" cadence).

## 번역 표시 안정성 후속 (2026-08-05 배치의 잔여, 정본 docs/TRANSLATE_DISPLAY_STABILITY.md)

- ~~**NE 베이스라인 실측**~~ ✅ 2026-08-10 완료 (PERF_LOG 상단 배치): ko1.wav × DNA3-4B 실측으로
  0.1.6 NE **2.86** → 0.1.7 **1.18**(−58%). 목표 밴드 0.2에는 미달 — 아래 P8 참조.
- ~~**P8 — 인터림 소스 LocalAgreement 게이팅**~~ ✅ 2026-08-10 구현·실측 완료 (PERF_LOG 최상단):
  `InterimSourceGate`(LocalAgreement-2 + 표면형 커밋 고정 + stall 3 탈출구)를 `livePartial` didSet에
  배선. NE **1.18 → 0.46**(−61%, 0.1.6 대비 −84%), 표시 상태수 7→5, 부수로 DNA 턴 30% 절감.
  비용 = 프리뷰 1주기(~1s) 지연(보류 꼬리 평균 14.4자).
- **P7 — 문장확정 프리셋**: 강의/행사용 옵션 = 인터림 번역 off, 문장 커밋 후 번역만
  (Interprefy standard/instant 이중 모드 선례). 클리닉 기본은 스트리밍 유지.
- **EN→KO 직독직해 스타일**: 엔진 target-prefix 이어쓰기 지원이 생기면 재평가
  (KO→EN은 동사후치 소스라 기각 — 문서 참조).

## 우측 패널 UI 방향 (2026-08-11, 사용자 지시)

- ✅ 녹음 중 WorkspaceExplorer(파일/통계) 숨김 — 라이브 중 무의미한 데이터 대신 전사 영역 확장.
- **AI 요약 계열 기능이 생기면 우측 패널에 "탭 추가" 방식으로 합류** (상시 패널 금지) —
  예: 파일 | 통계 | 요약 | 액션아이템. 라이브 중에는 실시간 가치가 있는 탭만 노출.
