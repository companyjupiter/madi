# 시작하기

Madi(마디)에 오신 것을 환영합니다. Madi는 회의를 **전부 이 Mac 안에서** 받아쓰고 옮기는 앱입니다. 실시간 받아쓰기, 오디오·영상 파일 전사, 화자 분리, 온디바이스 번역까지 — 모든 처리가 여러분의 Mac 안에서 일어납니다.

## Madi가 하는 일

- **실시간 전사** — 마이크나 회의 음성을 들으며 말이 끝나기 무섭게 텍스트가 나타납니다.
- **파일·영상 전사** — 녹음 파일이나 영상을 끌어다 놓으면 전체 대화를 받아씁니다.
- **화자 분리** — 누가 언제 말했는지 구분하고, 이름을 붙일 수 있습니다.
- **온디바이스 번역·통역** — 한국어·English·日本語·中文을 실시간으로 옮기고, 화면 위 자막으로 띄웁니다.
- **AI 요약·질의응답** — 회의가 끝나면 요약·결정·액션을 정리하고, 회의 내용에 대해 자연어로 질문할 수 있습니다.
- **검토와 교정** — 잘못 들은 단어를 찾아 고치고, 고친 내용을 기억해 다음 회의에 적용합니다.
- **시스템 받아쓰기** *(추후 제공)* — 어느 앱에서나 단축키를 누르고 말하면 그 자리에 글로 들어가는 기능을 준비 중입니다.

### 100% 온디바이스 — 내 데이터는 Mac을 떠나지 않습니다

Madi의 핵심 약속은 **주권(sovereign)** 입니다. 음성도, 전사문도, 번역도 클라우드로 전송되지 않습니다. 모델을 받아 둔 뒤에는 인터넷이 끊겨 있어도 동작합니다. 회의 내용은 오로지 여러분의 기기 안에만 존재합니다.

## 시스템 요구 사항

| 항목 | 최소 | 비고 |
|---|---|---|
| Mac | **Apple Silicon (M1 이상)** | Intel Mac은 지원하지 않습니다 (arm64 전용) |
| macOS | **14.0 (Sonoma) 이상** | |
| 메모리 | **8 GB 이상** | 최소 기준 기기는 M1 MacBook Air 8GB입니다 |
| 저장 공간 | **약 1 GB** (전사만) | 번역까지 쓰면 8GB Mac은 +1.31 GB, 16GB 이상은 +2.78 GB |
| 인터넷 | 모델 최초 다운로드 때만 | 이후에는 오프라인으로 동작합니다 |

8 GB Mac은 정식 지원 대상이며, Madi가 자동으로 저메모리 프로필을 적용합니다 — **8GB Mac 사용 가이드** 장을 보십시오.

## 모델 안내

Madi는 두 종류의 AI 모델을 씁니다.

- **Whisper large-v3 Turbo Q8 (약 867 MB, 필수)** — Madi의 독자 Zig/Metal 엔진에서 전사·화자 분리·실시간 프리뷰를 담당합니다. 표준 DMG에서는 첫 실행에 한 번 받습니다.
- **DNA3.0 언어 모델 (선택)** — **번역**, **AI 교정**, **요약·질의응답**에 쓰입니다. 8GB Mac은 2B(약 1.31 GB), 16GB 이상은 품질 우선 4B(약 2.78 GB)를 자동 추천합니다.
  - 받는 곳: **설정(⌘,) → 번역**의 “번역 모델 다운로드” 버튼.
  - 선택된 모델만 받으며 디스크 공간도 그만큼 필요합니다.

번역·요약·질의응답을 쓰지 않는다면 이 모델은 받지 않아도 됩니다. 전사와 화자 분리는 설치된 Whisper만으로 완전히 동작합니다.

## 메모리 안내 (솔직하게)

- **8 GB Mac (예: M1 MacBook Air)** — DNA3.0-2B와 4턴 번역 대기열을 자동 적용합니다. 번역 엔진의 실측 물리 메모리는 약 2.4 GB입니다.
- **16 GB 이상** — 번역 품질을 우선해 DNA3.0-4B를 사용합니다(실측 약 5.1 GB).
- **실시간 프리뷰**는 확정 전사와 같은 Whisper 프로세스를 공유하므로 두 번째 830 MB 모델을 띄우지 않습니다.

## Madi가 요청할 수 있는 권한

권한은 미리 한꺼번에 묻지 않고, **그 기능을 처음 쓸 때** 필요한 것만 요청합니다.

- **마이크** — 녹음할 때. 녹음을 처음 시작할 때 요청합니다.
- **화면 기록** — 시스템 오디오(Mac에서 재생되는 소리, 예: Zoom·Teams·YouTube)를 녹음할 때만. macOS가 오디오 캡처를 화면 기록 권한으로 묶어두기 때문이며, **화면은 저장하지 않습니다.**
(**손쉬운 사용(Accessibility)** 권한은 **시스템 받아쓰기**(추후 제공)에만 필요하며, 현재 베타에서는 요청하지 않습니다.)

권한을 거부했다가 나중에 켜고 싶으면 **시스템 설정 → 개인정보 보호 및 보안**에서 해당 항목(마이크 / 화면 기록)을 찾아 Madi를 켜면 됩니다.

## 처음 실행 빠른 시작

1. Madi를 열면 **이 Mac에 맞게 Madi 준비하기**가 나타납니다. 다운로드는 아직 시작되지 않습니다.
2. 번역·교정·요약까지 쓸 계획이면 **추천 구성 다운로드**를 고릅니다. Madi가 Mac 메모리를 감지해 8GB에는 DNA3.0-2B, 16GB 이상에는 DNA3.0-4B를 함께 받습니다.
3. 전사와 화자 분리만 먼저 쓸 계획이면 **전사만 준비**를 고릅니다. 필수 Whisper Q8만 받으며, DNA는 나중에 **설정(⌘,) → 번역**에서 받을 수 있습니다.
4. 추천 구성을 받는 중 Whisper가 먼저 준비되면 **전사를 먼저 시작하고 DNA는 계속 받기**를 눌러 바로 시작할 수도 있습니다.
5. 모델 준비가 끝나면 **회의 정보**에서 입력 언어·회의 모드·화자 수를 고르고, **지금 녹음 시작**을 누르거나 오디오·영상 파일을 끌어다 놓습니다.
6. **정지**를 누르면 거기까지의 기록이 정리됩니다. 자동저장이 켜져 있으면 파일로 저장됩니다.

> 기존에 Whisper Q8이 설치된 사용자는 업데이트 후에도 온보딩을 다시 거치지 않습니다. 8GB 전용 상세 가이드는 모델 온보딩과 겹치지 않게 이후 실행에서 최초 한 번 열립니다.

## 창 구조

녹음이 시작되면 창은 세 칸으로 나뉩니다.

- **왼쪽 — 세션 패널**: 녹음 버튼(일시 정지·정지), 총 시간, 화자 막대, 에너지 흐름 그래프. 설정(톱니)도 여기 있습니다.
- **가운데 — 전사문**: 받아쓴 내용. 여기서 읽고 고칩니다.
- **오른쪽 — 작업 폴더 탐색기**: 지난 회의 목록, 자동저장 스위치, 저장 폴더, 내보내기.

## 단축키

| 단축키 | 하는 일 |
|---|---|
| **⌘,** | 설정 열기 |
| **⌘K** | 명령 팔레트 (아래 참고) |
| **⌘F** | 전사문에서 찾기 |
| **⌘R** | 녹음 시작 / 정지 |
| **⌘P** | 일시 정지 / 재개 |
| **⌘?** | 이 사용자 매뉴얼 열기 |

### 명령 팔레트 (⌘K)

**⌘K**를 누르면 검색창이 뜹니다. 메뉴를 뒤지지 않고 이름으로 바로 실행합니다. (View 메뉴의 **명령 팔레트**로도 열 수 있습니다.)

- **요약 생성 / 화자별 요약** — 회의 요약 시트 열기 (전사문이 있을 때).
- **새 세션** — 현재 전사를 비웁니다.
- **작업 폴더 변경** — 저장 위치를 고릅니다.
- **화자 이름** — 이 회의의 화자로 바로 이동합니다.
- **지난 회의 이름** — 저장된 회의록을 열어 줍니다.

## 도움말 메뉴

- **Madi 사용자 매뉴얼 (⌘?)** — 이 문서를 엽니다. 인터넷 없이 열립니다.
- **8GB Mac 사용 가이드** — 8GB급 Mac에서만 나타납니다.
- **업데이트 설치…** — 새 버전이 있는지 확인하고 설치합니다.
- **정보** — 버전과 라이선스를 보여 줍니다.

준비 완료입니다. 더 자세한 내용은 녹음, 화자, 번역, 받아쓰기, 설정 등 다른 페이지를 참고하세요.
